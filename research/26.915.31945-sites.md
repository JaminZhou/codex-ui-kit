# Current Sites boundary — 26.915.31945

The installed `26.915.31945` / build `9922` package (`Chromium
153.0.8010.48`, app.asar SHA-256
`1f7939c1c781887c167043c4d1d307af3400d324685cfc315dfe2f80e634f483`) was
sampled through a loopback-only CDP endpoint in an isolated second process.
At the native 2560×1318 Renderer viewport, `Add files and more → Sites`
opened a first-use terms dialog. Its dialog is 600×428.359375px with a 25px
radius; the primary `Continue` action is 520×36px with a pill radius.

Continuing does not expose a Sites index in this account/runtime boundary. It
returns to the app shell with a `Back to ChatGPT` header and an embedded
pricing webview. The account and plan query values are intentionally redacted
from [`current-sites-26.915.json`](current-sites-26.915.json).

The capture is read-only. It does not create, publish, remove, or inspect a
site, and it does not claim pricing, entitlement, billing, visitor-submitted
content, third-party site runtime, compact viewport, or real native-window
resize behavior. The controlled two-row Sites index remains a separate
legacy-replay contract; `app.sites-index` therefore remains `partial`.

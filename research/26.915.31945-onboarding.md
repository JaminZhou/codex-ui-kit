# Codex Desktop 26.915.31945 onboarding route reachability

The installed `26.915.31945` / build `9922` package was opened once in a
disposable loopback CDP profile on 2026-09-21. The sanitized record is
[`current-onboarding-26.915.31945.json`](current-onboarding-26.915.31945.json).

The probe did not enter credentials, click an authentication control, or
navigate the existing signed-in process. `initialRoute=/login` resolved to the
authenticated shell (one main, one navigation, one Composer textbox), with no
login/onboarding markers. `initialRoute=/onboarding` resolved to a document
with one status region but no main, navigation, dialog, input, or login marker.

This is current-build route reachability evidence, not a LoginPage runtime
capture. The controlled playground LoginPage remains the implementation and
Browser/Electron/pixel contract for the public provider/API-key/device-code
states, while credentials, first-run persistence, native auth handoff, and
installed-product onboarding pixels remain explicitly open. A renderer-side
720×680 request also left the native viewport at `2560×1318`, so no compact
product claim is inferred from that request.

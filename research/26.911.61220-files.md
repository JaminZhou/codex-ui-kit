# Codex Desktop 26.911.61220 Files and folders boundary

This note records a read-only Renderer probe of the installed Codex Desktop
`26.911.61220` build. It does not claim that a file was selected, uploaded,
parsed, or sent.

## Fingerprint

- App version: `26.911.61220`
- Build: `9647`
- Chromium: `153.0.8010.36`
- `app.asar` SHA-256:
  `f3ea5ab82e0503d283c98ea21b0ecfde4d8fe4c740fb1d81dfc57b6f5c22ce1f`
- Capture date: 2026-09-18

## Probe

The probe used a fresh loopback-only CDP process on port `9977` with a unique
temporary profile. The selected Renderer target was `app://-/index.html` at
`2560×1318`; auxiliary avatar, detached-window, and webview targets were not
used. The `Add files and more` menu was opened, and its visible `Files and
folders` button was inspected without choosing a path.

Before and after opening the menu, the Renderer exposed zero
`input[type=file]` elements. Enabling the CDP file-chooser interception hook
also surfaced no Renderer file-chooser event when the `Files and folders`
button was activated. No native dialog was accepted, no path was read, and no
attachment card was created. This is evidence of a host/native file-selection
boundary, not evidence that selection or upload is unavailable in the product.

## Boundary

The current-build attachment row therefore gains a precise current runtime
observation for the menu-to-native boundary. Selection, drag/drop, upload
progress, parse failure, preview decoding, removal, and sent-message ownership
remain separately scoped to the existing `26.825.51511` capture and controlled
replay contracts. The promoted global baseline remains unchanged.

# Codex Desktop 26.901.51231 baseline discovery

Status: package identity observed on 2026-09-07; runtime refresh pending.
This is not a completed Browser, Electron, or pixel acceptance record.

## Installed identity

- Bundle: `/Applications/ChatGPT.app`
- Version: `26.901.51231`
- Build: `8109`
- Chromium declared by the bundle: `152.0.7977.83`
- `app.asar` size: `296211444` bytes
- `app.asar` SHA-256:
  `64fc2f27d2dddfa968acfacbe5e4e0328071bdc406351ff4a7d18f0b4692c83d`

Only metadata and the archive digest were inspected; no bundled source or
assets were extracted. The fingerprint has not yet been matched to a sampled
Renderer with the capture script's before/after identity checks.

## Runtime evidence gap

The historical main CDP endpoint at `127.0.0.1:9874` refused connection.
Listener/process inspection found no active Codex CDP instance. Neither result
establishes that a product surface is missing or that the application itself
is unavailable. Existing user windows were not navigated or resized.

The inventory remains pinned to `26.825.51511`; no evidence status is promoted
by this discovery. The 427-frame replay suite verifies its recorded scenarios,
not the newly installed product.

## Next capture

1. Start one isolated loopback-only CDP instance with a unique temporary
   profile; preserve the user's primary process and account data.
2. Use `scripts/capture-current-baseline.mjs` to verify bundle identity before
   and after sampling and select the main Renderer by URL and shell landmarks.
3. Re-observe shell, sidebar, conversation, Composer, Review, Terminal,
   Markdown, and MCP anchors at the declared viewport matrix. Record changed
   and unreachable states explicitly rather than weakening old assertions.
4. Update the inventory only for observed surfaces, then implement and gate
   each changed slice with Browser/CDP, Electron, and owned-region pixels.

Screenshots and account-specific runtime observations remain local-only.

## Initial runtime probe

An isolated instance subsequently started successfully on loopback port 9897.
Its CDP version endpoint reports Chromium `152.0.7977.83`, matching the bundle.
The original main window was not targeted. Visible DOM measurements on the
application page containing navigation and an editor returned:

- viewport: 1180×820;
- navigation: 321.875×774px;
- editable Composer region: 712×44px;
- document horizontal overflow: false.

A second application page had no navigation or editor. It is not a substitute
for the main Renderer; URL alone is insufficient for target selection.

The full legacy capture exited 1 at `inspectNativeProjectMenu`: the expected
internal native-item provider was unavailable. This is a capture-contract
failure, not evidence that the visible project menu is absent. No completed
baseline JSON or surface promotion is claimed. Replace internal-provider
inspection with visible DOM/native-menu observation for the new capture path,
while retaining prior-build contracts as historical tests.

## Project-menu follow-up and access boundary

The initial project-row hover was intercepted by main content while the
sidebar's DOM remained mounted and exposed `Show sidebar`. Thus a nonzero
navigation rectangle and CSS visibility alone do not prove interactive
visibility. The capture must establish the expanded sidebar state before
measuring its usable layout or interacting with project rows.

After explicitly showing the isolated sidebar, a normal project-menu trigger
click succeeded; the Renderer contained zero visible `[role="menu"]` nodes.
This does not prove menu absence or establish its native contents.

The native computer-use tool then explicitly denied access to `com.openai.codex`
for safety reasons. Native observation is blocked in this environment; do not
substitute another access path to bypass that restriction. No menu labels,
geometry, actions, or pixel parity are verified by this follow-up. A permitted
capture environment or user-provided native-menu evidence is required to
complete that gate.

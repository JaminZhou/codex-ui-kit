# Codex Desktop 26.915.31945 plugin detail boundary

This note records a read-only observation of the installed Codex Desktop build
on 2026-09-19. It refreshes the plugin-detail surface without promoting
installation, connection, or prompt execution behavior.

## Fingerprint

- App version: `26.915.31945`
- Build number: `9922`
- Chromium: `153.0.8010.48`
- `app.asar` size: `358872526` bytes
- `app.asar` SHA-256:
  `1f7939c1c781887c167043c4d1d307af3400d324685cfc315dfe2f80e634f483`

## Capture boundary

One isolated Renderer used loopback-only CDP port `9964` and a unique profile
under `/private/tmp`. The user-owned Codex process was not navigated or
resized. The capture only navigated between public plugin index/detail routes,
opened the existing action and account menus, read computed geometry, and
captured local-only screenshots. It never installed, uninstalled, reconnected,
disconnected, copied a link, followed a link, or submitted a prompt.

The reproducible helper is `scripts/capture-current-plugin-detail.mjs` with
`CODEX_CURRENT_PLUGIN_DETAIL_FINGERPRINT=26.915.31945`. The sanitized record is
[`current-plugin-detail-26.915.json`](current-plugin-detail-26.915.json).

## Re-observed contract

- Native-wide is `1180×820`; the compact emulated viewport is `720×680`.
- The detail scroller is `857.125×774` at x `322.875` on wide and `720×634`
  at x `0` on compact. Document-level horizontal overflow is zero.
- The shared header is `736×129.59375` wide and `688×129.59375` compact, with
  a 58px artwork slot and a 20px/26.6px title style at y `142`.
- Installed detail exposes three suggestions, two Apps, four fixed Information
  labels, four public information links, `Copy link`, `Try now`, and a
  read-only `Uninstall` menu. Discovery detail exposes three suggestions, one
  App, the same Information/link boundary, and `Install plugin`.
- The current account-action menu contains `Rename account`, `Reconnect`, and
  `Disconnect`; the extra rename item is a 26.915 difference from the older
  26.825 record and is retained explicitly.
- Wide bottom scroll reaches `599`px for the installed state and `364`px for
  the discovery state. Compact scroll heights are `1437` and `1158`px.

The record retains screenshot hashes only. Product artwork, account labels,
suggestion text, raw screenshots, process IDs, ports, and profile data remain
outside the repository. Pixel comparison against a committed current-product
reference is intentionally not claimed for this slice because the host-owned
artwork and dynamic content are not stable reference assets.

## Remaining boundary

Clipboard effects, Try-now navigation and submission, real install/uninstall,
connection persistence, OAuth/permission prompts, external information links,
and failure/retry effects remain unsampled. The corresponding inventory row
therefore stays `partial`.

# Renderer study: 26.715.31925

## Sample

- Installed application: `/Applications/ChatGPT.app`
- Bundle identifier: `com.openai.codex`
- Application display name and executable: `ChatGPT`
- App version: `26.715.31925`
- Build number: `5551`
- `app.asar` SHA-256:
  `0c9dd677134340cb944e7642b8bc2504c7b73c7dc334d9d756547858171eea41`
- ASAR header integrity:
  `5cc46a958479595ec01f57d09cd1ff940a05bf11d86c18feb72d4f24d0815f12`
- Bundled Codex runtime: `codex-cli 0.145.0-alpha.18`
- Chromium runtime: `150.0.7871.124`
- Owl runtime archive SHA-256:
  `b6cf8d880cac5a8d2c6aa00f86532e573fd793f202bc852958e6eaafbf84d690`
- Studied on: 2026-07-19

The raw archive extraction was kept in a uniquely named directory under
`/private/tmp` and is not part of this repository.

## Runtime continuity

- Packaged metadata still identifies the application as
  `openai-codex-electron`, using Owl in production and an Electron-shaped build
  pipeline.
- The build continues to declare Electron `42.3.0`, Vite `8.1.3`, TypeScript
  `5.9.3`, and Vitest `4.1.5`.
- The bundled Codex runtime remains `0.145.0-alpha.18`.
- The shared conversation stylesheet remains byte-for-byte identical to the
  `26.707.72221` and `26.715.21425` samples. Its SHA-256 is still
  `8860c371484ef3d1a16013491b0f862a649973eb1c5b23a0e0e98abbac475a33`.

The archive hash and Chromium patch version changed, but neither change is
evidence of a conversation-component delta.

## Conversation continuity

The high-signal conversation anchors sampled in the previous build remain
present: user-message bubbles, virtualized turn content, sidebar open and
closed labels, user-message navigation, latest-message navigation,
reconnection, plan writing, generated images, sources, and progressive
resource expansion.

The visible **chat** terminology introduced in `26.715.21425` remains in use
for loading, creation, archive, pin, and rename actions. The context lifecycle
states also remain unchanged:

| Mode | Running | Completed |
| --- | --- | --- |
| Manual | `Compacting context` | `Context compacted` |
| Automatic | `Context automatically compacting` | `Context automatically compacted` |
| Work | `Optimizing the conversation` | `Optimized the conversation` |

Continuation still distinguishes the current workspace, a new chat, and a new
worktree. No new conversation state branch or reusable component category was
observed in this sample.

## Parity conclusion

The existing public component APIs, state matrices, tokens, browser showcase,
and Electron playground continue to cover the sampled conversation surfaces.
This sample therefore refreshes the current-version baseline without requiring
a new Codex-specific component or transport contract.

## Verification boundary

Automated Computer Use is not permitted to control `com.openai.codex` in this
environment. The Codex-side evidence is limited to read-only packaged metadata,
semantic labels, state branches, and stylesheet continuity. Independently
implemented behavior and geometry are verified in this repository. No
extracted code, CSS, fonts, images, icons, sounds, private IPC, or service
details are included.

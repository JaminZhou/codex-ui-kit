# Codex Desktop 26.730.61639

Observed on 2026-08-06 from the locally installed
`/Applications/ChatGPT.app`.

## Package fingerprint

- App version: `26.730.61639`
- Build number: `6234`
- Chromium: `151.0.7922.71`
- `app.asar` path:
  `/Applications/ChatGPT.app/Contents/Resources/app.asar`
- `app.asar` size: `219945391` bytes
- `app.asar` SHA-256:
  `3fea92820c0fb7a69473e7a8308a8e5b8e91524289a84181a33533ec6cb51d45`
- Package modification time: `2026-08-05T11:45:21+0800`

## Evidence boundary

The probe used exact second-process PID `6418`, loopback-only port `9563`,
and unique profile
`/private/tmp/codex-ui-kit-terminal-current.F4TuJY`. It selected the largest
`app://-/index.html` Renderer by URL and area, fixed the viewport to
1180×820 or 820×680, and operated only the bottom Terminal in disposable new
chat state. The retained contract de-identifies project and transcript text.

The direct shell probes were read-only: `pwd`, `echo`, and bounded `sleep`
commands. One early draft-input probe inherited stale text and invoked `open`
only for two nonexistent relative paths; both failed without creating or
modifying a file. No command was submitted in the user's original process.

Evidence from `26.730.61309` and earlier builds is historical. A surface is
current-build verified only when an independent Browser and Electron contract
matches an observation recorded in this note.

## Terminal session lifecycle

The current bottom Terminal is an xterm surface. The outer panel measures
905.89×279px at x=274.11/y=541 in the 1180×820 Renderer. Its selected tabpanel
starts at y=581 and measures 905.89×239px. The active xterm helper textarea is
labelled `Terminal input`; after the cursor settles it measures about
7.22×16px and remains the keyboard entry point.

The current session sequence establishes:

- one project-named tab is 140px wide and exposes a 20×20 close button;
- the Review/Terminal/Browser/Files picker remains a four-item menu;
- repeatedly choosing Terminal creates three globally numbered tabs;
- three tabs remain visible at 820×680, each measuring about 135.96px, with no
  horizontal overflow;
- closing the selected third tab selects the nearest second tab;
- remaining labels are re-numbered, and the final single tab drops its numeric
  suffix;
- different-workspace sessions retain their own project name while sharing
  the global visible index;
- closing the last tab collapses the panel; using the top Toggle again creates
  a fresh project terminal with an empty transcript instead of restoring the
  closed session.

ArrowLeft moved focus to the active tab but did not change the selected
session in this build. The independent `WorkspacePanel` keyboard behavior is
therefore retained as an accessibility enhancement, not labelled as a current
product observation.

## Process and panel persistence

A real bounded `sleep 3; echo terminal-after-reopen` command was submitted in
one disposable Terminal. Before completion the transcript contained the
command without a returned prompt. The panel was closed, reopened through the
top Toggle while the command was still running, and showed the same incomplete
transcript. After three seconds the same session appended
`terminal-after-reopen` and returned to its prompt. A second session kept its
own earlier output throughout tab switches.

This verifies direct-shell running, output completion, per-session transcript
ownership, and panel close/reopen persistence. The independent public process
list still models host-supplied failed/exited status and reopen actions, but a
failed direct shell command does not add a separate status badge to the
current local Terminal tab.

## Worktree mismatch

Switching the chat from one project to another while the older Terminal was
active retained both project-specific tabs and inserted a warning above the
xterm transcript:

`This terminal's workspace does not match this chat's current worktree`

The warning provides `Dismiss` and `Open new terminal`. Dismiss removes only
the warning. Open new terminal creates and selects a session for the current
chat workspace while preserving the older session.

`TerminalWorkspaceMismatchNotice` implements this host-owned recovery surface.
The full replay also reindexes visible labels, closes the panel on the last
tab, and creates a fresh session from the top Toggle. Browser/CDP covers 93
lifecycle frames, and Electron repeats the mismatch recovery plus the existing
per-session input, close, picker, process-list, and resizing interactions.

Against the exact 906×820 current-build main reference, the independent
single-Terminal scene differs by `0.015119789637709389` in the 272px panel
region and `0.004003990135498351` in the 239px Terminal content region, below
hard limits of 2% and 1% respectively. The non-gated full-main ratio is
`0.086586711893609`; it includes intentionally different conversation-owned
content outside this Terminal slice.

## Concurrent and nested subagents

Two further isolated tasks on 2026-08-07 exercised sibling concurrency and
nested delegation in the current build. The sibling task started Alpha and
Beta together, then passed through `Active · 2`, `Active · 1` plus `Done · 1`,
and `Done · 2`. The summary row moved through `2 working`,
`1 working 1 done`, and `2 done`; the active Beta preview also exposed live
progress text before its final result. Alpha and Beta each opened an
independent transcript.

The nested task started Parent, which then started Child. The side panel
flattened both agents into the same active/done sections rather than drawing a
tree. The public `subAgentActivity.agentPath` values still preserve
`/root/parent` and `/root/parent/child`, so the protocol-neutral reducer keeps
that hierarchy for identity and naming while matching the flat current visual
composition. Child completion produced `Active · 1` plus `Done · 1`; Parent
then settled the shared `Done · 2` list, and both transcripts remained
independently reachable.

The probe used exact second-process main PID `38178`, loopback-only port
`9565`, and unique profile
`/private/tmp/codex-ui-kit-subagent-multi-current.YcVHqU`. It fixed the main
Renderer to 1180×820 and submitted only bounded `sleep`/`printf` commands in
disposable no-project tasks. The user's original PID `99263` remained
separate. No raw application screenshot, session, or profile is tracked.

The independent app-server replay now contains 24 schema-validated fixtures
and 281 events. Browser/CDP covers 111 lifecycle frames; Electron opens the
mixed sibling and nested lists, enters completed Alpha and Child transcripts,
and returns to the shared panel. Nine reviewed pixel scenes cover ten regions:
concurrent running and mixed summaries, mixed/completed panels, transcript,
plus nested running/mixed/completed/transcript and the nested Child/Parent main
activity band. Panel/summary/transcript differences range from `1.3329%` to
`4.6708%`, below the 5.5% hard limit. The compact text-heavy main activity band
differs by `11.4952%`, below its independent 12.5% limit; CDP separately locks
its exact Child-before-Parent text and geometry.

This promotes sibling concurrency, nested path preservation, mixed lifecycle
aggregation, live progress preview, and independent transcript navigation.
Waiting-before-initialization, failure, interruption/cancellation, transcript
streaming, pagination, and transport recovery remain separate gates.

## Visual asset provenance foundation

A separate read-only CDP capture on 2026-08-07 selected the largest current
main Renderer at 1180×820 and recorded only SVG primitives, control semantics,
geometry, computed styles, and font samples. It did not emit conversation or
project text. The capture establishes exact current geometry for the sidebar
mode chevron, Search, attention Activity, New chat, and Quick chat icons.

The main shell, navigation, and Composer all computed to
`-apple-system, system-ui, Segoe UI, sans-serif`; common shell text used weight
445 and the mode control used 500. OpenAI Sans Regular/Medium files exist in
the ASAR, but asset presence alone does not establish their use in these
surfaces. The UIKit default and current playground therefore use the observed
system stack while retaining OpenAI Sans as an explicit opt-in token.

[`visual-assets.json`](visual-assets.json) binds the five promoted icons to
this build fingerprint and their canonical geometry hashes. It also keeps 20
remaining shell/sidebar/Composer approximations explicit and leaves global
pixel parity ineligible until the inventory is complete and that denominator
reaches zero.

The promoted v4 evidence now locks all root and primitive snapshots to the
same 475 standard computed-style property names. A shared pre-output/pre-write
sanitizer permits only safe scalar values and local SVG `#fragment`
references after resolving CSS escapes. It covers both complete icon trees and
de-identified font-style samples before output. Promotion requires an ordered
primitive bijection with no leftovers; the attention Activity source is the
sole explicit derivation and its omitted dot has an exact observed geometry
contract. The capture date is part of every v4 hash and refreshes when the app
fingerprint changes.

The probe used exact second-process PID `51407`, loopback-only port `9570`, and
unique profile `/private/tmp/codex-ui-kit-visual-assets.xKSSwR`. Cleanup sent
`SIGTERM` only to PID `51407` and its two profile-owned Crashpad handlers
`51412`/`51414`; port `9570` closed. The recoverable profile moved to Trash as
`codex-ui-kit-visual-assets-61639-20260807`. No raw application screenshot,
profile, extracted font, or ASAR asset is tracked.

A root-attribute follow-up reused that recoverable profile through exact main
PID `55182` on port `9570`. It added SVG-root `fill` to the geometry hash and
confirmed Quick chat inherits `currentColor`. Cleanup stopped only PID `55182`
and its profile-owned Crashpad handlers `55187`/`55190`; the port closed and
the profile remained in the same Trash item.

A final hardened capture reused the profile through exact PID `62396` and
proved the port/profile command-line binding before connecting. It emitted
only allowlisted semantic IDs and a recursive SVG tree, then confirmed the
five promoted icons' complete rendering attributes. Cleanup stopped only PID
`62396` and Crashpad handlers `62401`/`62403`; port `9570` closed again.

The review follow-up reused the same recoverable profile through exact main
PID `65645`. The capture required an exact executable and argv-token match,
an exact realpath match for the whitespace-free profile, and loopback-only
`127.0.0.1:9570` listener records before connecting. It accepted inherited
listener ownership by the isolated Computer Use child while identifying
exactly one matching main process, waited for the main/navigation/SVG surface,
rejected raw SVG `style` strings, and emitted 29 recursive SVG records plus
three de-identified font samples. Cleanup stopped only PID `65645` and its
profile-owned Crashpad handlers `65650`/`65653`; port `9570` closed and the
profile remained in the same Trash item.

The final isolation review used exact main PID `69830` and proved the kernel
reported executable plus NUL-delimited argv, rather than parsing display text
from `ps`. The shared listener held by Computer Use PID `69934` was accepted
only after its parent chain reached PID `69830`; repeating the capture after
that child appeared still emitted 29 SVG records and three font samples. A
separate disposable Python process confirmed an argument containing trailing
whitespace remains one argv entry and cannot masquerade as an exact flag.
Inline SVG `style` attributes now fail capture instead of producing lossy
geometry. Cleanup stopped only PID `69830` and its profile-owned Crashpad
handlers `69841`/`69843`; port `9570` closed and the profile remained in the
same Trash item.

The visual-attribute follow-up used exact main PID `72944`, fixed the Renderer
to the manifest's 1180×820 viewport, and observed the shared Computer Use
listener at descendant PID `73044`. The strict preflight rejected uncaptured
root `class`/`viewBox` attributes before those two sources were modelled
explicitly. The final capture includes source root class, resolved root style,
rendered size, viewBox, root attributes, and recursive primitives in the v3
hash; it rejects class on descendants and every other unknown visual
attribute. All 29 SVG records and three font samples then captured without
loss. Cleanup stopped only PID `72944` and profile-owned Crashpad handlers
`72950`/`72952`; port `9570` closed and the profile remained in the same Trash
item.

One final class-effect pass used exact main PID `76514` and descendant listener
PID `76616`. It added the computed flex basis/grow/shrink values needed to
replay the promoted root classes' layout behavior, including the mode icon's
explicit `shrink-0`, and regenerated the v3 hashes at 1180×820. Cleanup stopped
only PID `76514` and profile-owned Crashpad handlers `76520`/`76522`; port
`9570` closed and the profile remained in the same Trash item.

The complete-style pass used exact main PID `81773` and descendant listener
PID `81871`. At the fixed 1180×820 dark/resting baseline, Chromium reported
475 standard computed-style properties on every captured SVG root and
primitive. The deterministic updater matched the five promoted geometries,
attached those complete root/descendant snapshots, and included the app
fingerprint, theme, interaction state, and viewport in each v4 hash. A dry-run
regeneration was byte-identical to the tracked 245,953-byte manifest, and all
111 pixel contracts passed without refreshing a baseline. Cleanup stopped
only PID `81773` and profile-owned Crashpad handlers `81779`/`81781`; port
`9570` closed and the profile remained in the same Trash item.

## Cleanup

Cleanup sent `SIGTERM` only to exact probe main PID `6418`, then stopped its
two profile-owned orphan Crashpad handlers `6423` and `6427`. The main process,
its Renderer/service children, modifier monitor, App Server, Computer Use
service, and shell exited; loopback port `9563` closed. The user's original
Codex PID `99263` remained running.

The unique profile and 18 external screenshots moved to the recoverable Trash
item `codex-ui-kit-terminal-61639-20260806`. No raw application screenshot or
profile is tracked by the repository.

The collaboration probe cleanup sent `SIGTERM` only to exact isolated main PID
`38178`, then stopped its two profile-owned orphan Crashpad handlers `38183`
and `38186`. Port `9565` closed, no process retained the unique profile path,
and the user's original Codex main PID `99263` remained running. The profile,
20 external capture artifacts, and ignored probe script moved to the
recoverable Trash item
`codex-ui-kit-subagent-collaboration-61639-20260807`; none is tracked.

A follow-up read-only CDP measurement reused that recoverable profile through
exact isolated main PID `79907` on loopback port `9566`. It confirmed the
completed Child and Parent activity rows use 13/20px weight-445 typography and
retain separate 28px blocks. Cleanup stopped only PID `79907` and its
profile-owned Crashpad handlers `79912`/`79914`; port `9566` closed, no process
retained the profile path, and the user's original Codex PID `99263` remained
running. Two temporary diagnostic crops were moved into the same recoverable
Trash item; no new raw evidence is tracked.

# Codex Desktop 26.915.31945 environment editor boundary

This note records a read-only current-build observation of the local
environment editor. It does not create a worktree, save an environment, run a
setup or cleanup script, or change project configuration.

## Entry and fingerprint

The isolated Renderer opened the run-location menu, selected `New local
worktree`, opened the local-environment picker, and selected `Set up project`.
The route settled on `Edit local environment` at the native `2560×1318`
viewport. The exact sanitized record is
[`current-environment-editor-26.915.31945.json`](current-environment-editor-26.915.31945.json).

- App version: `26.915.31945`
- Build: `9922`
- Chromium: `153.0.8010.48`
- `app.asar` SHA-256:
  `1f7939c1c781887c167043c4d1d307af3400d324685cfc315dfe2f80e634f483`
- Navigation rail: `321.875px` wide below the `46px` titlebar
- Main content: `2238.125px` wide
- Content heading: `768×28.796875px` at `x=1057.4375`, `y=112`
- Document horizontal overflow: `0`

## Observed editor contract

The editor exposes a 288×36px name field, separate setup and cleanup script
textareas at 768×129.375px, four platform tabs for each script, one Add action
control, and one Save control. The observation captured control geometry and
labels only; no private project name, script content, credential, or account
value was recorded.

## Boundary

This closes the current-build reachability and structural evidence gap for the
environment editor entry. It does not prove compact native resizing, populated
environment repair, setup execution, cleanup execution, persistence, Remote
authentication, or installed-product pixels. Those remain separate Stage 3
boundaries and require an independently authorized disposable mutation before
promotion.

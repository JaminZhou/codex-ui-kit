# Codex Desktop 26.820.60940 scoped runtime baseline

This note records the application-shell surfaces re-observed against the
installed Codex Desktop build on 2026-08-27 and 2026-08-28. Evidence that was
sampled only on 26.818 or an earlier build keeps its original version prefix;
this refresh does not relabel those screenshots or lifecycle probes as
current.

## Fingerprint

- App version: `26.820.60940`
- Build number: `7119`
- Chromium: `151.0.7922.170`
- `app.asar` size: `282402769` bytes
- `app.asar` SHA-256:
  `c964aebbf9a6a0f70799d01215c611d8ef6ee63f816b3d57beccddd47a811fd9`
- Installed resource:
  `/Applications/ChatGPT.app/Contents/Resources/app.asar`

The ASAR is 1721740 bytes smaller than the previous 26.818 package while the
Chromium base version is unchanged. The capture hashes the installed archive
before and after sampling and proves that the isolated Renderer started after
the sampled bundle metadata settled.

## Capture boundary

The probe used one user-authorized second app process on loopback-only CDP
port `9881` with a unique profile under `/private/tmp`. Existing Codex windows
were not navigated or resized. Main Renderer selection used the application
URL, viewport area, shell landmarks, and visible-control density; the separate
avatar-overlay Renderer was rejected structurally without reading private
content.

The reproducible capture remains:

```sh
CODEX_CURRENT_BASELINE_CDP_PORT=9881 \
CODEX_CURRENT_BASELINE_PROFILE=/absolute/path/to/the/isolated/profile \
CODEX_CURRENT_BASELINE_ALLOW_NAVIGATION=1 \
CODEX_CURRENT_BASELINE_OUTPUT=/absolute/path/to/the/isolated/profile/current-baseline.json \
pnpm capture:current-baseline
```

The output is a new direct child of the isolated profile and remains
untracked. It contains package identity, sanitized target selection, fixed
public labels, computed styles, geometry, focus state, and counts, but no
screenshots or account, project, repository, or task text.

The account-menu follow-up uses the same isolated-process boundary and an
explicit preference authorization:

```sh
CODEX_CURRENT_ACCOUNT_MENU_CDP_PORT=9881 \
CODEX_CURRENT_ACCOUNT_MENU_PROFILE=/absolute/path/to/the/isolated/profile \
CODEX_CURRENT_ACCOUNT_MENU_OUTPUT_DIR=/absolute/path/to/the/isolated/profile/current-account-menu-run \
CODEX_CURRENT_ACCOUNT_MENU_ALLOW_PREFERENCES=1 \
pnpm capture:current-account-menu
```

It normalizes the responsive shell through 820→721→720px, explicitly pins the
compact sidebar when required, captures Light and Dark, then restores System,
1180×820, and New chat. Screenshots stay local because the surrounding shell
can contain private account, project, and task content. The committed contract
uses only sanitized labels, numeric geometry, styles, icon geometry, focus,
and overflow evidence.

## Re-observed shell contract

- New chat remains structurally reachable at 1180×820, 820×680, 721×680, and
  720×680. The persisted sidebar is 322.90625px wide, the Composer editor is
  44px tall, and every sampled state has zero horizontal overflow.
- The sidebar remains visible at 720px until the explicit Hide action. Once it
  is hidden, the titlebar exposes Show sidebar and no longer keeps visible Back
  or Forward controls. Showing the sidebar restores the navigation and the
  compact Pull requests → New chat route round trip.
- Project-group rows remain 30px tall and preserve pointer, Enter, Space,
  focus, resize, and explicit Hide/Show continuity. Project and recent counts
  remain content-dependent and are not parity constants.
- Help remains one 320×272.06 DOM menu with eight menu items, focus inside the
  menu, Escape dismissal, and trigger-focus return.
- The representative local project menu remains the frozen
  `electronBridge.showContextMenu` provider with six actions, three separators,
  a 24×24 trigger, and a 221×187 Quartz window. Its Unpin, Edit, Reveal in
  Finder, Create permanent worktree, Archive chats, and Remove project SVG
  sources decode to the same sanitized geometry as the narrow public manifest.
  The capture selects this provider by public action IDs rather than assuming
  that the first user-owned project row has the same contextual actions.

## Projects Index drift

The wide 1180×820 Projects Index is unchanged at a 736px content width with
512/64/128 columns, 70px rows, one owned vertical scroller, sort cycling, empty
search, project expansion, and focus continuity. Row count and expanded Recent
chats height remain content-dependent.

At the explicitly collapsed 600×600 gate, the main route now owns a 599px
scroller beginning at x=1. The content and row widths are 559px and the visible
columns are 415/128 instead of the previous 560px and 416/128. This one-pixel
change is locked as 26.820 evidence rather than hidden inside a tolerance-only
version bump.

## Route and selection continuity

A later isolated second process on loopback port `9883` re-entered the current
Projects route through the live memory router without reading or writing task
content. Projects owns a distinct location key; Back restores the preceding
root location and enables Forward, and Forward restores the exact Projects key.
The 28×28px Sidebar, Back, and Forward controls remain at x=88/120/152. Enabled
controls use the default cursor at full opacity, while the unavailable direction
keeps the same color at `opacity: 0.4`.

At 720×680 the pinned sidebar and route history remain intact. Explicit Hide
removes visible Back and Forward alongside the navigation while preserving the
Projects location; Show followed by 1180×820 restoration returns the same route
and history position with zero horizontal overflow. The independent playground
adds a truncating route stack and repeats Projects → selected project chat →
Back → Forward, preserving the selected chat instead of reducing continuity to
New chat only. Browser/CDP and Electron own the interaction gate. The reviewed
pixel scene uses the local-only current product frame and passes at 2.6428% for
the titlebar controls, 4.9332% for the system-font Create crop, and 0.3054% for
the privacy-masked Projects route.

The fresh profile initially exposed the unified ChatGPT source, whose current
fixed navigation omitted Pull requests even though the ASAR fingerprint was
unchanged. That live source/feature-state drift is recorded rather than
misrepresented as an installation change; earlier 26.820 Pull requests evidence
remains valid for its captured source state.

## Account-menu drift

The current account menu was re-observed at Dark/Light × 1180×820/720×680. It
retains the 322.90625px sidebar, 306.90625×188.375px portalled menu, six
28.5625px rows, 9px visual divider, five sampled SVG icons, one avatar, no
`role=separator`, Escape dismissal, trigger-focus return, and zero horizontal
overflow. At 720px the probe uses the product's explicit Show sidebar action
to move from the responsive collapsed state into the pinned state before
opening the footer menu.

Two current differences supersede the previous 26.818 replay: pointer opening
focuses the `role=menu` surface rather than its first `menuitem`, and all six
rows compute to font weight 400 rather than 445. The account trigger width is
content-dependent, so the gate locks its 8px inset, 29px height, footer
position, sidebar containment, and minimum usable width instead of freezing a
private account label. The privacy-masked current-build menu comparisons pass
at 0.3864% Dark wide, 0.3794% Dark compact, 0.5284% Light wide, and 0.5250%
Light compact under the existing independent 0.5%/0.6% ceilings.

## Thread rows, actions, and ordinary status

A second isolated probe re-observed the shared project-task and Recents row
system without persisting task, project, repository, or account strings. The
capture records only content-dependent counts, title lengths, one disposable
title hash, generic action categories, styles, relative geometry, current SVG
hashes, and six local-only 28×30/72×30/140×32 crops. Its reproducible command
requires an isolated profile that already contains one unread task and one
disposable project task visibly blocked on approval:

```sh
CODEX_CURRENT_SIDEBAR_CDP_PORT=9882 \
CODEX_CURRENT_SIDEBAR_PROFILE=/absolute/path/to/the/isolated/profile \
CODEX_CURRENT_SIDEBAR_OUTPUT_DIR=/absolute/path/to/the/isolated/profile/current-sidebar-rows-run \
CODEX_CURRENT_SIDEBAR_WAITING_TITLE_SHA256=sha256-of-the-disposable-task-title \
CODEX_CURRENT_SIDEBAR_ALLOW_CAPTURE=1 \
pnpm capture:current-sidebar-rows
```

The sampled profile contained 17 content-dependent local rows: 11 inside
project groups and six in Recents. Every row measured 306.90625×30 at the
1180×820 gate. Project active-row hover and Recents idle-row hover both expose
the current 19×20 Pin/Archive buttons, 8px gap, 35px/8px right insets, 16×16
icons, and the same current path geometry. Hover replaces the active status
rail rather than composing actions beside it.

The active and unread paths remain a 20×20 rail at an 8px row inset. Active
uses the exact two-path 16×16 spinner with a two-second infinite rotation;
unread uses the same `rgb(131, 195, 255)` centered 8×8 dot. Browser/CDP and
Electron keep the existing deterministic lifecycle contract. Local current
product crops compare against the independent implementation at 3.3796% for
project actions, 3.1944% for Recents actions, 1.9048% for active, and 3.5714%
for unread. The unread ceiling is 4% because the current persisted
306.90625px row width places the otherwise exact dot on a 0.90625px raster
boundary; computed geometry and color remain exact.

The follow-up edge-state probe adds one disposable local project task whose
command is visibly blocked on the product's `Deny`/`Allow once` approval.
Away from that route, 26.820 renders the same 20×20 rail and exact 16×16,
two-path, two-second spinner used by running work; the old `Needs input` pill
is absent. Its privacy-safe 28×30 rail differs from the independent replay by
2.7381% under a 4% ceiling.

The same current capture reaches a live five-row project limit and a single
`Show more` button. The button is a 90.171875×24 control inside a 306.90625×32
`listitem`, with 14px/18px type, `2px 8px` padding, a full pill radius, and no
`aria-expanded`. Activating it reveals all 11 content-dependent rows in that
project and removes the control; no `Show less` appears. Browser/CDP and
Electron repeat the deterministic five-to-twelve one-way reveal, and the
privacy-safe 140×32 product control differs by 3.8393% under a 5% ceiling.

This refresh therefore promotes current-build evidence for normal history
rows, shared project/Recents actions, running, waiting-on-approval, unread,
and long-list expansion.

## Collection empty and worktree success

A follow-up isolated probe creates one disposable project with no source
folders and reaches a real expanded `No chats` collection. The empty row is
306.90625×29 with 14px/21px system type, weight 400, `4px 32px` padding,
0.5 opacity, and `rgba(255, 255, 255, 0.498)` computed paint. Its local-only
140×29 crop differs from the independent empty replay by 2.4138% after both
regions are reduced to foreground masks.

The same loopback-only profile creates a disposable `New local worktree` task
from the real `codex-ui-kit` project and sends an exact no-tool, no-file probe.
The sidebar reaches loading with a 306.90625×30 row, a 14×14 branch marker at
a 39px right inset, and one two-path spinner on the 20px trailing rail. After
the exact response, the spinner disappears and the same branch marker moves
to an 11px inset. The product continues to expose `kind=local` on this row;
worktree identity is therefore established by the selected Worktree context,
the workspace summary, and the branch marker instead of rewriting the raw
attribute. Privacy-safe 56×30 loading/restored crops differ from the replay by
1.0714% and 0%.

The branch source hash is unchanged between loading and restored. The sampled
worktree spinner still has two paths, but its current source hash differs from
the ordinary task spinner; the instantaneous transformed bounding box and
rotation angle remain deliberately outside the contract. The two exact probe
tasks are archived during cleanup, and their IDs, titles, full screenshots,
profile, and raw conversation state remain outside version control.

This follow-up promotes current-build runtime evidence for collection empty
and the sampled New-local-worktree selection, context, workspace, and sidebar
loading → restored success. The affected inventory groups are
`conversation.context-controls`, `composer.project-worktree-selection`,
`app.new-thread-workspace-selection`, `workspace.worktrees`, and
`app.sidebar-worktree-status-indicators`. Collection loading was not
visible during either normal hydration or a hard Renderer reload, so its
one-heading/four-row replay remains package-structural coverage rather than a
26.820 runtime claim. Ordinary error likewise retains its explicitly versioned
26.818 evidence.

## Worktree failure, unread, and Retry recovery

A second 2026-08-28 probe reuses the same installed fingerprint in a new
loopback-only profile on port 9882 and a disposable single-commit repository.
It selects the real `New local worktree` path with `No environment` and
`main`, then makes only that repository's `.git/worktrees` path a regular file.
The resulting task reaches the product's `Worktree setup failed` surface with
`Edit environment` and `Retry`; `git worktree list --porcelain` still reports
only the disposable root, so the failure is not inferred from a replay.

The first failed task was read in the foreground and correctly had no unread
dot. A second controlled task navigates to New chat before settlement and
reaches the current three-track background composition: a 14×14 branch marker
at a 67px right inset, a red 16×16 error at 38px, and the independent blue 8×8
unread dot at 14px. The privacy-safe 84×30 tail has a 0% foreground-mask pixel
difference from the independent playground under the existing 6.5% ceiling.
This distinction prevents the foreground-read failure state from being
misreported as failed-plus-unread.

Moving the blocker aside and activating the real `Retry` control removes the
failure surface, traverses the branch-plus-spinner transition, and creates
exactly one child worktree. The existing exact response remains available;
away from the recovered task, the row retains the branch plus blue unread
rail. The product continues to report raw `kind=local`, so worktree identity
still comes from the selected Worktree context, workspace summary, and branch
marker rather than an invented thread kind.

Both disposable tasks were archived, the exact child worktree was removed
through its owner repository, only the isolated process was terminated, and
port 9882 was verified closed while the pre-existing app process remained
running. The isolated profile and repository were moved recoverably to Trash;
task titles, IDs, repository paths, full screenshots, and raw conversation
state remain outside version control.

## Sidebar loading and ordinary-error reachability audit

The installed renderer source confirms that the global collection loader is
still present on 26.820. It renders `role=status`, `aria-live=polite`, a
screen-reader-only `Loading chats` label, one small 18%-wide shimmer heading,
and four seeded 30–72% shimmer rows. Its top-level branch is reachable for an
initial ChatGPT sidebar-preference read or when the selected source is not pure
Codex and ChatGPT projects or conversations are loading. Browser/CDP and
Electron now enforce this exact accessible structure instead of relying only
on screenshot appearance.

The same source maps an ordinary task row to `error` only when the current
thread runtime reports `systemError` or the latest turn retains a system
error. It explicitly maps `needs_resume` to idle. The public replay's 21×21,
three-path error SVG is byte-for-byte the current package asset; Browser/CDP
and Electron now compare all three path strings as well as its 16×16 rendered
geometry.

Two scoped attempts tested whether these states were safely reachable without
altering the main Codex process. Terminating only the isolated app-server
produced the global `ChatGPT stopped unexpectedly` surface; Restart returned
the disposable task to idle, matching the decoded `needs_resume` branch. A
second isolated profile used a closed loopback proxy, ChatGPT mode, hard
reload, and renderer latency, but persisted rows hydrated before the global
loader appeared. The disposable task was archived, both profiles were moved
recoverably to Trash, port 9882 closed, and the original PID remained alive.
These probes validate the boundary but do not promote ordinary error or
collection loading to current runtime evidence or current product pixels.

## Global success notification

A fresh isolated ChatGPT-mode task reaches a real success notification through
the reversible Pin chat → `⌘Z` path. The task returns to its original unpinned
state and the current product emits `Chat unpinned`. At 1180×820 the live
notification is a focusable Sonner list item inside a polite
`Notifications alt+T` section. The toaster is top-center, starts at y=48, uses
zero offset, an 8px gap, and keeps at most three items visible while retaining
older items in the DOM.

The observed success alert is 170.4375×42px with 8px padding, a 15px radius,
14/21px system text, a 500-weight title, `#011c0b` background, `#40c977`
foreground, and a 0 4px 12px 10%-black shadow. The implementation reuses the
two exact success SVG paths and exact Close path from the installed renderer.
Browser/CDP gates the live-region semantics, bounded mounted queue, computed
styles, geometry, paths, action removal, and focus continuation. Electron
repeats the queue interaction. The unmasked 172×42 product crop differs by
0.8029% under a 1.5% hard limit.

The product source independently confirms info/success/warning/danger/custom
levels, the exact icon paths for the four standard levels, a 4-second default
duration, swipe-right dismissal, and a three-visible toast limit. The public
`error` tone maps to the product's `danger` asset. Only the sampled success path
is promoted to current runtime and pixel evidence; other tones and simultaneous
live stacking remain source-structural plus replay coverage. The isolated
process alone is terminated, and task state, the profile, and full screenshots stay outside
version control.

## Evidence boundary

This refresh promotes the global shell, primary navigation, project-group
lifecycle, representative native project menu, Help menu, account-menu
matrix, shared thread-row/actions geometry, sampled active/unread/waiting
status, collection empty/long-list, worktree loading, controlled failure,
failed-plus-unread composition, Retry recovery, and restored success,
window-chrome visibility, compact route restoration, and Projects Index to
26.820 evidence. The sampled global success notification is also promoted with
current source, runtime, Browser, Electron, and regional pixel evidence.
Current home theme pixels, ordinary error, collection
loading, conversation/tool lifecycles, full Settings routes, Review, Terminal,
unsampled notification variants, and other unsampled states retain their explicitly versioned
earlier-build evidence until they are recaptured.

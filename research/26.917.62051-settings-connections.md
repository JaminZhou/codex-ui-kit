# Codex Desktop 26.917.62051 Settings → Connections candidate

Status: read-only structural observation for `settings.connections`. It does
not promote installed-product pixels or claim the production connection
lifecycle is implemented.

## Fingerprint and capture

- Sampled: 2026-09-24
- App version/build: `26.917.62051` / `10789`
- Chromium: `153.0.8010.53`
- `app.asar`: 370,175,042 bytes, SHA-256
  `c41157d36d701d3228c82e56852f49da2381941a7e5ccfac2d58b7e10e31aefd`
- Renderer viewport: `800×600`, device pixel ratio `1`
- Sanitized record:
  [`current-settings-connections-26.917.62051.json`](current-settings-connections-26.917.62051.json)

One isolated Renderer was selected by its app URL, viewport area, shell
landmarks, and visible-control density. Settings navigation and the three
Connections controls were clicked only to reach/read their public states. The
ASAR identity remained stable across the capture. No switch value, private
project label, credential, or account-specific text was retained. No
connection was created, paired, or changed; no SSH key exchange was started.

## Measured route

At `800×600`, the route content column is `478.125px` wide at x=`321.875px`;
the sidebar navigation is `321.875×554px` at y=`46px`. Horizontal overflow is
zero. The `Connections` heading is `437.125×28.796875px` at x=`342.875px`,
y=`66px`.

The three 28px-high selection controls are native buttons with
`aria-pressed`; none uses `role=tab`:

| Control | Position and size | Selected-state representation |
| --- | --- | --- |
| Control this Mac | x 342.875, y 126.796875, 117.984375×28px | `aria-pressed=true` in its state |
| Control other devices | x 462.859375, y 126.796875, 149.109375×28px | `aria-pressed=true` in its state |
| SSH | x 613.96875, y 126.796875, 44.140625×28px | `aria-pressed=true` in its state |

The Control this Mac panel exposes the public device heading, Allow connections,
Other settings, and Keep this Mac awake; both switch tracks measure `32×20px`.
The actual switch values are deliberately not recorded. Control other devices
shows its explanatory copy and a `57.375×24px` Set up button. SSH shows its
explanatory copy and a `42.312×24px` Add button.

Compared with the separate 26.915 capture at the same nominal viewport, the
26.917 sidebar begins at y=`46px` rather than y=`0`, while the Settings heading
and selection controls are 46px higher. This is recorded as an observed
candidate delta; the capture does not attribute its cause or treat it as a
pixel regression without a like-for-like image comparison.

## Still open

This record verifies only reachability, public labels, control semantics,
geometry, and zero horizontal overflow. Pairing, SSH exchange, connection
mutation/persistence, production Remote behavior, current-build Browser and
Electron replay, and installed-product pixel comparison remain separate gates.

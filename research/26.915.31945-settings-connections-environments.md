# Codex Desktop 26.915.31945 Settings connections and environments boundary

This note records a sanitized, read-only current-build Renderer observation.
It confirms route reachability and measured geometry; it does not save a
connection, environment, project, credential, or native directory selection.

## Fingerprint and capture

- App version: `26.915.31945`
- Build: `9922`
- Chromium: `153.0.8010.48`
- `app.asar` SHA-256:
  `1f7939c1c781887c167043c4d1d307af3400d324685cfc315dfe2f80e634f483`
- Isolated profile: unique private temporary profile; the main Codex process
  was not used or changed.
- Renderer viewport: `800×600`, device pixel ratio `1`
- Effective Settings rail: `321.875px`; main route: `478.125px`; horizontal
  overflow: `0`

The exact sanitized record is
[`current-settings-connections-environments-26.915.31945.json`](current-settings-connections-environments-26.915.31945.json).

## Connections

The current Settings route exposes three selection controls and their public
controls. This 26.915 structural record did not retain their accessibility
roles; the later 26.917 sample verifies native buttons with `aria-pressed`.

| Selection control | Public state/control | Measured geometry |
| --- | --- | --- |
| Control this Mac | Refresh, Allow connections, Keep this Mac awake | control `117.984×28px`; refresh `26×26px`; switches `32×20px` |
| Control other devices | Set up | control `149.109×28px`; button `57.375×24px` |
| SSH | Add | control `44.141×28px`; button `42.313×24px` |

No pairing, SSH key exchange, credential entry, or connection mutation was
started. This is structural current-product evidence only; it does not close
the production Remote registry or authentication boundary.

## Environments

The current Settings route exposes the public local-environment explanation,
project selection, `Add project`, and per-project `Add environment` controls.
The first sampled add-environment control is `28×28px`; project labels and
content-dependent row counts are intentionally not recorded or promoted.

## Native directory-selection boundary

The real `Create project` → `Add a folder on this computer` action was
invoked. The host reported that macOS was locked before the native picker could
be inspected, so no picker pixels or selection result are claimed. This is a
host-state boundary, not a failed renderer route, and it must be retried only
when an unlocked disposable host is available.

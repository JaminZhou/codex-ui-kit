# Codex Desktop 26.803.61601

This refresh promotes only the application shell, current left-sidebar,
ordinary task-status lifecycle, window-chrome, New chat Composer,
environment-entry unavailable route, full-page Settings shell, Git
preferences, Appearance settings, General settings, and fixed route-continuity
anchors to the installed build.
Evidence captured on earlier
builds remains useful regression coverage, but it is not relabeled as
current-product runtime evidence.

## Build fingerprint

- App version: `26.803.61601`
- Build number: `6396`
- Chromium: `151.0.7922.76`
- Installed `app.asar` bytes: `223451508`
- Installed `app.asar` SHA-256:
  `928129601e8b36eccba603114d6912352f2b13182f3a7d60b32166d0e81aafb5`
- Probe date: 2026-08-11
- Environment/sidebar/Settings follow-up probe date: 2026-08-13
- State: dark theme at 1180×820, 820×680, 721×680, and 720×680
  Renderer viewports

## Reproducible shell capture

An independently launched second process used a unique profile under
`/private/tmp` and a loopback-only CDP listener. The capture proved the exact
executable, argv tokens, canonical profile, listener parentage, installed
bundle device/inode/size/hash before and after sampling, and process start time.
Structural selection chose the 1180×820 `app://-/index.html` Renderer with two
`main` landmarks, one navigation landmark, one textbox, and 53 visible
controls. It rejected the 408×400 avatar overlay, which had no navigation or
textbox and only one visible control.

The fixed route sequence re-observed New chat, the wide/medium/721/720 width
matrix, automatic collapse at exactly 720px, explicit compact pinning, Pull
requests navigation, and New chat restoration. The recorded shell geometry is
unchanged from `26.803.41515`:

| State | Sidebar | Main/editor geometry | Overflow |
| --- | --- | --- | --- |
| 1180×820 New chat | 274.11px; one 705px `overflow-y:auto` owner | main 905.89px; editor x=371.05, 712×44 | 0 |
| 820×680 New chat | 274.11px; one 565px `overflow-y:auto` owner | main 545.89px; editor x=302.11, 489.89×44 | 0 |
| 721×680 New chat | 274.11px | main 446.89px; editor x=302.11, 390.89×44 | 0 |
| 720×680 collapsed | navigation absent; Show sidebar visible | main 720px; editor x=28, 664×44 | 0 |
| 720×680 pinned | 274.11px | main 445.89px; editor x=302.11, 389.89×44 | 0 |
| 720×680 Pull requests | 274.11px; current route | main 445.89px; no Composer | 0 |
| 720×680 restored New chat | 274.11px retained | main 445.89px; editor x=302.11, 389.89×44 | 0 |

The exact navigation `scrollHeight` is deliberately not a public invariant.
Fresh profiles observed 909px and 949px depending on the current de-identified
project/task population. The fail-closed contract now requires one owner with
the exact visible height, `overflow-y:auto`, and finite content height larger
than its client height. It no longer mistakes mutable user content for shell
geometry.

## Sidebar and visual assets

The sidebar lifecycle retains six 30px project rows, 30px task rows,
2px/8px child-list block padding, 1px row/group separators,
pointer/Enter/Space expansion, a 214.05px-wide project menu with six ordinary
actions or a 207.94px-high seven-action variant when Mark all as read is
available, and a
200×272.06 eight-item Help menu. The footer still exposes one Help
control and zero standalone Settings controls. A follow-up structurally opened
the redacted account trigger and observed a 258.11×188.38 six-item menu with one
avatar image, six SVGs, a visual 9px grouping band with zero role separators,
and Escape focus return. The sanitized visual pass also observed six
project-task action rows, six Recents Pin/Archive rows, and zero leading glyphs
in those sampled task bands.

All 77 scoped sidebar/menu/window-chrome/Composer/environment/Settings assets were
re-captured and
promoted under the new ASAR fingerprint. Their SVG primitive geometry remains
compatible with the independent renderer. The observed computed-style changes
are the dark scrollbar color from `rgba(255, 255, 255, 0.157)` to
`rgba(255, 255, 255, 0.082)` and the disabled Back/Forward cursor contract to
`not-allowed`. The public window-chrome disabled state now follows that current
contract.

## Persisted sidebar width and environment entry

The 2026-08-13 follow-up deliberately reused an isolated profile whose sidebar
width had been persisted at 322.91px. The 274.11px measurements above remain
the fresh-profile default; they are not a universal fixed width. The wide
resizer still exposes the observed 240–520px range, while the fixed 720px
pinned capture can promote only 240–368px persisted widths so at least 352px
of main content remains. Larger persisted widths fail closed instead of
fabricating a sub-minimum main column; their real narrow overlay/modal behavior
requires a separate capture model. Within the promotable range, main,
Composer, project-row, and menu geometry are derived from the observed width.

The current Composer run-location menu is a 216×189.31 `Work in` menu with
five ordinary `menuitem` roles: Local, New worktree, an external Codex web
anchor, disabled Send to cloud, and Usage remaining. The external anchor is
navigation to `https://chatgpt.com/codex/cloud`; it is not modeled as a local
execution environment and never produces a synthetic cloud working directory.
Selecting New worktree exposes the separate 264×125.69 no-environment menu
with Work without environment, an inert No environments found row, and
Environment settings.

Environment settings routes to the current page rather than the historical
600×600 local-environment dialog. The observed unavailable composition uses a
768px content column, a 24px Environments heading, a 14px Local environments
unavailable heading, and a 768×44.56 status card containing the exact current
unavailable message. The public replay, Browser/CDP, and Electron contracts
cover this route and Back restoration without creating an environment. The
three local-only comparisons pass at 3.9193% for Work in, 3.0724% for the
no-environment menu, and 0.0708% for the unavailable settings composition.

## Sidebar status lifecycle

A separate current-build probe reached an ordinary task while active and again
after it completed in the background. Each task row is 30px high. The trailing
status rail is 20×20 with a 4px right inset; active uses the exact two-path
16×16 spinner geometry with a 2s rotation, while unread uses a centered 8×8
dot with computed color `rgb(131, 195, 255)`. Queuing a follow-up while the
same task remained active did not replace that spinner with a second status.

Read-only package structure confirms that ordinary task presentation owns
`active | waiting | unread | idle`. A distinct pending-worktree lifecycle maps
`queued | creating | setting-up` to loading and `failed` to an error glyph.
That second lifecycle is now runtime-reached with isolated disposable Git
repositories: one real create reaches the loading spinner and restored branch
row; an intentionally blocked `.git/worktrees` path reaches `Worktree init
failed`, the main error details, and Retry; restoring the path and activating
Retry creates the worktree and clears the failure presentation. The main Codex
process and real projects are not touched.

The independent UIKit keeps ordinary and worktree state contracts separate
while retaining legacy aliases for consumers. Browser/CDP verifies the full
158-frame matrix, all five worktree phases, 30px rows, a 14×14 branch marker,
20×20 rail with 4px right inset, exact two-path spinner, and 16×16 red error
glyph at `rgb(255, 103, 100)`. Electron repeats the state and geometry contract.
Local-only active, unread, worktree-loading, worktree-error, and
worktree-restored foreground masks pass at 5.12%, 1.90%, 4.11%, 5.00%, and
1.67%, respectively.

## Worktree persistence and missing-directory lifecycle

A third isolated probe created one real worktree and task in a fresh disposable
repository. After a full app-process restart, the project and task row remained
available with the persistent 14×14 worktree marker. The app reopened at New
chat rather than automatically restoring the exact thread route; selecting the
retained task restored its title, messages, project/worktree context, and
workspace summary. Route auto-restoration and task persistence are therefore
separate contracts.

Moving only that disposable worktree out of place and reopening the task
reached `Current working directory missing` / `This chat's working directory
no longer exists` in a 736×37.125 informational `ASIDE`. The notice has 20px
radius, `8px 8px 8px 12px` padding, an 8px gap, 13px text with 21.125px line
height, and a semibold heading. After the workspace summary settled, Pull
request status became unavailable. The 736×98 Composer remained editable and
a harmless model-only turn completed while the warning stayed visible. No
Retry or repair control appeared, so the public component intentionally adds
neither.

Restoring the directory in place did not clear the warning, and New chat →
retained-task navigation did not recompute it. Restarting the isolated app
after restoration cleared the warning while preserving the project, task, and
conversation. The observed recovery is therefore app-session-latched rather
than a live filesystem watcher contract.

The independent replay combines the current sidebar, selected 30px task row,
14×14 worktree marker, restored two-message thread, 240×225 workspace summary,
missing-directory four-message thread, informational notice, and editable
Composer. Browser/CDP locks geometry and semantics; Electron verifies summary
toggle, a model-only submission, retained-task visibility from New chat, and
the missing-directory latch plus modal-sidebar dismissal after task
reselection at 600px. A local-only
like-owned notice crop differs by 3.2572% under a 4% ceiling. The product
screenshot remains untracked.

## Independent acceptance

A separate `current-dark-shell` scene composes the exact current sidebar,
window chrome, New chat selected state, workspace destination, context bar,
Composer, and all eight visible Composer assets. Browser/CDP verifies dark
theme ownership, selected New chat semantics, 274/906/736px geometry, disabled
navigation cursor behavior, icon order, and zero horizontal overflow. Electron
and the reviewed visual matrix render the same scene.

Against local-only 1180×820 current-product references, like-owned regions
pass at 3.1387% for the semi-transparent top band, 0.4126% for the account-masked
footer, and 0.3146% for the content-masked main region. The independent
120×46 window-chrome crop passes at 2.7536%, while the open account menu passes
at 0.1979% after masking only the de-identified avatar and mutable text. The
top comparison uses a 0.08
pixel color threshold because the transparent native/vibrancy capture changes
text antialias compositing; geometry and the 4.5% regional ceiling remain
separate hard gates. Product screenshots and raw capture records remain
untracked and local-only.

## Current project picker and Projects route structure

An isolated current Renderer re-opened New chat and measured the project
picker at 260×221 with a 28px search field, fourteen 28.5px options inside a
252×143.5 scroll owner, and one fixed `New project` action. The older
`Don't work in a project` action is absent. The independent Electron host now
exposes project-directory selection through a main-process IPC boundary; its
fixture selects only the repository path supplied by the test and returns to
the new workspace without writing to the project.

The installed `projects-index-page` package chunk establishes the current
structural contract for the separate Projects route: searchable Name/Updated
columns, local and pending-worktree statuses, 70px rows with 32px icons, row
actions, and expandable recent chats capped at ten. The public full-app replay
implements ready, expanded, loading, error, partial-error, search-empty, and
720px compact states. Browser/CDP locks all four row heights and compact column
visibility as part of 163 lifecycle frames, while Electron repeats the native
project-selection transition. The Projects route itself was not reached in
the current product runtime, so no current-product pixel ratio is claimed for
that page.

## Current branch creation entry

The isolated current Renderer opens a 296×280 branch menu with a 28px search
field, seven selectable branch rows, and one fixed `Create and checkout new
branch…` action. That action opens a 400×190.56 dialog with 25px corners, a
360×40 `Branch name` field, a disabled empty `Create and checkout` action,
Close, and `Set prefix`. Following `Set prefix` reaches the full Git settings
page and its Branch prefix field. That route now composes the independent
Settings shell and Git preferences described below.

The independent `BranchCreationDialog` keeps Git-reference validation and
mutation in the host, retains modal focus, and blocks dismissal while creation
is pending.
Browser/CDP covers blank, host-error, and selected-created states. Electron
runs invalid-name, duplicate-name, real create/checkout, and switch-back-to-
`main` through host-issued project tokens, including a two-repository routing
check, before continuing through the command → approval → Review → Terminal →
Pull request path. The host also enumerates each trusted repository's actual
local branches; replay-only projects without a token expose a disabled branch
control instead of sending fixture refs to Git. The three reviewed scenes
extend the matrix to 166 lifecycle frames.
Against the untracked current-product screenshot, the masked 400×191 dialog
crop differs by 3.0916% under an 8% ceiling.

Checkout results are scoped to the project that initiated the operation. A
delayed two-project acceptance confirms that Git may finish in the original
repository without overwriting the newly selected project's branch UI; the
new project's branch control remains disabled with a pending notice until the
original checkout settles.
Repository-scoped checkout errors are cleared when the selected project
changes, including the case where a branch disappears after enumeration.
Commands on host-switched branches remain rooted at the trusted project
directory; only explicit worktree selections derive a `.worktrees/...` cwd.
Host branch selection from Codex web restores Local before the next command.
Detached and unborn HEAD states expose no falsely selected ref while retaining
the create-and-checkout action. Unborn symbolic names remain contextual labels
until a commit creates the corresponding local ref.

The current-product create submission itself is not promoted yet: the active
probe has observed only the menu, dialog, validation-ready state, and Git
settings navigation. It will require selecting the isolated disposable
repository through the native macOS directory panel before mutation evidence
can be claimed.

## Current Settings shell and Git preferences

The same isolated Renderer reaches Settings as a full-page route rather than
an app-sidebar panel. At 1180×820 the navigation starts at y=46, remains
322.91px wide, owns a 306.91×687 scroll region, and contains a 258.91×18
searchbox plus 21 items across Personal, Integrations, Coding, and Archived.
Git is the only selected page. The sanitized visual manifest promotes Back,
Search, all 21 item glyphs, and Account's external-link glyph: 24 exact
Settings icons, taking the scoped manifest from 53 to 77.

The Git page owns a centered 768px content column, a 24px heading, a
768×304.81 preference card, one 224×35 Branch prefix field, two switches, two
segmented groups, and two 768×129.38 instruction fields. The 820×680 and
720×680 captures preserve the 322.91px navigation and shrink the main column
without horizontal overflow. Searching for `git` is a grouped global Settings
search rather than a simple item filter; the replay retains the observed Git,
GitHub, keyboard, Hooks, Configuration, Computer use, Connections, Worktrees,
and Browser result families without storing user values.

`SettingsShell` and `GitSettingsPage` expose the independent public API.
Browser/CDP gates 170 lifecycle frames including wide, compact, and light-theme
landmarks, geometry, exact icons, grouped search, toggles, segmented choices,
and theme-aware paint contrast. Electron follows the real Branch →
Set prefix route, edits and saves a harmless instruction fixture, returns to
the app, and continues the host-backed branch workflow. Reviewed internal
1180×820 dark, 1180×820 light, and 720×680 dark baselines pass; local-only
current-product comparisons pass at 2.4214% and 3.0210%. Hooks and the remaining
Settings pages keep their separate inventory rows and are not promoted by this
slice.

## Current Appearance settings

A second read-only Settings probe on the unchanged fingerprint reaches
Appearance at 1180×820, 820×680, and 720×680. The navigation remains
322.91px wide and starts below the 46px titlebar; the main scroll owner now
records that same y=46 boundary directly rather than reproducing the heading
position with an oversized page inset. Its centered content is 768px wide at
1180 and shrinks to 357.09px at 720 with zero horizontal overflow.

The top state contains System, Light, and Dark real radio inputs with three
248×175 previews at 1180 (about 110×78 at 720), followed by a 768×110
two-column diff preview. Light and Dark editors each expose Import, Copy theme,
a 240×328 menu with sixteen code themes, Accent/Background/Foreground, UI and
Code font fields, a translucent-sidebar switch, and a 0–100 contrast range.
The observed default contrast values are 45 and 60. The bottom Preferences
card records Use pointer cursors, two Dock icon radios, System/On/Off Reduce
motion, 11–16px UI and 8–24px code-size bounds, Color/+/- Diff markers, and
Font smoothing.

`AppearanceSettingsPage` is a controlled public component and keeps file
import, copy behavior, and Dock artwork host-owned. The public playground uses
the existing exact Settings navigation icon for the ChatGPT option and a
code-native Codex placeholder; the product's proprietary Dock rasters never
enter the repository. Browser/CDP verifies geometry, all sixteen menu choices,
radio/switch/range/number/keyboard state, light paint, compact overflow, and
the bottom scroll state. Electron independently follows Git → Appearance →
Git → Appearance, proves both controlled states persist, then returns through
Back to app. Four reviewed internal frames bring the matrix to 174.

Local-only ownership-masked comparisons pass at 1.2787% for wide top,
1.8977% for 720px top, and 3.4665% for the bottom Preferences state. General,
Hooks, code review, and all other Settings pages remain separate inventory
rows and are not promoted by this slice.

## Current General settings

The next read-only Settings probe reaches General at 1180×820 and 720×820,
then scrolls the full route without changing a stored preference. The shell
keeps the measured 322.91px navigation and y=46 main-scroll boundary. Content
is 768px wide at 1180 and 357.09px wide at 720 with no horizontal overflow;
the current route is 1,820px and 2,239px tall respectively.

Twenty-one rows are distributed across five cards: Permissions, General,
Composer, Popout Window, and Notifications. The surface exposes twelve
switches, five menus, a searchable list containing Auto detect plus 65
languages, two keyboard-operable segmented choices, the Open source licenses
action, and locally controlled Popout shortcut capture/formatting/cancel. The
observed file destination,
language, speed, send shortcut, follow-up, and notification selections are
host-state evidence only; the independent fixture mirrors them for comparison
but does not declare them product defaults.

`GeneralSettingsPage` is a controlled public component. File-opening behavior,
license presentation, global-shortcut registration, notification delivery,
and persistence remain host callbacks. The playground clears transient
shortcut capture when leaving General without clearing the controlled value.
Browser/CDP gates wide, light, 720px,
hotkey, and bottom states plus all menu/search/switch/keyboard interactions.
Electron independently follows Appearance → General → Git → General →
Appearance, verifies route state persists without crossing ownership, and
returns through Back to app. Six reviewed internal frames bring the matrix to
180, including a dedicated 720px shortcut-capture frame. Optional local-only
product comparisons cover the full wide, the top
720×680 crop, the shortcut-capture frame, and the bottom frame without masks;
their ratios are 4.6143%, 6.7412%, 4.7654%, and 4.8818% respectively. Raw
screenshots remain outside the repository. Hooks/code review and every other
Settings page remain separate inventory rows.

## Cleanup

The refresh used three exact fresh profiles on ports `9801`–`9803`. Two
fail-closed calibration profiles established that navigation content height is
dynamic; the third completed the full capture and local pixel reference. Only
the validated main process trees and their six exact reparented Crashpad
handlers were terminated. All three listeners plus every exact profile argv
were verified absent. The failed profiles and the final
raw/local-only reference profile moved recoverably to Trash; no screenshot or
capture JSON is tracked by the repository.

The account-menu follow-up used one additional exact fresh profile on
loopback-only port `9804`. Its validated main process tree and two exact
profile-owned Crashpad handlers were terminated, the listener and every exact
profile argv were verified absent, and the profile plus local-only product
reference moved recoverably to Trash. Only the de-identified measurements and
sanitized SVG observations are tracked.

The Settings follow-up used one isolated profile on loopback-only port `9823`.
After exact assets, computed geometry, grouped search, and wide/compact pixel
references were captured, only validated PID `34307`, its exact descendants,
and the two profile-owned Crashpad handlers were terminated. Port `9823` and
all exact profile argv were verified absent, the user's pre-existing Codex PID
`94321` remained running, and the profile plus local-only screenshots moved
recoverably to Trash. The ignored probe was removed; only sanitized values,
machine-readable assets, and reviewed independent baselines are tracked.

The Appearance follow-up used one exact isolated profile on loopback-only port
`9831`. After the wide/medium/720 structure, computed styles, sixteen-option
menu, and top/bottom local-only references were captured, only validated main
PID `51840`, its exact descendants, and the two profile-owned Crashpad
handlers were terminated. Port `9831` and every exact profile argv were
verified absent; the user's pre-existing Codex PID `94321` remained running.
The ignored probes, profile, and screenshots moved recoverably to Trash. Only
de-identified measurements, public implementation, and reviewed internal
baselines are tracked.

The General follow-up used one exact isolated profile on loopback-only port
`9832`. After the full route, five menus, shortcut-capture state, computed
styles, and wide/720 local-only references were captured, only validated main
PID `40290`, its exact descendants, and the two profile-owned Crashpad handlers
were terminated. Port `9832` and every exact profile argv were verified absent;
the user's pre-existing Codex PID `94321` remained running. The profile,
probes, JSON, and screenshots moved recoverably to Trash. Only de-identified
measurements, the public implementation, and reviewed independent baselines
are tracked.

The task-status follow-up used another exact second process with an independent
Chromium profile on loopback-only port `9811`. The Chromium profile was unique,
but the application task store remained shared, so the probe was limited to a
disposable test task. Its exact main process tree and profile-owned Crashpad
handlers were terminated; the listener and every exact profile argv were
verified absent. The full probe root, including local-only status screenshots,
moved recoverably to Trash. The user's primary application process was not
touched.

## Evidence boundary

This refresh promotes 19 inventory groups that were directly re-observed. It
does not promote prior MCP, Markdown, approval, attachment, subagent, Terminal,
Review, or Pull request lifecycle captures to `26.803.61601`. Those retain
their recorded build prefixes until separately re-reached on the current
product.

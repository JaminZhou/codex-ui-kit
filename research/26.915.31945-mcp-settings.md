# Codex Desktop 26.915.31945 MCP Settings boundary

This note records a read-only Plugins → MCPs probe of the installed Codex
Desktop `26.915.31945` build. The isolated process preserved the user-owned
Codex process and did not save, enable, disable, uninstall, authenticate, or
connect an MCP server.

## Fingerprint and route

- App version: `26.915.31945`
- Build: `9922`
- Chromium: `153.0.8010.48`
- `app.asar` SHA-256:
  `1f7939c1c781887c167043c4d1d307af3400d324685cfc315dfe2f80e634f483`
- Viewports: `1180×820` and `720×680`
- Settings route: `Plugins` → `MCPs`

The current route reports 13 Plugins, 6 Apps, 4 MCPs, 2 Skills, and 2
Marketplace entries. The four server rows are 52px high; the two plugin-
provided rows are 42.5625px high. The wide content root is `768px` wide at
`x=367.4375`; the compact content cards are `679px` wide at `x=21`. The
compact search field is intentionally unmounted and horizontal overflow is
zero in both viewports.

## Read-only lifecycle

The Add menu contains `Create plugin`, `Add a marketplace`, `Add MCP server`,
and `Record a skill`. The current editor exposes both STDIO and Streamable
HTTP forms, links to MCP documentation, and keeps Save disabled until the
form is complete. The existing-server detail exposes URL, bearer-token, and
header fields, an `Update <server> MCP` heading, and Uninstall while keeping
Save disabled on entry. Search preserves focus for the exact empty state
`No MCP servers found`.

The measured wide/compact list, empty, Add-menu, STDIO/HTTP create,
compact-HTTP, and update-detail records are in
[`current-mcp-settings-26.915.json`](current-mcp-settings-26.915.json).

## Boundary

This promotes current-build MCP manager structure and geometry only. Real
save/toggle/uninstall mutations, OAuth or permission prompts, transport and
validation failures, provider state, and installed-product pixel promotion
remain host-owned and unsampled.

# Codex Desktop 26.917.71314 candidate baseline

Status: candidate-only structural observation. The promoted global inventory
baseline remains `26.903.71938`; this capture does not promote installed-product
pixels or claim that every surface family has been refreshed.

## Package identity

- Sampled: 2026-09-25 (same exact installed build revalidated)
- Installed app: `/Applications/ChatGPT.app`
- App version/build: `26.917.71314` / `10954`
- Chromium: `153.0.8010.53`
- `app.asar`: 370,175,042 bytes, SHA-256
  `03108a728bdb1616958ab89587c5495cab0cf4cd1bbe109bdfb186df0a113804`
- Sanitized record: [`current-baseline-26.917.71314.json`](current-baseline-26.917.71314.json)

## Capture boundary

The read-only sample used one isolated Renderer with a unique temporary profile
and loopback-only CDP. The selected page was `app://-/index.html`, ranked using
public landmarks and visible-control density. The app bundle identity matched
before and after capture. Navigation was limited to New chat, Projects, Help,
Pull requests, and Settings → Connections. The Composer resource menu was
opened for inspection, but no file was selected and no prompt was submitted.
Connections tab views were read without toggling switches, pairing, or starting
SSH. The capture retained no screenshot or project/account-specific labels. It
is structural DOM/computed-layout evidence, not a product screenshot or pixel
comparison.

## Observed structure

- New chat shell: `1180×820`, `820×680`, `721×680`, and `720×680` viewports;
  the expanded rail measures `321.88px`, and horizontal overflow is zero. At
  720px the rail remains visible until explicitly collapsed; the collapsed
  state restores the editor to the full content width.
- Projects: the sampled `/projects` route has 15 content-dependent rows at
  `70px` each. Sort, empty-search focus, project-group expansion, sidebar
  collapse/restore, and owned scrolling were observed. Counts are sample data,
  not stable product constants; row/project titles are not retained.
- Sidebar and Help: the existing project group supports pointer and keyboard
  expansion; Help opens and closes with focus restoration. Menu contents are
  content-dependent and only structure is retained.
- Pull requests: the fixed shell route round-trips back to New chat at the
  compact viewport without horizontal overflow or losing the sidebar.
- Composer resource menu: at `1180×820`, the menu measures `736×320px`; its
  `726×310px` inner scroll owner has an `834px` content height. The DOM exposes
  22 visible entries, of which 19 public titles match the explicit safe-label
  allowlist. Item rows measure `28.5625px`; the menu uses a `20px` radius and
  `13px` text. No resource was selected, and no permissions, files, or
  account-specific labels were retained.
- Settings → Connections: at `800×600`, the shell rail is `321.875px` and the
  route main is `478.125px`; the three selection controls are native buttons
  with `aria-pressed`, not ARIA tabs. All three active views were observed with
  zero horizontal overflow. Switch values, device identities, credentials,
  pairing, and SSH operations were not captured or changed.
- A second isolated Renderer capture on 2026-09-25 confirmed the same ASAR
  fingerprint and all eight shell/viewport states. `New chat` exposes
  `aria-current="page"` after route restoration. Sidebar group counts and
  scroll extents changed with the profile's content, so they remain sampled
  observations rather than product constants; no project, task, or account
  labels were retained.
- No installed-product pixels were captured. Project-owned Browser/Electron
  replay frames remain implementation regression evidence, not product-image
  parity.

## Still required before promotion

The previous `26.917.62051` records remain useful previous-build evidence but
are no longer current-package observations. Re-observe all remaining affected
surface families on this exact build, including visual assets/pixels,
conversation states, resource selection/execution, and integrations. Keep
package identity, structural runtime observation, controlled replay, and
installed-product pixels as separate evidence classes; Settings connection
mutation/pairing and production Remote behavior remain open. Do not change the
promoted baseline until the current-build promotion gates are met.

# Codex Desktop 26.917.71314 candidate baseline

Status: candidate-only structural observation. The promoted global inventory
baseline remains `26.903.71938`; this capture does not promote installed-product
pixels or claim that every surface family has been refreshed.

## Package identity

- Sampled: 2026-09-24
- Installed app: `/Applications/ChatGPT.app`
- App version/build: `26.917.71314` / `10954`
- Chromium: `153.0.8010.53`
- `app.asar`: 370,175,042 bytes, SHA-256
  `03108a728bdb1616958ab89587c5495cab0cf4cd1bbe109bdfb186df0a113804`
- Sanitized record: [`current-baseline-26.917.71314.json`](current-baseline-26.917.71314.json)

## Capture boundary

The read-only sample used one isolated Renderer with a unique temporary profile
and loopback-only CDP. The selected page was `app://-/index.html`, ranked using
public landmarks and visible-control density. The app bundle identity matched
before and after capture. Navigation was limited to New chat, Projects, Help,
and Pull requests; no prompt was submitted, no screenshot was retained, and
project/account-specific labels were not recorded. The record is structural
DOM/computed-layout evidence, not a product screenshot or pixel comparison.
The optional Composer resource menu was not sampled in this pass.

## Observed structure

- New chat shell: `1180×820`, `820×680`, `721×680`, and `720×680` viewports;
  the expanded rail measures `321.88px`, and horizontal overflow is zero. At
  720px the rail remains visible until explicitly collapsed; the collapsed
  state restores the editor to the full content width.
- Projects: the sampled `/projects` route has 15 content-dependent rows at
  `70px` each. Sort, empty-search focus, project-group expansion, sidebar
  collapse/restore, and owned scrolling were observed. Counts are sample data,
  not stable product constants; row/project titles are not retained.
- Sidebar and Help: the existing project group supports pointer and keyboard
  expansion; Help opens and closes with focus restoration. Menu contents are
  content-dependent and only structure is retained.
- Pull requests: the fixed shell route round-trips back to New chat at the
  compact viewport without horizontal overflow or losing the sidebar.
- No installed-product pixels were captured. Project-owned Browser/Electron
  replay frames remain implementation regression evidence, not product-image
  parity.

## Still required before promotion

The previous `26.917.62051` records remain useful previous-build evidence but
are no longer current-package observations. Re-observe all affected surface
families on this exact build, including visual assets/pixels, conversation
states, notifications, and integrations. Keep package identity, structural
runtime observation, controlled replay, and installed-product pixels as
separate evidence classes; do not change the promoted baseline until the
current-build promotion gates are met.

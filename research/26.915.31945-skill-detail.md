# Codex Desktop 26.915.31945 Skill detail boundary

This note records a read-only observation of the installed Codex Desktop build
on 2026-09-19. It refreshes the installed Skill detail and unsent `Try now`
entry without changing enablement, uninstall state, or prompt execution.

## Fingerprint

- App version: `26.915.31945`
- Build number: `9922`
- Chromium: `153.0.8010.48`
- `app.asar` size: `358872526` bytes
- `app.asar` SHA-256:
  `1f7939c1c781887c167043c4d1d307af3400d324685cfc315dfe2f80e634f483`

## Capture boundary

One isolated Renderer used loopback-only CDP port `9965` and a unique profile
under `/private/tmp`. The user-owned Codex process was not navigated or
resized. The capture opened one installed public Skill, inspected its existing
actions menu, scrolled its instruction region, and clicked `Try now` only to
observe the unsent draft. It never toggled, opened, revealed, copied,
uninstalled, or submitted.

The reproducible helper is `scripts/capture-current-skill-detail.mjs` with
`CODEX_CURRENT_SKILL_DETAIL_FINGERPRINT=26.915.31945`. The sanitized record is
[`current-skill-detail-26.915.json`](current-skill-detail-26.915.json).

## Re-observed contract

- Native-wide is `1180×820` with a centered `600×720` dialog at `(290, 50)`;
  compact is `720×680` with a `600×680` dialog at `(60, 0)`.
- The instruction scroller is `558×490` at wide and `558×450` at compact,
  with `scrollHeight=1890` and zero document-level horizontal overflow.
- The enabled switch remains a 32×28 `switch` with `aria-checked=true`.
  Close, More actions, Try now, and Uninstall retain fixed 24/28px targets.
- The existing actions menu remains `Open`, `Reveal in Finder`, and
  `Copy Markdown`.
- Compact `Try now` closes the dialog and creates an unsent contenteditable
  textbox at `664×44`. The current build renders the inline skill mention as
  a `636.203×37` row, with Add files and Send at 28×28; Send is enabled.

The record retains screenshot hashes only. Host artwork, skill instructions,
account/project labels, raw screenshots, process IDs, ports, and profile data
remain outside the repository. Pixel comparison against a committed
current-product reference is intentionally not claimed because dynamic
instructions and host artwork are not stable reference assets.

## Remaining boundary

Enable/disable persistence, Open/Reveal/Copy effects, real uninstall, Try-now
submission, successful execution, and failure/retry remain unsampled. The
corresponding inventory row therefore stays `partial`.

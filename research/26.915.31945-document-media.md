# Codex Desktop 26.915.31945 document/media route reachability

The installed `26.915.31945` / build `9922` package was opened once in a
disposable loopback CDP profile on 2026-09-21. The sanitized record is
[`current-document-media-26.915.31945.json`](current-document-media-26.915.31945.json).

The read-only probe tested `initialRoute=/workspace-document-preview`,
`initialRoute=/workspace-image-preview`, and `initialRoute=/artifact`. Each
candidate first exposed a blank Renderer, then one generic status region; at
15 seconds a main container appeared without navigation, dialog, input,
button, data-frame, or data-testid markers. No document, image, or artifact
preview panel became reachable. The native window remained `2560×1318` for
the entire probe; no compact product viewport was inferred.

This is current-build route reachability evidence, not a document/media
runtime capture. The controlled playground document and generated-image
routes remain the implementation and Browser/Electron/pixel contract for
loading, ready, and error states. Installed-product document decoding, image
generation/decoding, artifact selection, and product pixels remain open.
No credentials, authentication controls, prompts, or private task text were
used or recorded.

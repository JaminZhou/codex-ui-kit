# Codex Desktop 26.803.41515

This note records a narrowly scoped visual-asset refresh. It does not replace
the broader `26.730.61639` UI inventory baseline or promote untested routes,
lifecycles, Browser flows, Electron flows, or pixel scenes to this build.

## Build fingerprint

- App version: `26.803.41515`
- Build number: `6321`
- Installed `app.asar` SHA-256:
  `5f6e773aafd542d3cf09e10b5dca6cabd301d0a155f4b8ce870e3915fc3da25e`
- Probe date: 2026-08-08
- State: dark theme, resting interaction, 1180×820 viewport

## Read-only runtime observation

An isolated second Codex process used loopback-only CDP port `9570` and the
recoverable profile
`/Users/JaminZhou/.Trash/codex-ui-kit-visual-assets-61639-20260807`. The
capture preflight proved the exact executable, argv tokens, canonical profile,
listener address, and inherited-listener parentage before selecting the
largest `app://-/index.html` Renderer.

The capture returned 77 allowlisted SVG records and three de-identified font
samples. Fixed product labels were converted to allowlisted semantic IDs
inside the Renderer; raw page, project, thread, account, title, aria-label,
test-id, and screenshot data were not written to the repository.

## Exact visual promotions

[`visual-assets.json`](visual-assets.json) now binds ten sidebar icons to this
exact build fingerprint:

- revalidated: mode chevron, Search, New chat, Quick chat;
- refreshed: neutral Activity, whose current primitive differs from the prior
  attention-state-derived bell geometry;
- newly promoted: project folder, Pull requests, Sites, Scheduled, and
  Plugins.

The five fixed navigation rows each resolve through one region-scoped semantic
ID. The folder promotion is intentionally structural: six project rows shared
the explicit legacy-geometry hash
`19f3960517971cf25ba4d32df632b5088a93be74278125ac49ce01d854a59124`
and one identical complete visual fingerprint. The updater rejects a missing,
single, ambiguous, or style-divergent replacement instead of choosing one
silently.

The playground's `CurrentBuildIcon` renderer now consumes all ten manifest
entries. The left navigation and project rows use the exact current-build
folder, Pull requests, Sites, Scheduled, and Plugins primitives instead of
their hand-drawn approximations.

## Remaining boundary

Fifteen named approximations remain: sidebar More, Pin, Settings, and Thread;
window chrome Sidebar, Back, and Forward; and Composer Project, Worktree,
Branch, Add files, Permission, Model chevron, Dictate, and Voice. Global pixel
parity remains ineligible until the complete inventory is known and this
denominator reaches zero.

The public npm package boundary is unchanged: the manifest and renderer remain
research/playground-only, while the package publishes only `dist`.

## Validation and cleanup

The deterministic updater dry run was byte-identical to the tracked manifest.
`pnpm check` passed 620 package tests, 107 Codex playground tests, research and
package contracts, TypeScript, builds, accessibility, and both Electron
playgrounds. The separate acceptance gate passed 111 Browser/CDP lifecycle
frames, the Electron interaction contracts, and 111 pixel contracts without a
baseline refresh.

Cleanup sent `SIGTERM` only to exact probe main PID `21028`, then to its two
profile-owned orphan Crashpad handlers `21034` and `21036`. All other profile
children exited with the main process, loopback port `9570` closed, no process
retained the profile path, and the recoverable profile remained in Trash. Raw
capture JSON and screenshots stayed outside the repository under
`/private/tmp`.

# Codex Desktop 26.803.41515

This note records a narrowly scoped visual-asset, sidebar-action, window-chrome,
Composer, attachment, and subagent-structure follow-up.
It does not replace the broader `26.730.61639` UI inventory baseline or
promote routes and lifecycles outside the sampled sidebar contract to this
build.

## Build fingerprint

- App version: `26.803.41515`
- Build number: `6321`
- Installed `app.asar` SHA-256:
  `5f6e773aafd542d3cf09e10b5dca6cabd301d0a155f4b8ce870e3915fc3da25e`
- Probe dates: 2026-08-08, with the Recents, window-chrome, Composer, and
  attachment follow-ups on 2026-08-09 and the subagent structural follow-up on
  2026-08-10
- State: dark theme, resting interaction, 1180×820 viewport

## Read-only runtime observation

An isolated second Codex process used loopback-only CDP port `9570` and the
recoverable profile
`/Users/JaminZhou/.Trash/codex-ui-kit-visual-assets-61639-20260807`. The
capture preflight proved the exact executable, argv tokens, canonical profile,
listener address, and inherited-listener parentage before selecting the
largest `app://-/index.html` Renderer.

The refreshed Composer capture returned 60 visible allowlisted SVG records and three
de-identified font samples. Fixed product labels were converted to allowlisted
semantic IDs inside the Renderer; raw page, project, thread, account, title,
aria-label, test-id, and screenshot data were not written to the repository.
One overlapping project-clear SVG was excluded because its owning ancestor had
zero opacity and failed the ancestor-aware visibility check.

## Exact visual promotions

[`visual-assets.json`](visual-assets.json) now binds 25 sidebar,
window-chrome, and Composer icons to this exact build fingerprint:

- revalidated: mode chevron, Search, New chat, Quick chat;
- refreshed: neutral Activity, whose current primitive differs from the prior
  attention-state-derived bell geometry;
- newly promoted: project folder, Pull requests, Sites, Scheduled, and
  Plugins;
- promoted by the hover/footer follow-up: More, Pin, Archive, and Help.
- promoted by the window-chrome follow-up: Sidebar, Back, and Forward.
- promoted by the Composer follow-up: Project, Worktree, Branch, Add files,
  Permission, Model chevron, Dictate, and Voice.

The five fixed navigation rows each resolve through one region-scoped semantic
ID. The folder promotion is intentionally structural: six project rows shared
the explicit legacy-geometry hash
`19f3960517971cf25ba4d32df632b5088a93be74278125ac49ce01d854a59124`
and one identical complete visual fingerprint. The updater rejects a missing,
single, ambiguous, or style-divergent replacement instead of choosing one
silently.

The follow-up observed six project-row More instances, five task-row Pin
instances, five task-row Archive instances, and one footer Help instance.
Every repeated category shared one complete visual fingerprint. Off-viewport
SVGs are excluded before promotion, and semantic fallback matching is scoped
to the sidebar project/footer regions. The neutral Activity icon may be
retained only on the unchanged app fingerprint when the live account happens
to expose the separate attention-state variant.

The de-identified capture protocol also records one Help control, zero Settings
controls, five project-task action rows, and zero leading SVGs in those sampled
project-task row bands. A second scoped pass scrolls the same internal
navigation container to one independently identified Recents section, pairs
six Pin/Archive action rows, and records zero leading SVGs in those six row
bands. The updater removes the Settings and `sidebar-thread` approximations
only when their separate fail-closed evidence predicates pass again.

The playground's `CurrentBuildIcon` renderer now consumes all 25 manifest
entries. The left navigation and project rows use the exact current-build
folder, Pull requests, Sites, Scheduled, Plugins, More, Pin, Archive, and Help
primitives instead of their hand-drawn approximations. The same current-build
probe establishes that the footer is Help rather than Settings and that the
sampled project-task and Recents rows do not render a leading thread glyph.
The current composition limits the deterministic Recents fixture to six rows,
omits the leading glyph, and uses the exact Pin/Archive pair; legacy showcase
fixtures retain their historical thread glyph and More action.

The window-chrome follow-up resolves only the visible `Hide sidebar`, `Back`,
and `Forward` instances. The Renderer also retains hidden layout copies of
those controls; the capture now rejects any SVG whose computed visibility is
not `visible`, so hidden and visible snapshots cannot be merged into one
fingerprint. The playground supplies the three exact assets only to the
private current-build composition; the public component defaults remain
independently drawn.

Read-only hover measurements lock project-row action buttons at 24×24 with a
6px gap, nested task action buttons at 20×20 with an 8px gap, and the footer
Help button at 32×32 with an 18×18 icon. Recents action buttons are 24×24 with
a 4px gap. Browser/CDP and Electron repeat these semantics and computed
geometries.

Window-chrome controls remain at x=88/120/152 with 28×28 button bounds and
16×16 visible SVGs. Back and Forward retain the observed disabled state, while
Forward replays the captured horizontal transform. Browser/CDP and Electron
gate the semantic IDs, geometry, and state; the external 120×46 pixel crop
passes at 3.2609% under a 5% ceiling.

The Composer follow-up identifies the three visible 16×16 context assets by
their fixed left-to-right structure and the five bottom actions by allowlisted
labels plus unambiguous 16×16/viewBox-20 permission and 14×14/viewBox-16 model
geometry. Capture fails closed unless it observes exactly three top and five
bottom icons with all eight semantic mappings; the updater independently
rejects any missing, duplicate, or unpromoted Composer semantic ID before it
can set the scoped approximation denominator to zero. The private playground consumes
these exact assets in current conversation and workspace compositions; public
component defaults remain independently drawn. Browser/CDP covers their order
and 14/16px geometry across 64 affected visual states, including one aggregate
contract for 43 current replay scenes, and Electron repeats the full
eight-icon ready-workspace contract. The ready-workspace pixel gate also
requires every icon's rounded `left`, `top`, `width`, `height`, and semantic ID
to equal the current-build bounds before comparing both images at those fixed
reference coordinates. Against the same 1180×820 current-build screenshot,
the aligned workspace comparison passes at 0.3140%; all eight fixed-coordinate
icon rasters have zero changed pixels and are each gated against their own 16%
ceiling before the aggregate check.

## Attachment file-card follow-up

A separate isolated process used loopback-only port `9582`, an independently
owned temporary profile, the exact installed executable, and a 1180×820
Renderer override. The selected target was the `app://-/index.html` page with
the application landmarks and fixed `Do anything` Composer, not the avatar or
authentication pages. The Add files menu exposed one `Files and folders` row,
but no `input[type=file]` existed while it was open. Synthetic clipboard and
drag/drop `DataTransfer` payloads containing three non-sensitive text fixtures
were both prevented by the Renderer and produced zero file cards.

The native picker is therefore a host-owned boundary in this build. The
Computer Use runtime also explicitly refused control of `com.openai.codex`, so
the probe did not open an uncloseable macOS panel or claim a native selection
that was not observed. The independent Electron playground now supplies its
own trusted preload/main-process file-and-folder picker. Its deterministic
acceptance path substitutes five repository-local fixture paths before the
dialog boundary, returns only an identifier, basename, kind, and extension or
`Folder` label to the Renderer, and verifies multi-selection, removal, and
submission while preserving the existing tray and draft when selection is
cancelled. It does not expose absolute paths or private product IPC to the
public package. macOS retains the mixed picker; Windows and Linux first
present an explicit Files/Folders choice and then open a platform-valid
single-kind multi-selection dialog.

Current Renderer script inspection through CDP supplied structural evidence
without copying bundled source into the repository. The sampled current file
card contract uses a maximum 256px card, 12px padding, 10px gap, 40px icon
well, 16px top-right remove control, filename truncation, and extension/line
range or `Folder` subtitle. Current message IDs establish `Uploading…` while
a local file is copied to a remote host, `Unable to upload file` for copy
failure, a separate file-too-large maximum-size error, and a filename-labelled
image progressbar with a 0–100 value. `Preview unavailable` is present for a
current media preview fallback, but that exact Composer transition was not
runtime-reached in this probe.

The public component and private playground now cover file, folder, image,
uploading, upload-error, and preview-error attachments; attachment-only submit
eligibility through an explicit host signal; five-card horizontal overflow;
bounded progress semantics;
retry/removal; host-gated submission while attachments are unsettled; and
1180×820 plus 820×680 composition. Browser/CDP gates five cards, 64px card
height, 40px icon wells, overflow, 62% progress, error/retry, preview recovery,
submit availability, and attachment cleanup across Replay/Live mode changes.
Electron repeats the native bridge selection with five items and the
cancellation path. The existing pasted-image submission
trace remains the only
schema-backed sent-attachment path: non-image local selections are kept as a
host-side pre-submit contract rather than converted into an invented App
Server notification.

Evidence remains tiered. Menu reachability, the absent DOM file input, and
rejected synthetic paste/drop are runtime-observed. Card structure and status
semantics are current-build source-structural evidence. The real product's
post-picker file cards, transfer failure, progress animation, and preview
failure are not current runtime/pixel evidence, so their independent visual
baselines are regression gates rather than product-level pixel comparisons.
The generic file-type badge remains an explicit approximation; the exact
folder primitive reuses the current manifest-backed Composer project asset.

## Subagent recovery structural follow-up

Read-only inspection of the unchanged installed ASAR fingerprint confirms the
public collaboration status set: `pendingInit`, `running`, `interrupted`,
`completed`, `errored`, `shutdown`, and `notFound`. The current side panel maps
only `done` rows into Done, leaves every other presentation status in Active,
shows `Waiting` as a separate trailing status, and limits the first visible
Active and Done pages to four and ten items respectively. No product recovery
transition was synthesized or runtime-claimed.

The independent playground adds one schema-valid twelve-agent trace. It
preserves the raw terminal state while presenting terminal agents in Done,
renders an `interacted` activity as updated and interruption/shutdown activity
as interrupted, keeps waiting in Active, and retains the streamed progress or
terminal message in the panel and transcript. Browser/CDP verifies 132 total
lifecycle frames plus both pagination paths and computed panel styles;
Electron expands the real disclosure, repeats 4/10 pagination, and opens the
failed Validator transcript. Three reviewed internal panel/transcript pixel
baselines pass. This closes the independent waiting, progress, mixed-terminal,
pagination, and transcript slice while leaving real-product recovery
reachability and transport recovery open.

## Markdown streaming and long-content structural follow-up

Read-only inspection of the unchanged installed ASAR fingerprint confirms that
conversation content is represented as `inline-markdown`, remains in progress
until completion, and participates in the latest-turn follow controller's
`static`, prework, and user-follow modes with a 24px bottom tolerance. The
current Markdown table structure also exposes its container, horizontal
scroller, wrapper, cells, copy action, and optional expand-to-preview action.
Those observations supplied the structural boundary for the later runtime
follow-up below.

The independent playground adds a nine-event trace whose four public deltas
exercise an incomplete link, open fenced code, nested and task lists, a
readable multi-column table, and twelve long sections before canonical item
completion. Browser/CDP checks five mutation/completion checkpoints inside the
132-frame matrix. Electron verifies exact code copy, table focus, real
wheel-driven scroll-away, the floating return control, and return to latest.
Four reviewed internal baselines cover the streamed fence, table, long running
tail, and completed tail. Product runtime reachability for those streaming
mutations, images, math, citations/sources, plugin variants, and Markdown error
variants remain open.

## Very-wide Markdown table runtime follow-up

A disposable task in an isolated second process on loopback-only port `9583`
reached the current table implementation with exactly 18 columns and three
data rows at 1180×820. The table has four rendered rows including its header,
a 1665.86px intrinsic width inside an 802/1714px visible/scroll width, 49px
headers, 87px body rows, 24px trailing cell padding, and `break-word`
wrapping. Its 32px action rail transitions from opacity zero to one on hover;
the two buttons are 24×24 and expose `Expand table`/dialog semantics plus
`Copy table`. The captured expand, copy, copied, and close glyphs are simple
current-build vector primitives and are reproduced exactly in the public
component rather than redrawn approximations.

The real Copy action wrote the raw five-line Markdown source, including its
compact `|---|` separator, as `text/plain` with length 1863 and also supplied
serialized table HTML. Expand opened a viewport-sized `Table preview`; the
close control received initial focus at 42×40, and the inner 892.8×389 surface
used `#2d2d2d`, a 1px border, 20px radius, 32px padding, horizontal overflow,
and the same 1665.86×323 table. The product returned focus to `BODY` after
close in this sample; the public `Dialog` deliberately restores the Expand
trigger as its documented accessibility contract, so that focus difference
is not claimed as exact product behavior.

`AgentMarkdown` now copies exact source Markdown plus rendered HTML and exposes
an `allowWideTables` host opt-in for the overhang, action rail, and preview.
Its renderer identities remain stable as later streaming deltas arrive, so an
existing table retains horizontal scroll, preview-open, and copy-feedback state
while source slicing still reads the latest Markdown.
The schema-valid five-event replay, 132-frame Browser/CDP matrix, real Electron
copy/horizontal-wheel/preview flow, and three reviewed internal baselines pass.
At 720×680, the rail moves inward by the measured table overhang, leaving both
24×24 actions fully visible and operable without document-level overflow.
An optional local-only 1180×820 reference gates the owned preview and close
regions at 3.9737% and 0.5929% changed pixels under 4% and 1% ceilings. The
reference remains outside the repository. Table error variants, images, math,
citations/sources, and plugin-specific Markdown remain open.

## Remaining boundary

The scoped visible-shell approximation list is empty after the eight Composer
promotions. This is not a global parity claim: the broader UI inventory and
current-build lifecycle denominator remain incomplete, so global pixel parity
remains explicitly ineligible.

The public npm package boundary is unchanged: the manifest and renderer remain
research/playground-only, while the package publishes only `dist`.

## Validation and cleanup

Two consecutive deterministic updater dry runs were byte-identical to the
tracked manifest. The updater retains only inherited `scrollbar-color` from
the first reviewed capture on an unchanged fingerprint because macOS changes
that non-SVG-pixel property with window activation; every other computed
property is refreshed.

`pnpm check` passed 670 package tests, 141 Codex playground tests, research and
package contracts, TypeScript, builds, accessibility, and both Electron
playgrounds. The separate
acceptance gate now covers 132 Browser/CDP lifecycle frames, the Electron
interaction contracts, and 132 pixel contracts after adding three current-style
wide-table baselines, four independent Markdown-streaming baselines, and five
attachment-variant baselines alongside
the reviewed `current-sidebar-recents` baseline and 64 Composer-affected
states. Against the fresh 1180×820 product
screenshot, the existing top, ownership-masked selected row, and account-masked
footer ratios remain 3.4526%, 0.1163%, and 0.4126%; the dedicated
ownership-masked Recents region passes at 2.5356%.
The window-chrome follow-up refreshes the 14 affected workspace/shell internal
baselines and passes its independent external crop at 3.2609%.
The Composer follow-up also refreshes ten workspace-entry baselines after
aligning the context controls and lower toolbar to the observed geometry. It
passes the full workspace at 0.3140% and the eight fixed-coordinate icon crops
at 0% changed pixels.

Cleanup sent `SIGTERM` only to exact validated probe main PIDs `34179`,
`36619`, `90688`, `98436`, `4302`, `15037`, `20617`, `69337`, `72920`, and
`74915`, followed by validated
profile-owned orphan Crashpad handlers, including PIDs `98440`/`98443` from
the window-chrome run and `4308`/`4310` plus `15042`/`15044` from the Composer
runs. Loopback
port `9570` closed, no
process retained the profile path, and the recoverable profile remained in
Trash. Raw capture JSON and screenshots stayed outside the repository under
`/private/tmp`. The wide-table profile was likewise returned to the recoverable
Trash item `codex-ui-kit-markdown-table.apH3xp`; port `9583` was closed and
the ignored probe scripts were removed.

## Independent light-theme follow-up

A later read-only Appearance inspection confirmed that the installed build
offers System, Light, and Dark choices and that the sampled application was on
System with a dark macOS appearance. A CDP `prefers-color-scheme: light`
override changed the media query result but left the product Renderer dark;
therefore it was rejected as current-product Light evidence. The probe did not
change the application preference and its exact process and loopback listener
were stopped before the temporary profile and screenshot were moved to Trash.

The private shell/workspace playground now has an independent System/Light/Dark
state contract, theme-aware current-icon paint, a current-sidebar plus
workspace light composition, Browser/CDP and Electron interaction coverage,
and one reviewed internal light baseline. Project, Environment, and Worktree
overlays share the light semantic surface/text contract and are opened under
Electron acceptance. The shell success indicator has a contrast-safe light
foreground, and a System launch resolves the native Electron background before
the Renderer loads. Conversation and Pull Request routes retain their
deterministic dark presentation until their custom paint is converted. The
combined matrix reaches 132 CDP and 132 pixel frames. These results prove the
independent implementation only; the current-build light-theme inventory
remains unpromoted until a real Light preference capture and ownership-scoped
external comparison are available.

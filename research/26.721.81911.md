# Codex Desktop 26.721.81911

Observed on 2026-07-29 from the locally installed
`/Applications/ChatGPT.app`.

## Package fingerprint

- App version: `26.721.81911`
- Build number: `5973`
- `app.asar` size: `205927628` bytes
- `app.asar` SHA-256:
  `3c9a101d9beec3718b0fcfc19e427c644a934045f48b3fe0e16b68b0b3f23e61`
- Package modification time: `2026-07-29T15:31:24+0800`

## Evidence status

The package version, build, size, and hash were verified read-only. Fresh
main-Renderer CDP captures now cover the dark application shell, all six
left-sidebar inventory groups, and one real MCP failed-call recovery.

Runtime observations outside the sampled sidebar and MCP recovery from
`26.721.41059` and earlier builds remain useful historical evidence. Browser
and Electron parity results tied only to those observations remain
`partial_legacy` until the affected surface is re-observed on this build.

## Main Renderer selection

The probe launched one exact second process with a separate temporary Chromium
profile and a loopback-only debugging port. It selected the
`app://-/index.html` target by URL, viewport area, and application-shell
landmarks rather than by target order. The smaller auxiliary application page
was excluded.

- Chromium: `150.0.7871.128`
- sampled Renderer: 1180×820
- observed theme: dark
- titlebar-safe inset: 46px
- sidebar/main split: 274/906px
- 820×680 emulation: 274/546px split
- 720×680 emulation: sidebar hidden by default; reopening restores the 274px
  sidebar while leaving the main viewport ownership to the host

The second process exposed a Renderer target but not a controllable native
window through macOS System Events. The compact current-app observation is
therefore Renderer emulation, not a claim about native window resizing.
Independent Electron acceptance covers real 1180×820, 820×680, 800×600, and
720×680 `BrowserWindow` geometry.

The running application stayed dark under light-color emulation, so this
capture does not promote light-theme current-build evidence.

## Sidebar contract

The current main target exposed:

- a 274px sidebar with a 46px titlebar inset and a 70px header from y=46 to
  y=116;
- 30px rows at x=8 with a 258px content width, 12.5px radius, 13px type, and
  an 18.57px line height;
- an 8% white selected-row background;
- primary Codex, Search, New chat, Pull requests, Sites, Scheduled, and
  Plugins controls;
- collapsible Pinned, Projects, and Recents groups;
- dense recent-task rows with running, queued, unread, and error state
  affordances plus sibling action toolbars;
- a navigation scroll owner beginning at y=116 with 54px bottom clearance;
- a fixed account/settings footer from y=774 to y=820.

## Independent acceptance

The protocol-neutral implementation has:

- Browser verification at 1180×820, 820×680, and the 720px modal transition,
  including project expansion, focusable row actions, main-surface inertness,
  and zero console warnings;
- Electron CDP and host verification for the same sidebar lifecycle, including
  a default 1180px window resized to its reachable 720px native minimum,
  modal main-region inertness, Escape dismissal, and focus return;
- deterministic 18-frame full-app pixel baselines;
- an optional ownership-scoped current-build pixel gate using
  `CODEX_UI_KIT_SIDEBAR_REFERENCE`.

The sampled current-build external comparison passed at the strict 0.05 pixel
threshold with changed-pixel ratios of 0.034919708 for the fully unmasked top
controls, 0.001033592 for the privacy-masked selected row, and 0 for the
privacy-masked footer. The top region includes the visible Codex/Search/New
chat/global-route controls so missing glyphs, labels, or typography cannot be
hidden by the gate.

## MCP failure recovery contract

A second disposable task used only the OpenAI Developer Docs integration. It
called `fetch_openai_doc` with `not-a-valid-url`, observed the failure, retried
with `search_openai_docs`, then fetched the first valid result. No shell,
browser, file mutation, or private application interface was used.

The completed main Renderer exposes:

- `Worked for 28s`, with the intermediate assistant explanation and tool group
  owned by the same collapsible timeline;
- `Used OpenAI Developer Docs integration`, even though its first call failed;
- three ordered rows: Fetch, Search, Fetch;
- a 736px-wide neutral failed-call output card containing `plaintext` and
  `Invalid URL`;
- 14px/21px group typography and 25px compact call rows;
- the final assistant recovery sentence and standard response actions.

The protocol-neutral replay preserves that recovered-group rule and continues
into a second turn with two commands, an accepted command approval, one file
change, and a Review panel. Schema validation covers 10 traces and 106 events.
Browser/CDP, real Electron, and reviewed pixels cover 22 lifecycle frames.

The optional 906×820 current-build recovery comparison uses
`CODEX_UI_KIT_MCP_RECOVERY_REFERENCE`. At a 0.05 pixel threshold, the accepted
sample changed-pixel ratios were:

- full main region: `0.029202337`;
- MCP recovery region: `0.051956799`;
- user region: `0.047369405`;
- Composer region: `0.015698995`.

The external screenshot, temporary Chromium profile, and CDP probe were
deleted after validation.

No extracted application source, CSS, assets, fonts, private IPC, or
credentials are retained in this repository.

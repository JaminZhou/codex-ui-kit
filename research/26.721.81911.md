# Codex Desktop 26.721.81911

Observed on 2026-07-29 and reverified on 2026-07-31 from the locally installed
`/Applications/ChatGPT.app`.

This is now a previous-build record. The authoritative current package is
[`26.727.40816`](26.727.40816.md); evidence below remains regression history.

## Package fingerprint

- App version: `26.721.81911`
- Build number: `5973`
- `app.asar` size: `205927628` bytes
- `app.asar` SHA-256:
  `3c9a101d9beec3718b0fcfc19e427c644a934045f48b3fe0e16b68b0b3f23e61`
- Package modification time: `2026-07-29T15:31:24+0800`

## Evidence status

The package version, build, size, and hash were verified read-only. Fresh
main-Renderer CDP captures now cover the dark application shell, all six
left-sidebar inventory groups, window navigation, Pull requests route loading
and selection continuity, and one real MCP failed-call recovery.

Runtime observations outside the sampled shell, sidebar, and MCP recovery from
`26.721.41059` and earlier builds remain useful historical evidence. Browser
and Electron parity results tied only to those observations remain
`partial_legacy` until the affected surface is re-observed on this build.

## Main Renderer selection

The probe launched one exact second process with a separate temporary Chromium
profile and a loopback-only debugging port. It selected the
`app://-/index.html` target by URL, viewport area, and application-shell
landmarks rather than by target order. The smaller auxiliary application page
was excluded.

- Chromium: `150.0.7871.128`
- sampled Renderer: 1180×820
- observed theme: dark
- titlebar-safe inset: 46px
- sidebar/main split: 274/906px
- titlebar controls: 28×28px at x=88, x=120, and x=152 for Sidebar, Back, and
  Forward
- 820×680 emulation: 274/546px split
- 720×680 emulation: sidebar hidden by default; the 12px left edge opens a
  temporary floating preview, while clicking Show sidebar pins the regular
  274px column and leaves 446px for the main route

The second process exposed a Renderer target but not a controllable native
window through macOS System Events. The compact current-app observation is
therefore Renderer emulation, not a claim about native window resizing.
Independent Electron acceptance covers real 1180×820, 820×680, 800×600, and
720×680 `BrowserWindow` geometry.

The running application stayed dark under light-color emulation, so this
capture does not promote light-theme current-build evidence.

## Sidebar contract

The current main target exposed:

- a 274px sidebar with a 46px titlebar inset and a 70px header from y=46 to
  y=116;
- 30px rows at x=8 with a 258px content width, 12.5px radius, 13px type, and
  an 18.57px line height;
- an 8% white selected-row background;
- primary Codex, Search, New chat, Pull requests, Sites, Scheduled, and
  Plugins controls;
- collapsible Pinned, Projects, and Recents groups;
- dense recent-task rows with running, queued, unread, and error state
  affordances plus sibling action toolbars;
- a navigation scroll owner beginning at y=116 with 54px bottom clearance;
- a fixed account/settings footer from y=774 to y=820.

## Independent acceptance

The protocol-neutral implementation has:

- Browser verification at 1180×820, 820×680, and the 720px modal transition,
  including project expansion, focusable row actions, main-surface inertness,
  and zero console warnings;
- Electron CDP and host verification for the same sidebar lifecycle, including
  a default 1180px window resized to its reachable 720px native minimum,
  modal main-region inertness, Escape dismissal, and focus return;
- deterministic 36-frame full-app pixel baselines;
- an optional ownership-scoped current-build pixel gate using
  `CODEX_UI_KIT_SIDEBAR_REFERENCE`.

The sampled current-build external comparison passed at the strict 0.05 pixel
threshold with changed-pixel ratios of 0.034919708 for the fully unmasked top
controls, 0.001033592 for the privacy-masked selected row, and 0 for the
privacy-masked footer. The top region includes the visible Codex/Search/New
chat/global-route controls so missing glyphs, labels, or typography cannot be
hidden by the gate.

## Application-shell continuity contract

The non-mutating Pull requests route exposed an application-owned 46px window
chrome above the route outlet. Sidebar, Back, and Forward are 28px controls
starting at the 82px traffic-light-safe inset; Back was enabled and Forward
disabled in the sampled history state. Activating Pull requests briefly
announced `Loading pull requests`, then restored the selected route.

Renderer emulation across 1180, 961/960, 721/720, 1920×1080, and 2560×1440
retained the selected Pull requests route without horizontal overflow. The
right workspace panel auto-collapsed at 960 and restored above it. The fixed
sidebar auto-collapsed at 720 and restored above it. At 719/720, moving into
the first 12px exposed a temporary floating preview without taking main-route
width; the explicit Show action instead pinned the regular column. Dismissing
the first-use Sites terms dialog also preserved the selected route. A hard
Renderer reload reset the application route to its default, so this evidence
establishes in-session selection continuity, not persistence across process
or hard-page reload.

Read-only ASAR inspection corroborated the responsive state machine without
copying bundled source or styles: 960px coordinates the workspace panel,
720px coordinates the fixed sidebar, 96rem (1536px at the default root size)
changes thread-edge layout, the sidebar defaults to 275px and clamps to
240–520px, the right panel preserves at least 352px of main content and a
320px panel, and layout measurements are normalized by window zoom. The
current default wide Markdown-block limit is 64rem. A 42rem new-thread
override is present, while 48rem is not established as a universal thread
limit on every route.

The independent public contract adds `AppWindowChrome`, `AppRouteOutlet`, and
`AppNotificationRegion`, plus `AppShell` layout-continuity callbacks. Browser
CDP verifies deterministic ready, loading, empty, error, offline,
reconnecting, and stale-data variants. Real Electron acceptance drives an
offline alert through retry, loading, ready content, and a restored
notification, then resizes 1180×820 → 720×680 → 1180×820 while preserving the
selected route and restoring only surfaces that responsive layout
auto-collapsed.

Only the loading and in-session route-selection behavior were reached in the
current Codex build. Offline, error, reconnecting, stale-data, and restored
notification states remain independent synthetic acceptance coverage rather
than current-build runtime claims. The external 120×46 window-chrome
comparison passed at the strict 0.05 pixel threshold with a changed-pixel
ratio of `0.039311594`; the hard limit is 0.05.

## Conversation and Composer lifecycle contract

A disposable task used synthetic prompts to observe the current Composer
without mutating repository files. The 1180×820 main target exposed a 736px
Composer column. Its empty/focused input is 712×44 inside a 736×98 card; a
three/four-line value grows the input to 80px and the card to 134px. Submitting
a real turn clears the value, retains focus, and replaces the send control with
a 28×28 Stop control. Completion and explicit interruption both return the
input to its idle state.

While a real turn was running, a follow-up created a tray inset 13px from the
Composer card. The row exposed Steer, Delete queued prompt, and queued-message
actions. Stop preserved the row, added `Queue paused because you interrupted`,
and exposed Resume; Resume restored the running queue. The permission menu
measured 454.38×224.5 and contained Ask for approval, Approve for me, Full
access, Custom (`config.toml`), and Learn more. The Add files and more menu
showed Goal, Plan mode, Record a skill, and Plugins. The menu was opened only;
no attachment upload or plugin action was performed.

The independent replay has 46 events and 11 turns. Browser/CDP covers the ten
new lifecycle frames plus the shared 36-frame matrix, including multiline,
disabled, attachment, running, queued, queue-paused, scroll-away, message
navigation, windowing, and completion. A real Electron `BrowserWindow` drives
submit → running → queue → Stop → paused → Resume, then navigation and
return-to-latest.

The optional 792×320 regional gates use
`CODEX_UI_KIT_COMPOSER_QUEUED_REFERENCE` and
`CODEX_UI_KIT_COMPOSER_PAUSED_REFERENCE`. At pixel threshold 0.05, the accepted
masked changed-pixel ratios were `0.002722538` for the queued Composer and
`0.004478378` for the paused queue. Masks exclude unrelated transcript text
and fixture-specific labels while retaining the queue/card silhouettes,
insets, controls, background, radius, and spacing.

Exact current-build long-thread virtualization was not re-observed, so message
navigation and windowing remain independent regression coverage rather than a
current-product parity claim.

## Coding workspace entry contract

A non-mutating new-chat capture re-observed the current workspace entry at
1180×820. The main route retained the 274px application sidebar and 46px
titlebar. The new-conversation surface occupied a 768px column, with a 736×98
Composer at x=359/y=706 and a 736×28 context row immediately above it at
y=673. Project, environment, and worktree were independent controls; project
opened a 260×250 dialog containing a 252×142.81 scrollable listbox with 14
options, 28.56px rows, a 12.5px selected-row radius, folder icons, and a
selected check.

The current-product probe opened and dismissed the project dialog without
selecting a real project. It did not create or switch a real worktree.
Environment-dialog, repair, and worktree-selection behavior therefore retain
their previous-build runtime evidence; they are covered in this build only by
the independent public implementation and deterministic acceptance.

The independent Codex App playground now composes the current new-chat entry,
project picker, `Start in` menu, searchable 600×600 local-environment dialog,
branch menu, repair-disabled state, and protocol-backed coding workflow. Real
Electron acceptance travels from synthetic project selection through
environment/worktree selection, command execution, `Allow once`, a two-file
Review, Terminal, and Pull request navigation without fixture-only route
jumps. CDP and pixel gates now cover 49 lifecycle frames, including six
workspace-entry states and six terminal-lifecycle states.

The optional current-build comparisons use
`CODEX_UI_KIT_WORKSPACE_REFERENCE` for the privacy-masked main region and
`CODEX_UI_KIT_WORKSPACE_PROJECT_REFERENCE` for the project listbox. At pixel
threshold 0.05, the accepted changed-pixel ratios were `0.003953319` for the
main workspace region and `0.028824956` for the listbox. The masks remove
private labels while preserving layout, icons, selected-row treatment,
Composer geometry, and context-control placement.

## Current Review rename/delete contract

A separate 1180×820 disposable-task probe used ignored files only and
re-observed the turn-owned file card plus the workspace-owned Review panel.
The delete state exposed 28px Undo and Review actions, `+0 −1`, and a 370px
side panel with `Last Turn`, the deleted path, and one deletion line.

The rename-only task is a useful negative semantic result. Although the public
App Server schema can represent an update with `move_path`, this current
product run rendered the move as separate `+0 −0` file entries and `No
content`, not one combined rename-arrow row. The independent playground keeps
the public rename semantic and separately exercises delete, binary, and
host-derived conflict content; only delete and rename/no-content behavior are
claimed as current-build observations.

The mixed deterministic trace established 43 shared CDP/pixel frames before
the Terminal-lifecycle expansion described below.
Real Electron drives Review-header selection, close/reopen, sibling
preservation, and group Undo. The current-build crops, exact temporary profile,
loopback listener, and disposable ignored files were removed after inspection.

## Current Terminal session contract

A fresh read-only second-process probe re-observed the bottom Terminal on the
installed build without running a command. The selected main Renderer was
1180×820 with a 906px main route. The outer bottom-panel container measured
279px including its resize affordance; its visible tab strip measured 28px and
the tabpanel remained 239px. `Terminal input` stayed the accessible input
name.

The plus action exposed a menu named `Open bottom panel tab` with Review,
Terminal, Browser, and Files. Repeated Terminal activation created
auto-numbered tabs; each tab had its own `Close … tab` button and the active
tab used `aria-selected=true`. Three tabs measured 140px each at 1180×820 and
about 136px each at 820×680, where they fit without horizontal overflow.
Closing the active third tab selected the second. The tab typography measured
13px with an approximately 18.57px line height and 8px internal gap.

The independent public `TerminalPanel` and `TerminalProcessList` keep session
creation, process execution, restore policy, and command submission owned by
the host. A 15-event deterministic trace covers running, failed, exited,
multi-tab, picker, all-closed/restore, and compact states. Browser/CDP and
reviewed pixels now cover 49 frames; real Electron drives selection, per-tab
input, close-nearest, picker creation, close-all/restore, and reopening a
failed process. The actual installed app's running/failed process lifecycle
was not triggered in this probe, so only the session-tab/input/compact contract
is promoted as current-product evidence.

The exact second process, loopback listener, temporary profile, and external
screenshots were removed after inspection. The original Codex process remained
running.

## Current Pull request lifecycle contract

A fresh read-only second-process probe opened public PR `#80` in the current
product. At 1180×820 the application kept its 274px sidebar and 906px main
route, then overlaid a 369.28px non-modal PR detail panel at x=810.72 with a
16px separator beginning at x=801.72. The underlying main route stayed
interactive. Summary exposed Branch, Reviewers, Comments, Checks, Description,
an enabled Merge action for the sampled state, and a disabled empty comment
action. Timeline exposed public comments and bot-review entries.

The responsive contract remained an overlay rather than a reserved grid track:
the panel measured 321.97px at x=638.03 for 960×720, about 319px at x=498.03
for 820×680, and 329.31px at x=390.69 for 720×680 after the sidebar collapsed.
Expand replaced the panel with the full 906px main route and Restore returned
it to x=810.72/369.28px. Back hid the detail while retaining the PR index;
Forward restored Summary and the previous wide geometry.

The independent public implementation adds explicit list/detail loading,
failure, and retry states; running/failed/passed checks; review and comment
submission feedback; merge requirements and blocked/ready/merging/merged
states; plus the same wide, medium, and compact panel lifecycle. Browser/CDP,
real Electron, and reviewed pixels now cover 58 lifecycle frames. Electron
also drives review submission, comment failure/recovery, merge completion,
panel expansion/restoration, and route restoration.

No comment, review, or merge was submitted to the real public PR. The sampled
Code tab remained in a bounded loading state, so successful current-product
Code content is not claimed. Those transitions are deterministic host-owned
acceptance, while the current-product evidence is limited to the read-only
Summary, Timeline, loading boundary, geometry, and navigation lifecycle.

## MCP failure recovery contract

A second disposable task used only the OpenAI Developer Docs integration. It
called `fetch_openai_doc` with `not-a-valid-url`, observed the failure, retried
with `search_openai_docs`, then fetched the first valid result. No shell,
browser, file mutation, or private application interface was used.

The completed main Renderer exposes:

- `Worked for 28s`, with the intermediate assistant explanation and tool group
  owned by the same collapsible timeline;
- `Used OpenAI Developer Docs integration`, even though its first call failed;
- three ordered rows: Fetch, Search, Fetch;
- a 736px-wide neutral failed-call output card containing `plaintext` and
  `Invalid URL`;
- 14px/21px group typography and 25px compact call rows;
- the final assistant recovery sentence and standard response actions.

The protocol-neutral replay preserves that recovered-group rule and continues
into a second turn with two commands, an accepted command approval, one file
change, and a Review panel. Schema validation now covers 13 traces and 170
events. Browser/CDP, real Electron, and reviewed pixels cover 58 lifecycle
frames.

The optional 906×820 current-build recovery comparison uses
`CODEX_UI_KIT_MCP_RECOVERY_REFERENCE`. At a 0.05 pixel threshold, the accepted
sample changed-pixel ratios were:

- full main region: `0.029202337`;
- MCP recovery region: `0.051956799`;
- user region: `0.047369405`;
- Composer region: `0.015698995`.

The external screenshot, temporary Chromium profile, and CDP probe were
deleted after validation.

No extracted application source, CSS, assets, fonts, private IPC, or
credentials are retained in this repository.

# Codex Desktop 26.915.31945 Browser and Computer use settings

The installed `26.915.31945` / build `9922` package was opened once in a
disposable loopback CDP profile on 2026-09-21. The probe opened the account
menu, entered Settings, and selected the existing `Computer use` and `Browser`
pages without changing a setting or starting a connection. The sanitized
geometry is retained in
[`current-settings-browser-computer-use-26.915.31945.json`](current-settings-browser-computer-use-26.915.31945.json).

Both pages share the current `321.88px` Settings rail beginning below the
`46px` titlebar and a centered `768px` content column. The Computer use page
exposes four switches, one textbox, and two visible sections; Browser exposes
five switches, one textbox, and eight visible sections. The observed heading
style is 24px, weight 400, `28.8px` line-height in the dark theme, and there is
zero horizontal overflow at the native `2560×1318` viewport.

This is current-build read-only structural evidence only. Browser profiles,
computer-use execution, permissions, provider/account values, persistence,
Electron behavior, and product-pixel promotion remain open. No private values
or browser content were recorded.

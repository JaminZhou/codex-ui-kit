# Codex Desktop 26.818.41509 scoped runtime baseline

This note records only surfaces re-observed against the installed Codex
Desktop build on 2026-08-23 and the command, MCP, and Markdown slices
re-observed on 2026-08-24.
Earlier build evidence remains attached to its original version and is not
promoted by a passing UIKit test.

## Fingerprint

- App version: `26.818.41509`
- Build number: `6962`
- Chromium: `151.0.7922.170`
- `app.asar` size: `284124509` bytes
- `app.asar` SHA-256:
  `8eb91bd9efbf9a4dd04b9b0afdbfcb4e0bab5da18c1919ad74ca327c00c7e791`
- Installed resource:
  `/Applications/ChatGPT.app/Contents/Resources/app.asar`

The installed resource is a non-empty regular file. The capture records its
device, inode, size, hash, metadata-change time, and the isolated process start
time before and after sampling. The running Renderer therefore cannot be
silently relabeled after an application update.

## Capture boundary

The probe used one explicitly authorized second app process on loopback-only
CDP port `9881` with a unique profile under `/private/tmp`. The user's primary
Codex process on port `9874` was not navigated or resized. Main Renderer
selection used URL, viewport area, shell landmarks, and visible-control density
rather than target order or private text.

The reproducible Renderer capture is:

```sh
CODEX_CURRENT_BASELINE_CDP_PORT=9881 \
CODEX_CURRENT_BASELINE_PROFILE=/absolute/path/to/the/isolated/profile \
CODEX_CURRENT_BASELINE_ALLOW_NAVIGATION=1 \
CODEX_CURRENT_BASELINE_OUTPUT=/absolute/path/to/the/isolated/profile/current-baseline.json \
pnpm capture:current-baseline
```

The output path must be a new direct child of the isolated profile. The JSON
contains package identity, structural target-selection evidence, fixed control
labels, computed styles, geometry, focus state, and counts. It contains no
screenshot or private account, project, repository, or task text.

## Re-observed current surfaces

- New chat at 1180×820, 820×680, 721×680, and 720×680, including the fixed
  route marker, 44px Composer editor, sidebar scroll ownership, controls, and
  zero horizontal overflow.
- Six sidebar project groups with 30px rows, pointer/Enter/Space expansion,
  focus continuity, explicit Hide/Show, the current 721→720 visible-sidebar
  behavior, persisted 322.91px width, and compact route restoration.
  The sanitized current capture now derives the expanded Projects wrapper
  from its visible Recent chats group: 71px collapsed wrapper + 108px group +
  12px structural gap = 191px in the sampled profile. This replaces the stale
  fixed 119px assumption without recording private task labels or counts.
- Primary New chat, Pull requests, Sites, Scheduled, and Plugins destinations.
- The unchanged DOM Help menu with eight `menuitem` rows, 320×272.06 geometry,
  focus inside the menu, Escape dismissal, and trigger-focus return.
- The Projects Index at 1180×820 and explicitly collapsed 600×600: fourteen
  70px rows, 512/64/128 and 416/128 columns, sort cycles, empty search, project
  expansion, focus continuity, and one owned vertical scroller.

## Basic conversation shell

A separate disposable task submitted exactly
`Reply with exactly CURRENT BASIC MESSAGE.` and reached the fixed assistant
reply `CURRENT BASIC MESSAGE.`. At 1180×820 the live product uses a 768px
thread column, a 736px message column, a 329.36×38 user bubble with 20px radius
and 8×12 padding, and a flat 736×22 assistant message. Both messages use the
system stack at 14/22px and weight 445.

The completed response exposes four 26×26 controls in 28px steps: Copy, Good
response, Bad response, and Fork chat from here. Their SVG sources match the
existing exact current-build manifest. The 736×98 Composer exposes five real
controls with current assets and hit targets: 28px Add, 101.06×28 permission,
147.36×28 model, 28px Dictate, and 28px Send.

The independent replay locks those computed styles, labels, assets, and
geometry in Browser/CDP. Electron repeats the native 1180×820 contract, focus
on Fork, and an 820×680 compact resize with no horizontal overflow. A
local-only 768×774 product crop is compared without ownership masks at
`0.004320090439276485` changed pixels under a `0.005` hard limit. The task
profile, full screenshots, and crop remain untracked.

## Command failure, Stop, and same-thread recovery

A disposable current-build task ran exactly one read-only shell command that
printed `CURRENT FAILURE STDOUT` and `CURRENT FAILURE STDERR`, then exited 7.
The completed turn exposed `Worked for 10s`, a `Shell` disclosure, the current
terminal glyph, both output lines, `Exit code 7`, and the exact assistant reply
`CURRENT COMMAND FAILURE OBSERVED`. A no-tool follow-up then returned exactly
`CURRENT COMMAND RECOVERY ACCEPTED` in the same task.

A second exact 120-second read-only command was stopped through the live 28px
Stop action. The completed state read `You stopped after 58s`, retained the
`Background terminal stopped with …` row, and accepted the exact no-tool reply
`CURRENT INTERRUPTION RECOVERY ACCEPTED`. The capture does not infer a
successful command completion from the stopped background process.

The independent traces preserve those current labels, durations, output,
terminal state, and recovery turns. Browser/CDP gates the computed message,
timeline, command, Composer, icon, and response-action geometry. Electron
repeats failure/recovery and interruption/recovery in native 1180×820 and
720×680 windows with zero horizontal overflow. The local-only product regions
are compared without masks: failure differs by
`0.04435362605258635` under a `0.05` hard limit, and interruption differs by
`0.03951164228312384` under a `0.045` hard limit. Full product screenshots,
task data, and the isolated profile remain untracked.

## Native project-menu change

The project action trigger still renders as a 24×24 button with
`aria-haspopup="menu"`, but 26.818 no longer inserts a DOM `role="menu"`.
`app.asar` and the live React owner both resolve the project action provider to
`window.electronBridge.showContextMenu`. The bridge object and method are
frozen in the Renderer.

The sampled pinned local project supplies six selectable actions and three
separators in this exact order:

1. `unpin-project` — Unpin
2. `edit-project` — Edit
3. `project-actions-separator`
4. `reveal-project-folder` — Reveal in Finder
5. `create-permanent-worktree` — Create permanent worktree
6. `project-chat-actions-separator`
7. `archive-project-threads` — Archive chats
8. `project-remove-separator`
9. `remove-project` — Remove project

`mark-project-threads-read` remains a conditional action immediately before
Archive chats when the project has unread activity. The current baseline
contract accepts only those two ordered variants.

For the native observation, the system pointer was moved to the exact CDP
trigger coordinate before invoking the frozen bridge. Quartz then reported one
new ChatGPT-owned layer-101 window at 221×187, anchored at the project action
button. A system Escape event closed it and returned the Renderer trigger to
`aria-expanded="false"`. The full-screen capture and profile remain local-only
because surrounding navigation contains user-owned text.

This supersedes the previous 214.05px DOM project menu and its DOM focus
contract. The independent playground now matches the observed six fixed items,
three separators, 221×187 geometry, 13px/400 typography, current labels, and
current native SVG sources. The six observed sources are frozen separately in
[`current-project-menu-assets.json`](current-project-menu-assets.json); in
particular, Edit now uses the 20×21 pencil and Reveal uses the current 20×20
folder rather than their previous-build shapes.

Browser/CDP and Electron both gate item order, exact relative row/separator
geometry, computed styles, focus return, and the conditional seven-item unread
fixture. A local-only full 221×187 native-window crop compares every fixed
label and glyph without masks at 3.8449% changed pixels under a 5% limit. The
remaining pixels are dominated by AppKit/CoreText versus Chromium rasterization;
source hashes and geometry are independently exact. Because the sampled
current project had no unread activity, the conditional Mark all as read
behavior is verified against the known provider contract and deterministic
fixture, but its current-build native icon is not promoted.

## MCP success, invalid-URL recovery, and Sources summary

A disposable task used only the OpenAI Developer Docs integration. The first
turn reached Search → Fetch in 37 seconds and returned the official
`Model Context Protocol` page at
`https://learn.chatgpt.com/docs/extend/mcp`. A second task intentionally sent
one invalid Fetch URL, then recovered through Search → Fetch in 33 seconds and
returned `Use Codex with the Agents SDK` at
`https://learn.chatgpt.com/docs/mcp-server`. The failed Fetch retained the
67.3125px plaintext `Invalid URL` output while the integration group settled
to completed.

The recovered task also exposed the current Sources summary. At 1180×820 it
is a 300×189 surface inside a 316px dock at x=864, y=59. Pinning reserves that
316px ownership region and moves the 736px conversation content from x=222 to
x=64. The current lifecycle is pinned → floating → outside-pointer dismissed
→ repinned; an outside pointer does not dismiss the pinned state. At 720×680,
the same recovered failure card is 688×67.3125 at x=16, y=271 with zero
horizontal overflow.

The independent replay preserves the two exact durations, call order, final
titles, and public URLs. The final URL mention uses a locally embedded copy of
the publicly served `learn.chatgpt.com` favicon so acceptance is deterministic
and does not depend on a network fetch. Browser/CDP locks disclosure semantics,
computed styles, wide/compact geometry, and the complete Sources lifecycle.
Electron repeats both real-window flows. Unmasked product-region comparisons
pass at 2.1854% under 2.5% for the success group, 1.1649% under 1.3% for the
compact recovery card, and 2.5926% under 3% for the Sources panel. Product
screenshots, task data, and the isolated profile remain local-only.

## Markdown messages

A separate disposable no-tool task requested a deterministic Markdown reply
containing one H1, strong text, inline code, blockquote, unordered list,
two-column table, and TypeScript code block. At 1180×820 the live assistant
Markdown root is 736×358; after resizing to 720×680 it is 688×358 at x=16.
The narrow table fills the full content column at both sizes, while the code
block is 73px high with a 12.5px radius and
`rgba(255, 255, 255, 0.05)` background. Both states have zero horizontal
overflow. Its 24px Enable word wrap control toggles `aria-pressed` and the
wrapped code state beside the 26px Copy control.

The independently embedded 20×20 word-wrap glyph reuses the exact SVG path
observed in the installed 26.818 `app.asar`; the application archive itself and
other product-owned assets remain outside the repository.

In this sampled source-annotated response, the visible `public link` text is
plain text rather than an inline anchor; `example.com` is instead owned by the
task's Sources summary. This is recorded as the behavior of the sampled
response, not as a claim that every Markdown link variant is suppressed.

The independent replay keeps the previous-build Markdown fixture unchanged and
adds a build-scoped 26.818 fixture. Browser/CDP locks the semantic counts,
computed system typography, 736/688px geometry, full-width narrow table,
73px code block, word-wrap transition, four exact 26px response actions,
source-owned URL presentation, and bottom/overflow state.
Electron repeats the 1180×820 and 720×680 contracts. Two reviewed internal
baselines pass, and local-only unmasked Markdown-root comparisons differ by
`0.011681746417294147` wide and `0.011932408730674289` compact under separate
`0.013` hard limits. Full product screenshots, user-owned task text, and the
isolated profile remain untracked.

## Sidebar thread history, actions, and ordinary status

A separate isolated current-build probe created one disposable task, observed
its active → completed → unread lifecycle, and archived it after capture. The
sampled project-task and Recents rows are 30px high. Completed rows use no
ordinary status glyph; active rows reserve a 20×20 rail at an 8px right inset
and render the exact 16×16 two-path spinner with a two-second rotation; unread
rows render an 8×8 `rgb(131, 195, 255)` dot centered in the same rail. The
runtime rotation angle is intentionally not frozen.

Hovering either a project task or a Recents task replaces the ordinary status
rail with the same Pin/Archive toolbar: two transparent-border 19×20 buttons,
16×16 current SVGs, an 8px gap, and right insets of 35px and 8px. This
supersedes the previous-build Recents-only 24×24/4px composition. The sampled
profile exposed eight Recents rows with no leading SVG, but row count and text
are treated as private-data-dependent content rather than fixed product
geometry.

Browser/CDP and Electron lock the row, rail, action, accessibility, hover, and
replacement contracts. Local-only privacy-safe 72×30 action-tail comparisons
pass at 3.1481% for a project task and 3.1944% for a Recents task. The 28×30
ordinary-status tails pass at 2.3810% for active and 0% for unread. Full
screenshots, task text, and the isolated profile remain untracked.

## Current worktree entry and sidebar lifecycle

Two isolated current-build apps used disposable single-commit repositories to
exercise the real `New local worktree` path without touching the primary Codex
instance. The five-item `Work in` menu is now `Local`, `New local worktree`,
`Connect Codex web`, disabled `Cloud`, and `Usage remaining`. The selected
worktree context exposes `Select where to run the chat`, `Select a local
environment`, and `What branch should this chat start from?`; its visible
controls measure 155.64×28, 136.02×28, and 68.98×28. The current 264×91.125
Environment menu has exactly `Work without environment` and `Set up project`,
with no static empty row.

The success probe reached loading and restored rows. The failure probe used a
controlled `.git/worktrees` blocker, reached `Worktree init failed` and
`Worktree setup failed`, exposed 121.36×24 `Edit environment` and 72.78×24
`Retry` actions, then recovered after the blocker was moved aside. All sampled
sidebar rows are 30px high. Loading places the 14×14 branch marker 39px from
the right and reserves a 20×20 status rail at 8px; the exact two-path spinner
rotates over two seconds, while its transformed runtime bounding box is
intentionally not frozen. Failure keeps three simultaneous tracks: the branch
at 67px, a 16×16 `rgb(255, 103, 100)` error glyph at 38px, and an 8×8
`rgb(131, 195, 255)` unread dot at 14px. Restored keeps only the 14×14 branch
marker at 11px with `rgba(255, 255, 255, 0.498)` paint.

Browser/CDP and Electron repeat the entry menus, context controls, success,
failure, Retry, action replacement, and restored semantics. Privacy-safe
unmasked tail comparisons pass at 2.0833% for loading, 0.7540% for failed plus
unread, and 0.1786% for restored. Both disposable tasks were archived; the
repositories, isolated profiles, full screenshots, and exact capture records
remain untracked.

## Current sidebar collection and ordinary attention states

The primary current Renderer exposes one real empty project group under
`codex-app-server-client`. Its `No chats` row measures 306.91×29 with 14px/21px
type, 4px 32px padding, and 50% description opacity. The current ASAR ties the
global loading branch to one 18%-wide 12px heading shimmer plus four 12px row
shimmers (deterministic widths 70.532%, 67.090%, 51.773%, and 50.000%) and
limits nested project history to five rows before `Show more`.

`AppSidebarCollection` and `AppSidebarCollectionState` now model those
loading, empty, error, bounded, expanded, and focus-preserving states. The
ordinary waiting row follows the current `Needs input` status pill rather than
reusing the unread blue dot, while a separate ordinary error row retains the
16×16 failure glyph. Browser/CDP verifies the initial five rows, keyboard
expansion to twelve and `Show less`, Electron repeats the lifecycle with real
navigation overflow, and four reviewed internal frames cover status, empty,
loading, and long-list rendering. A privacy-safe 140×29 live-product empty
crop differs from the independent implementation by 1.8227%; the live crop
remains local-only.

## Evidence boundary

The current slice promotes Browser/Electron verification only for the shell,
Composer shell, basic completed messages, command execution, command-failure
recovery, command interruption/recovery, primary navigation, project-group
lifecycle, project action menu, Help menu, compact route restoration, and
Projects Index, core Markdown messages, MCP success/failure recovery, the
Sources summary/panel lifecycle, sidebar thread history, task actions,
ordinary active/completed/unread/waiting/error status, collection
empty/loading/long-list states, plus the New local worktree entry, real
create/failure/Retry recovery, and worktree-specific sidebar status. It records
narrow footer/window-control anchors as current runtime evidence without
promoting their older visual implementations. It does not relabel the
remaining 26.810 conversation lifecycles, approval, Review, Settings,
account-menu, or broader MCP variants as current. Those
remain previous-build regression evidence and their verification status is
downgraded until each surface is re-reached on 26.818.

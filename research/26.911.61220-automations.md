# Codex Desktop 26.911.61220 Scheduled tasks boundary

This note records a read-only Scheduled tasks probe of the installed Codex
Desktop `26.911.61220` build. Navigation reached the current native viewport;
no task was created, edited, paused, resumed, executed, delivered, or granted
permission.

## Fingerprint and geometry

- App version: `26.911.61220`
- Build: `9647`
- Chromium: `153.0.8010.36`
- `app.asar` SHA-256:
  `f3ea5ab82e0503d283c98ea21b0ecfde4d8fe4c740fb1d81dfc57b6f5c22ce1f`
- Native Renderer viewport: `2560×1318`
- Horizontal overflow: `0`

The route title is `Scheduled tasks` with the current description
`Ask ChatGPT to schedule tasks, set reminders, or monitor for updates`. The
search frame is `728×32px` at `x=1077.4375`, uses a 9999px radius and the
current system font, and the All/Active/Paused/Completed filters remain 28px
high. Three existing rows expose Pause actions, and the visible suggestion
labels are Daily brief, Weekly review, and Follow-up monitor. Exact path
hashes for those icons are retained in
[`current-automations-26.911.61220.json`](current-automations-26.911.61220.json).

## Boundary

This promotes current-build Scheduled tasks labels, counts, icons, and native
viewport geometry only. The existing 1180×820 and 720×680 replay, Electron,
and pixel contracts remain separately scoped until recaptured on 26.911.
Cloud persistence, delivery, permissions, and automation mutation effects
remain host-owned and unsampled.

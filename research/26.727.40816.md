# Codex Desktop 26.727.40816

Observed on 2026-07-31 from the locally installed
`/Applications/ChatGPT.app`.

## Package fingerprint

- App version: `26.727.40816`
- Build number: `6067`
- Chromium: `150.0.7871.182`
- `app.asar` path:
  `/Applications/ChatGPT.app/Contents/Resources/app.asar`
- `app.asar` size: `218362753` bytes
- `app.asar` SHA-256:
  `0e4f824024d0838dd7548751c02d3a7d21917c4fc3edf74c9e98d88ea9e3127d`
- Package modification time: `2026-07-31T09:38:23+0800`

## Evidence boundary

The probe used one exact second process, a unique temporary Chromium profile,
and a loopback-only CDP port. It selected the 1180×820
`app://-/index.html` main Renderer by URL, viewport area, and application-shell
landmarks. The temporary profile did not isolate Codex task or account data,
so the initial shell/PR run stayed on New chat, the read-only Terminal shell,
and public pull request `#80`; it did not submit a prompt, execute a command,
post a pull request comment or review, or activate merge. Two later
user-authorized disposable tasks submitted narrowly scoped prompts that used
only the OpenAI Developer Docs integration for successful and failure/recovery
evidence. A third user-authorized disposable task used synthetic text-only
prompts to observe the Composer queue and Stop lifecycle. A fourth exact
second process entered an unsubmitted synthetic draft and opened only the
permission and Add-resource surfaces. None of those tasks used shell, browser
navigation, or files, and the fourth probe did not submit a turn or select a
permission/resource action.

Evidence from `26.721.81911` and earlier builds remains historical. A surface
is current-build verified only when an independent Browser and Electron
contract matches an observation recorded in this note.

## Application shell and New chat

At 1180×820 the main Renderer retained a 46px titlebar and a 274.11px sidebar.
The primary stack exposed New chat, Pull requests, Sites, Scheduled, and
Plugins. Pinned and Recents were expanded, Projects was collapsed, project
rows exposed Pin/Archive actions, and profile/help remained in the footer.
Hide sidebar, Back, and Forward stayed in the titlebar-safe region.

New chat exposed a `Do anything` textbox at x=371.05/y=720, 712×44px, with
14/20px typography. Project, Local, and the current branch remained separate
context controls. The project trigger opened a 260×249.5px dialog containing
a 252×142.81px listbox with approximately 28.56px rows.

## Composer queue, Stop, and automatic continuation

The text-only Composer probe used exact PID `78399`, loopback port `9514`, and
profile `/private/tmp/codex-ui-kit-composer-cdp.Ma9hyd`. A real running turn
kept the 712×44 textbox and 28×28 Stop button, then accepted a queued
follow-up without interrupting the active response.

At 1180×820 the queued tray occupied x=372.05/y=667, 710×39px. Its bordered,
blurred top shell contained a 708×38px scroll region and one 684×28px row at
x=385.05/y=673. The row used 14/16px system typography and exposed the current
`Steer`, `Delete queued message`, and `Queued message actions` controls.

This build changed the interruption lifecycle observed on `26.721.81911`.
Clicking Stop produced `You stopped after 2s`, removed the tray, promoted the
queued prompt into the next user turn, and started it automatically. There was
no `Queue paused because you interrupted` header and no Resume action. The
independent default replay now follows automatic continuation; the former
paused/Resume state remains an explicit legacy compatibility frame only.

CDP covers 61 lifecycle frames and locks the queue geometry, labels, current
Stop summary, empty queue after interruption, and next-turn running state.
Real Electron repeats the same transition. Two optional 792×320 current-build
regional gates use a strict 0.05 pixel threshold and a 2% changed-pixel limit:

- queued Composer: `0.003480114`
- Stop plus automatic continuation: `0.005105745`

Only dynamic thread and text glyph bands are masked; the queue shell, visible
actions, interruption rule, Composer boundary, Stop/send control region, and
surrounding spacing remain compared. Cleanup stopped the exact process,
verified port `9514` and the profile process set were gone, and moved the
profile plus screenshots into
`/Users/JaminZhou/.Trash/codex-ui-kit-composer-cleanup.FE0SB8`. The original
Codex process remained running.

## Composer multiline, permissions, and Add resources

The fourth current-build probe used exact PID `89666`, loopback port `9515`,
and profile `/private/tmp/codex-ui-kit-composer-menus-cdp.8CjIBw`. It entered
a four-line synthetic draft without submitting it. The 736×134 Composer sat
at x=359.05/y=670 with a 712×80 textbox at x=371.05/y=684 and 14/20px
typography. A 20-line draft retained 400px scroll content, clamped the textbox
to 205px (`25dvh`), and grew the Composer to 736×259 at y=545.

The 101.06×28 Full access trigger opened a 480.36×222.44 menu at
x=401/y=544.06. Its four approximately 47.11px choices were Ask for approval,
Approve for me, Full access, and Custom (`config.toml`). `Add files and more`
opened a 736×320 inline resource picker at x=359/y=346. Its inner
726×310 scroller exposed files/folders, active-app attachment, project, Goal,
Plan mode, skill recording, and plugin resources before continuing into the
host's installed skills/apps/context entries. Those lower user/environment
specific labels are not copied into the independent fixture.

The public implementation adds `ComposerPermissionMenu` and
`ComposerResourcePicker`, keeps the host's option data outside the component,
and aligns the thread Composer's multiline geometry with the current build.
CDP now covers 61 frames. Real Electron verifies permission selection,
Escape/focus restoration, resource keyboard selection, and the 20-line clamp.
At the strict 0.05 pixel threshold, the ownership-masked regional gates pass
under their 0.5%, 0.5%, and 0.8% limits:

- four-line Composer: `0.000994318`
- permissions: `0.001280084`
- Add resources: `0.003352986`

The exact process and its app-server child were stopped, port `9515` was no
longer listening, and the unique profile was moved into the recoverable
`/Users/JaminZhou/.Trash/codex-ui-kit-composer-menus-cleanup.NV2plx`
directory with the external reference images. The original Codex process
remained running. Attachment lifecycle and active Goal/Plan state transitions
were not inferred from the resource picker. The active modes were sampled
separately below.

### Current Composer Goal and Plan modes

The fifth current-build Composer probe used exact PID `1770`, loopback port
`9516`, and a unique temporary Chromium profile. It stayed on an unsubmitted
new-chat draft. Selecting Goal from the Add-resource picker changed the
Composer textbox accessibility label and visible placeholder to
`Describe your goal, define measurable outcomes for best results`; selecting
Plan mode changed both to `Describe your task to generate a plan...`.

Both modes retained the 736×98 Composer at x=359.05/y=706 and the 712×44
input at x=371.05/y=720. Goal exposed a 65.80×28 clearable footer control at
x=512.11/y=768 with `Clear goal`; Plan used a 64.33×28 control at the same
origin with `Plan`. Clearing either mode restored the default `Do anything`
Composer and input focus without submitting the draft.

The independent `ComposerModeIndicator` uses original SVG geometry and the
current pill sizing. CDP now covers 63 frames, and real Electron drives
Goal → clear → Plan → clear through the public Add-resource selection path.
At the strict 0.05 pixel threshold, ownership-masked 906×820 comparisons pass
at `0.003763528` for Goal and `0.003486243` for Plan, below the 0.5% hard
limit.

`Files and folders` invokes a native macOS file panel rather than a Renderer
file input. The panel was not automated because Computer Use intentionally
blocks control of Codex itself; no attachment was added or submitted, so
`composer.attachments-plugins` remains `partial_legacy`.

The exact process and app-server child were stopped, port `9516` was verified
closed, and the unique profile plus external screenshots were moved to the
recoverable
`/Users/JaminZhou/.Trash/codex-ui-kit-composer-modes-cleanup.CJuP6u`
directory. The ignored synthetic attachment probe file was removed, and the
original Codex process remained running.

## Terminal boundary

The read-only Terminal probe opened the bottom panel without executing a
command. The outer separator occupied x=274.11/y=532.53, 905.89×16px. This
build changed the selected tab from the previous auto-numbered presentation
to the project name `codex-ui-kit`, with Close and Add controls. The
independent single-session model matches that project-named tab and its
close/add controls. A 906×820 main-region comparison passes without relaxing
the existing thresholds:

- Terminal panel: `0.017931924`
- Terminal content: `0.007324485`

The independent running/failed/exited process lifecycle remains historical
regression coverage because no current-product command was executed.

## Successful MCP/tool-call contract

A second user-authorized disposable task used only the OpenAI Developer Docs
integration. It did not execute a shell command, navigate a browser, read or
write files, or reuse an existing task. The completed current Renderer
exposed:

- `Worked for 31s`, expanded into one intermediate assistant explanation and
  one integration group;
- `Used OpenAI Developer Docs integration`;
- two successful ordered rows: Search OpenAI docs, then Fetch OpenAI doc;
- 14px/21px typography, the current system-font stack, 445 weight, 0.6
  secondary text color, and approximately 25px call rows;
- a linked Markdown answer and the standard response actions.

The independent replay now uses the same two-call order and 31-second
disclosure. CDP locks the disclosure, labels, computed typography, and
expanded state. The 906×820 current comparison passes without changing the
existing thresholds:

- masked full main region: `0.020305013`
- masked tool-call structure region: `0.000481220`
- answer region: `0.018667095`
- Composer region: `0.019202869`

The masks exclude only dynamic header/task/intermediate/group-label glyphs and
the non-owning scrollbar. The answer remains unmasked; bubble, separator,
disclosure, icon, row, answer, and Composer geometry remain compared. The
exact temporary process, loopback listener, profile, and screenshots were
cleaned up after validation.

## MCP failure and recovery contract

A separate user-authorized disposable task used only the OpenAI Developer
Docs integration. It first called Fetch OpenAI doc with
`not-a-valid-url`, received `Invalid URL`, then recovered through three Search
OpenAI docs calls and one successful Fetch OpenAI doc call. It did not execute
a shell command, navigate a browser, or read or write files.

The completed current Renderer exposed:

- `Worked for 51s`, with both assistant explanations owned by the same
  expanded activity timeline;
- a standalone failed Fetch row whose expanded neutral output is
  `plaintext / Invalid URL`;
- a subsequent `Used OpenAI Developer Docs integration` group containing
  three ordered Search rows and one Fetch row;
- a 736×67.3125px failed-output card with 12.5px radius,
  `rgba(255,255,255,0.05)` background, and
  `rgba(255,255,255,0.157)` border;
- 13/18.5714px language-label typography and 14/22.75px, 445-weight
  monospace output typography;
- a linked Markdown recovery answer and standard response actions.

The independent replay now preserves the real separation between the failed
call and the recovered integration group. CDP gates their order, status,
disclosure ownership, accessible failure label, current system-font stack,
445 group weight, 0.6 secondary color, and failed-card geometry. Electron
drives the same disclosure and raw-output lifecycle before continuing into
the existing mixed command/approval/file-review turn.

The masked 906×820 current comparison passes without changing the existing
limits:

- full main region: `0.016253432`
- recovered tool/answer region: `0.028989319`
- upper activity/failure region: `0.010849453`
- Composer region: `0.019065999`

Only dynamic header, timing, assistant, tool-label, and non-owning scrollbar
glyphs are masked. The failed-card boundary/background, disclosure structure,
final answer, response actions, and Composer remain compared. Cleanup
terminated only PID `67251` plus its two exact orphaned Crashpad helpers,
verified `127.0.0.1:9513` was no longer listening, and moved the unique
profile and screenshots into one recoverable Trash directory. The original
Codex process remained running.

## Pull request route and Code

The current product changed the PR detail contract:

- Summary and Code are the only top-level tabs; Timeline is integrated below
  the Summary comment composer.
- Header actions are Open in browser, Auto-merge, Merge, and Expand.
- Summary exposes Branch, Reviewers, 17 Comments, Successful Checks, Ready for
  review, Description, and the sticky comment composer.
- Code loaded successfully after the bounded wait and exposed real multi-file
  diffs in a 369.28px-wide scroller with an 81492px scroll height.
- Code toolbar controls are Review options, Collapse all diffs, split/unified
  display, and Show file tree.
- Review options contains only Enable word wrap, Enable rich preview, and
  Enable word diffs. It is a display menu, not a review-submission action.

The detail auto-hid after responsive resizing and was reopened explicitly from
the selected row before each measurement. At 1180×820 the panel began at
x=810.72 with width 369.28px and a separator beginning at x=801.72. At 960px
it began at x=638.03 with width 321.97px; at 820px it began at x=501 with
width 319px; and at 720px, after the sidebar hid, it began at x=390.69 with
width 329.31px.

The independent replay now follows the two-tab/integrated-Timeline structure,
keeps review submission host-owned behind an explicit synthetic action, and
passes CDP, real Electron, and 61 reviewed regional pixel frames. The optional
906×820 current-build gate passed without relaxing its thresholds:

- full main region: `0.057544554`
- PR index region: `0.046825123`
- PR detail region: `0.064395978`

No current-product comment, review, rerun, auto-merge, or merge transition was
triggered. Those states remain deterministic host acceptance rather than
runtime evidence.

## Long-thread navigation and windowing

A read-only second-process probe opened an existing long conversation and did
not submit, edit, delete, or otherwise mutate its content. At 1180×820 the
main thread viewport measured x=274.109/y=47, 905.891×773px, used
`flex-direction: column-reverse`, kept latest at `scrollTop = 0`, and reached
approximately 44,740px of scroll height after loading.

The current thread exposed 82 accessible `Jump to user message N` buttons.
The compact navigation list measured x=290.109/y=146.5, 36×574px with an
820px scroll height. Selecting message 40 produced `scrollTop = -28484`, seven
mounted turn nodes and seven mounted user bubbles. Its selected button
measured 36×10px at x=290/y=290.5 and the active marker 26×2px at
x=290/y=294.5. The visible 32×32px `Scroll to bottom` control measured
x=711.047/y=615. Activating it restored message 82, `scrollTop = 0`, and the
hidden control state.

The independent fixture retains only synthetic labels and leaves the host
responsible for the mounted window. Browser/CDP and real Electron repeat the
selection and restoration flow. An external 906×820 main-region comparison
masks all conversation content and gates only the compact rail and floating
control; it passes at `0.005937382` under a 1% hard limit.

Cleanup stopped exact main PID `6481` and child `6523`, verified loopback port
`9517` was closed, restored the observed task to latest before shutdown, and
moved the unique profile and external evidence into
`/Users/JaminZhou/.Trash/codex-ui-kit-long-thread-cleanup.XSqBZw`.

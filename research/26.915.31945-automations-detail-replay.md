# Scheduled task detail replay boundary on 26.915

This note records the private playground replay added for the current
`26.915.31945` Scheduled tasks slice. The installed package observation remains
read-only and is limited to the index geometry in
[`current-automations-26.915.31945.json`](current-automations-26.915.31945.json).

## Replay contract

- `scheduled-current-26-915-detail` and `scheduled-current-26-915-detail-compact`
  cover the ready detail state at 1180×820 and 720×820.
- `scheduled-current-26-915-detail-error` and its compact counterpart cover the
  permission-shaped error and Retry transition.
- The four scenes exercise Run now, Retry, Pause → Resume, Edit, and Save.
- Browser/CDP checks assert the four detail facts, `Workspace brief` identity,
  zero horizontal overflow, and 768px/680px detail widths.
- Electron repeats the same actions in a real `BrowserWindow`.
- Four reviewed own-fixture baselines pass with zero repeat-capture drift.

## Evidence boundary

This is a deterministic, host-controlled replay extension. It does not claim
that the installed product exposes the detail route at the current build, nor
does it claim cloud persistence, permissions, delivery, or automation
execution. Those effects remain host-owned and require a separate authorized
live capture.

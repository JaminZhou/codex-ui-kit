# Codex Desktop 26.911.61220 Documents selection boundary

This note records a read-only selection probe of the installed Codex Desktop
`26.911.61220` build. It describes the Composer resource lifecycle only; no
prompt was submitted and no document was created or decoded.

## Fingerprint and entry

- App version: `26.911.61220`
- Build: `9647`
- Chromium: `153.0.8010.36`
- `app.asar` SHA-256:
  `f3ea5ab82e0503d283c98ea21b0ecfde4d8fe4c740fb1d81dfc57b6f5c22ce1f`
- Capture date: 2026-09-18
- Selected Renderer: `app://-/index.html`, viewport `2560×1318`

The current `Add files and more` menu exposes a `Documents` row at
`726×28.5625px` inside the `736×320px` menu. The row uses the current public
label and description, `Documents` / `Create and edit documents`.

## Observed transition

Activating the row leaves the target at `app://-/index.html`; it does not open
a new page or webview. The Composer receives a non-editable inline mention with
these observed host-owned attributes:

- plugin name: `documents`
- display label: `Documents`
- prompt link: `plugin://documents@openai-primary-runtime`
- brand color: `#2563EB`
- mention geometry: `98.375×20px`
- Composer textbox geometry: `712×44px`
- the Composer also exposes a `Templates` heading (`734×36px`)

The Renderer exposed zero `input[type=file]` elements after the selection, no
attachment card, and no document-preview route. No task, file, or template was
submitted or persisted. The plugin icon source remains an installed-app asset
and is not copied into the public package.

## Boundary

This promotes a current-build Composer resource-selection observation and
keeps `workspace.document-previews` at `not_sampled`: selecting the Documents
resource is not evidence of document decoding, preview rendering, generation,
or template execution. The public preview shell and its controlled replay stay
separately scoped to host-owned effects.

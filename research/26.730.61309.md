# Codex Desktop 26.730.61309

Observed on 2026-08-05 from the locally installed
`/Applications/ChatGPT.app`.

## Package fingerprint

- App version: `26.730.61309`
- Build number: `6223`
- Chromium: `151.0.7922.71`
- `app.asar` path:
  `/Applications/ChatGPT.app/Contents/Resources/app.asar`
- `app.asar` size: `219943419` bytes
- `app.asar` SHA-256:
  `9de942a9a058fca20b78d171032e0fe65ccb1063868f175ff7eb4e159efc2c38`
- Package modification time: `2026-08-05T09:29:52+0800`

## Evidence boundary

The probe used exact second-process PID `99165`, loopback port `9520`, and a
unique temporary Chromium profile. It selected the largest
`app://-/index.html` Renderer, fixed its viewport to 1180×820, and created one
disposable task. The task requested only `seq 1 400`; it prohibited all other
commands and file reads/writes. Ask mode ran that low-risk command without an
approval card, so this is not approval allow-once evidence.

Evidence from `26.727.40816` and earlier builds remains historical. A surface
is current-build verified only when an independent Browser and Electron
contract matches an observation recorded in this note.

## Left sidebar

A read-only second-process sweep selected the largest `app://-/index.html`
Renderer by URL, area, and application-shell landmarks. At 1180×820 and
820×680 the sidebar measured 274px wide with a 46px titlebar inset, a 70px
header, 30px route rows, a fixed 46px footer, and 54px bottom scroll
clearance. The header exposed Codex mode, Search, View activity, New chat, and
Quick chat controls. The scroll region contained Pinned, Projects, and
Recents plus five project groups, nested tasks, five project action controls,
five project New chat controls, and two visible status markers. Project names
and task text were not retained in tracked evidence.

The exact responsive sweep covered 1180, 960, 900, 820, 800, 780, 760, 740,
every width from 739 through 721, and 720px. The 274px sidebar remained in
flow through 721px and auto-hid at 720px. An explicit Show action at 720px
pinned a non-modal 274/446 split and navigating to Pull requests did not
dismiss it. After an explicit Hide action, hovering the first inline pixel for
1.2 seconds did not reopen the historical edge preview. The current native
separator exposes `role=separator`, but it did not expose range value
attributes or respond to Home/End in the sampled Renderer; the independent
UIKit keeps keyboard resize semantics as a deliberate accessibility
improvement.

`AppSidebarProjectGroup` models the observed expandable project/task
hierarchy, and the de-identified full-app scene retains five groups without
shipping private names. Browser/CDP and internal pixel acceptance cover 75
lifecycle frames. Electron drives the 720px auto-collapse, explicit pin,
route navigation, and explicit hide sequence. Against the external 1180×820
current-build reference, the top region differs by
`0.036233576642335764`, the text-masked selected row by
`0.0012919896640826874`, and the text-masked footer by `0`, under hard limits
of 4.5%, 1%, and 0.5% respectively.

Cleanup terminated only PID `40154`, its exact process tree, and the
reparented modifier child PID `40202`; port `9532` was verified closed. The
four ignored probe scripts were removed, and the two exact temporary profiles
plus sidebar screenshots were moved into the recoverable Trash directory
`codex-ui-kit-sidebar-evidence.ptbmTs`. The original Codex process remained
running.

## Long command output

The completed task exposed `Worked for 19s`, one commentary message,
`Ran seq 1 400`, and the exact final answer. Expanding both disclosures
produced the same sampled contract as `26.727.40816`:

- 736×227.06px command card at x=274.05/y=101.61;
- `Shell`, `$ seq 1 400`, two copy controls, and `Success`;
- 734×144px reverse-column output viewport;
- 13/19.5px weight-500 monospace output;
- 401 split lines including the trailing newline;
- 7816px scroll height and `scrollTop = 0`, with lines 394–400 at the latest
  edge;
- 12.5px radius, `rgba(255, 255, 255, 0.05)` background, and
  `rgba(255, 255, 255, 0.157)` border.

The schema-validated independent trace still contains exactly one command
request/output pair. Browser/CDP now covers 75 lifecycle frames and verifies the
disclosure, styles, overflow, keyboard operation, and collapse/reopen tail
restoration. Electron drives the same interaction. An ownership-masked full
1180×820 comparison against this build passes at `0.001655643` under the 1.5%
hard limit.

This verifies only the sampled successful long-output path. The rest of the
`26.727.40816` inventory is previous-build evidence until separately reached
again on `26.730.61309`.

Cleanup stopped exact main PID `99165`, its app-server and modifier children,
and the two profile-specific Crashpad helpers; port `9520` was verified
closed. The ignored probe script was removed, and Finder moved the unique
profile plus screenshots and computed-state JSON into the recoverable Trash
item `codex-ui-kit-current-cdp.mrkFgT`. The original Codex process remained
running.

## Command failure and same-thread recovery

A second disposable task ran only this read-only probe:

```sh
sh -c 'i=1; while [ "$i" -le 80 ]; do printf "stdout-%03d\n" "$i"; printf "stderr-%03d\n" "$i" >&2; i=$((i+1)); done; exit 7'
```

The completed turn exposed `Worked for 12s`, a collapsed failed-command row,
and an expanded 736×246.58px `Shell` card. Its 734×144px reverse-column
viewport has a 3136px scroll height, 13/19.5px weight-500 monospace text,
161 split lines including the trailing newline, two copy controls, and an
`Exit code 7` footer. The exact observed merged output begins with
`stderr-001`, retains all 80 stdout and 80 stderr records, and ends with
`stdout-080` followed by `stderr-080`. A second no-tool turn then returned
`Recovery follow-up accepted.` in the same thread.

The schema-validated independent replay adds running, collapsed-failed, and
expanded-recovered frames. Browser/CDP covers 75 lifecycle frames and locks
semantics, geometry, computed styles, keyboard disclosure, copy, and
collapse/reopen tail restoration. Electron repeats the lifecycle in a real
1180×820 `BrowserWindow`. An ownership-masked full-window comparison passes
at `0.008898305084745763` under a 1% hard limit.

The probe used exact main PID `71571`, loopback port `9541`, and unique profile
`/private/tmp/codex-ui-kit-command-failure-cdp.Pwo1hz`. Cleanup stopped only
that process and its two profile-specific Crashpad helpers; all three PIDs are
gone and port `9541` is closed. The ignored probe was removed, and the unique
profile plus screenshots were moved into the recoverable Trash item
`codex-ui-kit-command-failure-cdp.Pwo1hz`. The original Codex process remained
running.

## Command interruption and same-thread recovery

A third disposable task used only this read-only command:

```sh
seq 1 120 | while read i; do printf 'interrupt-probe-%03d\n' "$i"; sleep 1; done
```

The current Renderer exposed `Working for 1m 35s`, an expanded
`Running …` row, `Running command for 1m 28s`, and the 28×28 Stop action.
Activating Stop changed the turn to `You stopped after 1m 35s` and emitted a
transient `Background terminal stopped with …` row. After the background
terminal settled, that row became `Ran …`. A no-tool follow-up in the same
thread returned exactly `INTERRUPTION RECOVERY ACCEPTED`, retained the
interruption summary, and restored focus to the empty Composer.

The settled summary starts at x=359.05/y=46.5, spans 736×30px, and uses the
current 14/21px weight-445 system typography. The settled command text starts
at x=381.05/y=94.5 in a 21px row. The Composer input remains 712×44px inside
the 736×98px dock, and Stop/Send retain the sampled 28×28 geometry.

The independent public replay separates running, immediate-stop, settled, and
recovered frames. Browser/CDP covers all 75 frames and also clicks Stop, waits
for background settlement, sends the exact recovery prompt, and verifies
focus restoration. Electron repeats that lifecycle in a real 1180×820
`BrowserWindow`. The ownership-masked immediate-stop full-window comparison
passes at `0.0043520049607275735` under a 0.5% hard limit.

The working second instance was opened with `open -na` using exact main PID
`94866`, loopback port `9542`, and unique profile
`/private/tmp/codex-ui-kit-command-stop-cdp.ZbTpcV/profile`; direct executable
launch first exited through Electron's single-instance behavior. Cleanup
terminated only the second-instance process tree, verified port `9542` closed,
and left the original Codex process running. The ignored probe was removed,
and the exact profile, screenshots, and computed-state JSON were moved to the
recoverable Trash item `codex-ui-kit-command-stop-cdp.ZbTpcV`.

## Manual context compaction and same-thread recovery

A fourth disposable task first sent the no-tool prompt `Do not use tools.
Reply with exactly: COMPACTION BASELINE READY`, then entered `/compact` into
the empty Composer. The current slash-command row was 726×28.56px and exposed
`Compact` plus `Compact this chat's context (9% full)`. Selecting it produced
`Working`, a full-width rule, `Compacting context`, and the current 28×28 Stop
action. Across 131 samples the transition settled to `Context compacted` in
8.136 seconds. A second no-tool message in the same thread returned exactly
`COMPACTION RECOVERY ACCEPTED` and restored the empty Composer.

The running label uses 14/21px weight-445 system typography. Its measured text
starts at x=381.05/y=223 in a 21px row; the Composer input remains 712×44px at
x=371.05/y=720, with Stop at x=1059.05/y=768. The completed label retains the
same typography and normal Send state.

The schema-validated public replay adds command-menu, running, completed, and
recovered frames. Browser/CDP drives the menu and transition, checks manual
mode, separator, Stop/Send ownership, exact recovery text, and focus
restoration; Electron repeats the lifecycle in a real 1180×820
`BrowserWindow`. The resulting matrix covers 75 reviewed frames. An
ownership-masked full-window comparison retains the compaction event and
Composer boundary and measures `0.0016256717651922281`, below the 0.5% hard
limit.

The isolated instance used exact main PID `13096`, loopback port `9543`, and
profile `/private/tmp/codex-ui-kit-context-compaction-cdp.aJjET6/profile`.
Cleanup terminated only that process tree, verified port `9543` closed, and
left the original Codex process running. The ignored probe was removed, and
the exact profile, screenshots, and computed-state JSON were moved to the
recoverable Trash item `codex-ui-kit-context-compaction-cdp.aJjET6`.

## Thread summary overlay

A fifth disposable task sent one no-tool synthetic prompt and opened the
current `Toggle summary` header action after the exact reply settled. At the
fixed 1180×820 Renderer viewport, the control is 28×28px at x=1076/y=9 and the
overlay is anchored 8px below it at x=804/y=45. The overlay measures 300×199px,
uses a 25px radius and `rgb(45, 45, 45)` background, and retains 14/21px
weight-445 system typography.

The sampled `Environment` section exposes a 28px setup action and five
272×29px rows: Changes with +0/-0, Local execution, the current branch,
disabled Commit/Push, and Create pull request. This establishes the summary as
a thread-owned environment/Git workflow overlay; it is not the text emitted by
context compaction.

The independent public replay keeps the no-tool message and summary state
deterministic. Browser/CDP and Electron verify the trigger, Escape focus
return, section collapse/expand, outside dismissal, current geometry, and
computed styles. A 300×199 ownership-scoped comparison against the raw current
application screenshot measures `0.028609715242881074`, below the 3% hard
limit. The reviewed matrix now covers 76 frames.

The isolated instance used exact main PID `29147`, loopback port `9544`, and
profile `/private/tmp/codex-ui-kit-context-summary-cdp.gZ7rha`. Cleanup
terminated only that main process, its descendants, two profile-specific
Crashpad helpers, and the reparented modifier monitor; all recorded PIDs are
gone, port `9544` is closed, and the original Codex process remains running.
The ignored probe was removed, and the exact profile plus external screenshots
were moved into the recoverable Trash item
`codex-ui-kit-context-summary-cdp.gZ7rha-20260805`.

## Command approval: Allow once completion

A sixth disposable task requested exactly `open -a Calculator` and prohibited
all other tools or commands. Calculator was absent before the decision. The
current pending approval replaced the Composer with a 736×162 card at
x=359.05/y=642, using a 25px radius and `rgba(45, 45, 45, 0.96)` background.
It exposed `Terminal`, the command preview, 106.58×28px Deny, 107.63×28px
Allow once, and the 23×28px split-options trigger. The application-generated
question was localized, while the actionable labels remained Deny and Allow
once.

Activating Allow once started Calculator only after the decision. The command
completed successfully, the final response was exactly `ALLOW ONCE COMPLETE.`,
the approval card disappeared, and the empty Composer returned focused with
its policy still `Ask for approval`. Calculator was then closed and verified
absent.

The independent schema-validated accept trace raises the fixture set to 18
traces and 225 events. Two reviewed frames raise the Browser/CDP and pixel
matrix to 78; CDP additionally drives the transition and verifies one
completed command, exact response, empty focused Composer, and unchanged
permission. Electron repeats the lifecycle in a real 1180×820
`BrowserWindow`. Ownership-masked 906×820 comparisons against this build pass
at `0.012554514617993862` for pending and `0.002993592849835783` for completed,
below separate 1.5% hard limits.

This promotes only the sampled one-time command approval. Persistent/session
approval, timeout, repeated denial, and non-command approval kinds remain
separate gates; denial remains previous-build evidence.

The isolated instance used exact main PID `37635`, loopback port `9545`, and
profile `/private/tmp/codex-ui-kit-approval-allow-once-cdp.wc3qhK`. Cleanup
terminated that main process, its descendants, and two profile-specific
Crashpad helpers; all recorded PIDs are gone and port `9545` is closed. The
ignored probe was removed, Calculator is absent, and the original Codex PID
`96228` remains running. The exact profile and external screenshots were moved
into the recoverable Trash item
`codex-ui-kit-approval-allow-once-cdp.wc3qhK-20260805`.

## Command approval: Allow similar commands

A seventh disposable task requested exactly `open -a Calculator` twice. The
first request reached the same current 736×162 approval card after 1m 38s.
Opening its split menu exposed exactly `Allow once` and the current
`Allow similar commands` label with a rule-information icon. The measured
public menu is 195.25×68px at x=884/y=691, matching the approximately 194×68px
current-build surface at the same right edge.

Selecting `Allow similar commands` applied the proposed execpolicy amendment
for `open -a Calculator`, completed the first turn after 1m 41s, and restored
the empty focused Composer. An identical second command then completed after
7s without another approval request. The exact final responses were
`SESSION APPROVAL FIRST COMPLETE.` and `SESSION APPROVAL SECOND COMPLETE.`.
The Composer's global mode remained `Ask for approval`, proving that the
matching rule is distinct from the global permission selector. Calculator was
closed after each observation and verified absent.

The independent 16-event trace records the object-valued
`acceptWithExecpolicyAmendment` decision and no approval request in the second
turn. It raises the suite to 19 fixtures and 241 events. Two reviewed frames
raise Browser/CDP and pixels to 80; CDP drives the full menu → first completion
→ repeated completion lifecycle, and Electron repeats it in a real 1180×820
`BrowserWindow`. Ownership-masked 906×820 comparisons pass at
`0.012100899154686911` for the open menu and `0.013686534216335542` for the
repeated completed state, below independent 1.5% hard limits.

The isolated instance used exact main PID `46243`, loopback port `9546`, and
profile `/private/tmp/codex-ui-kit-approval-session-cdp.uLaPIG`. Cleanup
terminated only that process, its descendants, two profile-specific Crashpad
helpers, and the profile-owned Computer Use service; all recorded PIDs are
gone, port `9546` is closed, Calculator is absent, and the original Codex PID
`96228` remains running. The ignored probe was removed. The exact profile,
external screenshots, computed result, and debug captures were moved into the
recoverable Trash item
`codex-ui-kit-approval-session-cdp.uLaPIG-20260805`.

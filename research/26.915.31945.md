# Codex Desktop 26.915.31945 candidate shell baseline

This note records a fresh isolated shell/sidebar baseline for the installed
Codex Desktop `26.915.31945` build. It is newer than the `26.911.61220`
candidate, but it does not replace the promoted cross-surface baseline in
[`26.903.71938.md`](26.903.71938.md).

## Fingerprint

- App version: `26.915.31945`
- Build: `9922`
- Chromium: `153.0.8010.48`
- `app.asar` bytes: `358872526`
- `app.asar` SHA-256:
  `1f7939c1c781887c167043c4d1d307af3400d324685cfc315dfe2f80e634f483`
- Capture date: 2026-09-19

## Capture method

The capture used one temporary isolated loopback CDP process on port `9933`
and a unique profile under `/private/tmp/codex-ui-kit-*`. Existing Codex
windows were not navigated or resized. The process was selected by URL,
viewport area, shell landmarks, and visible-control density; the avatar
overlay target and non-app target were excluded. Navigation was limited to the
allowlisted New chat, Projects, Pull requests, sidebar, Help, and fixed
viewport interactions.

The script hashed `app.asar` before and after Renderer sampling and proved the
same device, inode, size, timestamp, and SHA-256. No prompt, project,
repository, account, or remote resource was submitted or persisted.

## Shell and Composer anchors

The current New chat shell records a `321.88px` expanded navigation rail and a
`858.13px` main track at `1180×820`. The Composer editor is `712×44px` at
`14px/20px`, weight `430`, using the installed system font stack. The Add
files-and-more, Dictate, and voice-chat controls remain 28px controls and the
main shell has zero horizontal overflow. The `820×680`, `721×680`, and
`720×680` states repeat the responsive lifecycle and preserve the zero-overflow
contract.

The same capture opens the Add files-and-more menu read-only. It is a
`736×320px` menu with a `726×310px` scroll owner, `951px` of content, and
`28.5625px` rows. The sanitized public catalog contains 19 items; the current
`Documents` row is `726×28.5625px` with the description `Create and edit
documents`. The menu and editor both retain zero horizontal overflow. This is
resource-menu geometry only; it does not claim file selection, document
decoding, template execution, or tool authorization.

## State matrix

The recorded matrix is in
[`current-baseline-26.915.31945.json`](current-baseline-26.915.31945.json):

- Viewports: `1180×820`, `820×680`, `721×680`, and `720×680`
- Renderer states: wide New chat, medium New chat, threshold New chat,
  compact visible-before-collapse, compact collapsed, compact pinned, compact
  Pull requests, and compact restored
- Project Index: 15 content-dependent rows in wide and compact views
- Sidebar: `321.88px` wide baseline/compact width and `305.88×30px` project
  rows
- Help menu: eight items in a `320×272.06px` menu with focus inside while
  open and focus returned after close
- Responsive lifecycle: collapse, pin, route change, keyboard restoration,
  and zero horizontal overflow

## Startup hydration boundary

A second disposable `26.915.31945` process was polled for the first 15 seconds
on 2026-09-21. The sanitized record is
[`current-startup-26.915.31945.json`](current-startup-26.915.31945.json).
The Renderer first exposed a blank document, then a shell with `aria-busy`
activity and 10 → 26 → 29 sidebar rows, and finally settled with 29 rows and
no busy marker. No prompt was submitted, no private labels were recorded, and
the original signed-in process was not touched.

This confirms the current-build startup/hydration boundary, but the ordinary
collection skeleton and ordinary task error row were not observable in this
startup window. The result therefore remains negative reachability evidence;
the existing 26.915 collection replay continues to be structural evidence only
and is not promoted to installed-product pixels.

## Promotion boundary

This is candidate evidence for the installed `26.915.31945` shell/sidebar
family. The promoted global baseline remains `26.903.71938`; affected surface
families need their own current-build Browser/CDP, Electron, and
regional-pixel refresh before promotion. This record is structural runtime
evidence only and does not claim installed-product pixel parity for Composer
submission, attachments, streaming, tools, approvals, or other host-owned
lifecycles.

## Controlled replay refresh

The playground now carries the candidate geometry into six deterministic
wide/720 routes: shell ready, DOCX-style document preview, and generated-image
preview. The CDP contract and native Electron contract verify the `321.875px`
expanded rail, `46px` titlebar, hidden compact rail, format identity, zero
overflow, and ready/error ownership. Six reviewed baselines reproduce with
zero own-fixture drift. This replay validates the renderer contract only; it
does not turn the isolated product capture into installed-product decoding,
generation, or product-pixel evidence.

# Codex Desktop 26.917.62051 candidate baseline

Status: candidate-only structural observation. The promoted global inventory
baseline remains `26.903.71938`; this capture does not promote installed-product
pixels or claim that every surface family has been refreshed.

## Package identity

- Sampled: 2026-09-24
- Installed app: `/Applications/ChatGPT.app`
- App version/build: `26.917.62051` / `10789`
- Chromium: `153.0.8010.53`
- `app.asar`: 370,175,042 bytes, SHA-256
  `c41157d36d701d3228c82e56852f49da2381941a7e5ccfac2d58b7e10e31aefd`
- Sanitized record: [`current-baseline-26.917.62051.json`](current-baseline-26.917.62051.json)

## Capture boundary

The read-only sample used one isolated Renderer with a unique temporary profile
and loopback-only CDP. The selected page was `app://-/index.html`, ranked using
public landmarks and visible-control density. The app bundle identity was
checked before and after capture. Navigation was limited to public shell,
Projects, Help, and Composer resource-menu surfaces; no prompt was submitted,
and project/account-specific labels were not retained. The record is structural
DOM/computed-layout evidence, not a product screenshot or pixel comparison.

## Observed structure

- New chat shell: `1180×820`, `820×680`, `721×680`, and `720×680` viewports;
  the expanded rail measures about `321.88px`, and horizontal overflow is zero.
  At 720px the rail remains visible until explicitly collapsed; the collapsed
  state restores the editor to the full content width.
- Composer anchor: `712×44px` at `1180×820`, with 14px font size, 20px line
  height, and weight 430. Compact and threshold states are recorded separately.
- Projects: the sampled `/projects` route has 15 content-dependent rows at
  `70px` each. The content column measures `736px` wide and `559px` at the
  600px compact sample. Sort, empty-search focus, project-group expansion,
  sidebar collapse/restore, and owned scrolling were observed. Counts are sample
  data, not stable product constants.
- Sidebar and Help: 15 sampled project groups (9 expanded); Help opens a
  `320×300.63px` menu with 9 items, moves focus into the menu, and returns focus
  on close. Menu contents are content-dependent.
- Composer resources: the Add menu measures `736×320px`, its scroll owner
  `726×310px`, content height `951px`, and rows `28.5625px`; 19 public titles
  were retained. This does not establish selection, upload, authorization, or
  execution behavior.
- The native project context-menu items were unavailable to the Renderer
  capture, so they remain unobserved.

## Still required before promotion

The installed Settings → Connections route was not sampled in this capture.
The 26.915 candidate's controlled Connections replay has local Browser/CDP,
Electron, and own-fixture pixel coverage, but that does not refresh 26.917
product evidence. Re-observe each affected surface family on this exact build
and keep package/runtime evidence, controlled replay, and installed-product
pixels as separate claims. Do not change the promoted baseline until the
project's current-build promotion gates are met.

# Codex Desktop 26.915.31945 Scheduled tasks boundary

This note records a read-only Scheduled tasks probe of the installed Codex
Desktop `26.915.31945` build. Navigation reached the native Scheduled tasks
route; no task was created, edited, paused, resumed, executed, delivered, or
granted permission.

## Fingerprint and geometry

- App version: `26.915.31945`
- Build: `9922`
- Chromium: `153.0.8010.48`
- `app.asar` SHA-256:
  `1f7939c1c781887c167043c4d1d307af3400d324685cfc315dfe2f80e634f483`
- Native Renderer viewport: `2560×1318`
- Horizontal overflow: `0`

The route title remains `Scheduled tasks` with the description
`Ask ChatGPT to schedule tasks, set reminders, or monitor for updates`. The
search frame is `728×32px` at `x=1077.4375`, with a 9999px radius and the
current system font. All/Active/Paused/Completed remain 28px high, and three
existing rows expose `Pause` actions. The current build no longer renders the
three suggestion templates that were visible in the 26.911 capture; the
sanitized record explicitly records an empty suggestion list rather than
borrowing that older evidence. The refreshed task-action SVG path set is
retained as a hash in [`current-automations-26.915.31945.json`](current-automations-26.915.31945.json).

## Boundary

This promotes current-build Scheduled tasks labels, counts, icon geometry, and
native viewport structure only. The existing 1180×820 and 720×680
Browser/CDP, Electron, and pixel replay contracts remain separately scoped
until re-recorded on `26.915.31945`. Cloud persistence, delivery, permissions,
and automation mutation effects remain host-owned and unsampled.

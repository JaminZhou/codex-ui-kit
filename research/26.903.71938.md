# Codex Desktop 26.903.71938 baseline discovery

This note records the first reproducible shell capture against the currently
installed Codex Desktop build on 2026-09-12. It supersedes the pending
package-only note for 26.901.51231 as the current baseline. Earlier
26.825.51511 evidence remains historical regression evidence until the affected
surface is re-observed on this build.

## Fingerprint

- App version: `26.903.71938`
- Build number: `8576`
- Chromium: `152.0.7977.83`
- `app.asar` size: `306621494` bytes
- `app.asar` SHA-256:
  `58fef82480b9064e209b5b2fd934992e8d71515aea8084482369cfeaff1b8ee0`
- Installed resource:
  `/Applications/ChatGPT.app/Contents/Resources/app.asar`

The capture hashed the archive before and after Renderer sampling, retained
device/inode identity, and proved the isolated Renderer started after the
bundle metadata settled. A stale isolated process from the earlier
26.901.51231 installation was rejected and terminated before this capture;
the successful record therefore cannot attribute old Renderer evidence to the
new archive.

## Capture boundary

One user-authorized second process used loopback-only CDP port `9901` and the
unique profile `/private/tmp/codex-ui-kit-cdp.OaVvvn`. Existing Codex windows
were not navigated or resized. Target selection used URL, viewport area,
shell landmarks, and visible-control density. The committed record contains
only sanitized fixed labels, geometry, computed styles, focus state, route
markers, and content-dependent counts; private account, project, repository,
task text and screenshots remain local-only.

The reproducible capture is:

```sh
CODEX_CURRENT_BASELINE_CDP_PORT=9901 \
CODEX_CURRENT_BASELINE_PROFILE=/private/tmp/codex-ui-kit-cdp.OaVvvn \
CODEX_CURRENT_BASELINE_ALLOW_NAVIGATION=1 \
CODEX_CURRENT_BASELINE_OUTPUT=/private/tmp/codex-ui-kit-cdp.OaVvvn/current-baseline.json \
pnpm capture:current-baseline
```

The capture covers New chat at 1180×820, 820×680, 721×680, and 720×680,
explicit Hide/Show, Pull requests → New chat restoration, project expansion
with pointer/Enter/Space focus continuity, and the Projects route at 1180×820
and collapsed 600×600. Every sampled state has zero horizontal overflow.

## Current shell and Projects observations

- The main Renderer was selected from two `app://-/index.html` candidates by
  structure rather than target order. The selected target was 1180×820 with
  one navigation landmark, two main landmarks, two sidebar triggers, and one
  textbox.
- The persisted sidebar is `321.88px` in the wide capture and remains visible
  at 720px until explicitly hidden. Hide removes the visible navigation and
  Show restores it; the Pull requests round trip returns to New chat.
- Project-group rows are 30px tall. The sampled capture contained nine
  content-dependent groups and nine expanded groups; row counts are not treated
  as a fixed product denominator.
- Help remains one 320×272.06 DOM menu with eight items, menu focus, Escape
  dismissal, and trigger-focus return.
- Projects is 736px wide at 1180×820 with 512/64/128 columns and 70px rows;
  the current title uses `28px`/`33.6px` type at weight `500`. At collapsed
  600×600 it is 559px wide with 415/128 columns and the same 70px rows. Sort,
  empty search, expansion, focus continuity, and one owned scroller were
  re-observed.

## Native project-menu boundary

The project action trigger is current-build verified as a 24×24 button with
`aria-haspopup="menu"`, a frozen `electronBridge.showContextMenu`, and the
`electron-native-context-menu` render mode. The new Renderer does not expose
the old React Fiber `getNativeItems` provider, and CDP cannot inspect the
native AppKit window. The record therefore stores
`observationStatus: "native-items-unavailable"` and `items: null` instead of
inventing menu contents. The older exact native item list remains a historical
fixture; native menu labels, geometry, and submenu artwork require a permitted
native observation before promoting that surface on 26.903.

## Scheduled tasks current-build observation

An isolated read-only Renderer reached the Scheduled tasks route on this build
without submitting a task mutation. The sanitized record is
`research/current-automations-26.903.71938.json`. At the native 2555×1318
viewport, the route exposes the fixed title/description, a 728×32 search
field, four filters, three paused task rows, and the three fixed suggestion
labels. The title is `28px`/`33.6px` at weight `500`; the older 26.825 record
reported weight `400` and two observed rows. Current task and suggestion icon
path hashes are recorded without committing raw renderer path data.

This promotes current native runtime evidence only. Existing 1180×820,
720×680 Browser/CDP, Electron, and regional-pixel replays remain explicitly
legacy 26.825 evidence until the current-build route is re-recorded at those
controlled viewports.

## Evidence status

This file promotes only the current shell, sidebar geometry/lifecycle,
Projects route, Help DOM menu, route restoration, the bounded Scheduled tasks
native observation above, and target-selection/runtime identity anchors. It
does not promote every inventory row to 26.903. Existing
Browser/CDP, Electron, and regional-pixel results tied to 26.825 remain
independent regression evidence until a matching current-build slice is
captured.

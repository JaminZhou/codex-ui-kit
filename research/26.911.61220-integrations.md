# Current Plugins and Skills boundary — 26.911.61220

The isolated current-build capture used the installed `26.911.61220` / build
`9647` package (`Chromium 153.0.8010.36`, app.asar SHA-256
`f3ea5ab82e0503d283c98ea21b0ecfde4d8fe4c740fb1d81dfc57b6f5c22ce1f`). The
main Codex process was preserved. CDP sampled the Settings → Plugins route at
its native wide viewport and at an emulated 720×680 compact viewport. The
sanitized record is `research/current-integrations-26.911.json`; raw frames and
the isolated profile remain local-only.

The current build presents one shared `Plugins` route title and the description
`Manage plugins, skills, and MCPs` for both tabs. Plugins reports 13 items and
Skills reports 2. At the native wide viewport the content root is 768px wide,
the search field is 224×32px, and the single-column card grid begins at
y=225.796875px. At compact width the content root is 358.125px wide and the
search field is unmounted; the single-column grid remains bounded with zero
horizontal overflow. This compact omission is recorded as observed product
behavior rather than filled with a synthetic control.

Visible current labels are limited to the public catalog surface: GitHub,
Watch PR, AppKit Inspector, Default templates, Plugin Management, Sites,
Documents, PDF, Spreadsheets, Presentations, Template Creator, Computer Use,
Visualize, ASC Tooling, and RedRocket Market. The capture is read-only and
does not install, uninstall, authorize, enable, execute, or mutate any plugin
or skill. It therefore refreshes current package/runtime evidence without
promoting host-owned installation, permission, OAuth, registry, network, or
third-party artwork behavior to a renderer guarantee.

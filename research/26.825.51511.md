# Codex Desktop 26.825.51511 scoped runtime baseline

This note records the application-shell surfaces re-observed against the
installed Codex Desktop build on 2026-08-30. Evidence sampled only on
26.825.31414 or an earlier build remains previous-build regression evidence
until that surface is probed again.

## Fingerprint

- App version: `26.825.51511`
- Build number: `7377`
- Chromium: `151.0.7922.174`
- `app.asar` size: `284032150` bytes
- `app.asar` SHA-256:
  `f56ac8d5254a10fc4a04e7417fa787d135c3bbca49bad7d668d4ae65833d40c7`
- Installed resource:
  `/Applications/ChatGPT.app/Contents/Resources/app.asar`

The capture hashed the installed archive before and after sampling, retained
its device and inode identity, and proved that the isolated Renderer started
after the sampled bundle metadata settled.

## Capture boundary

One user-authorized second process used loopback-only CDP port `9897` and a
unique profile under `/private/tmp`. Existing Codex windows were not navigated
or resized. Main Renderer selection used the application URL, viewport area,
shell landmarks, and visible-control density. The capture contains only the
package identity, sanitized fixed labels, geometry, styles, focus state, and
content-dependent counts. Screenshots and private account, project,
repository, and task text remain local-only.

The reproducible capture is:

```sh
CODEX_CURRENT_BASELINE_CDP_PORT=9897 \
CODEX_CURRENT_BASELINE_PROFILE=/absolute/path/to/the/isolated/profile \
CODEX_CURRENT_BASELINE_ALLOW_NAVIGATION=1 \
CODEX_CURRENT_BASELINE_OUTPUT=/absolute/path/to/the/isolated/profile/current-baseline.json \
pnpm capture:current-baseline
```

## Re-observed shell contract

- New chat remains reachable at 1180×820, 820×680, 721×680, and 720×680.
  The persisted sidebar is 321.875px wide, the Composer editor is 44px tall,
  and every sampled state has zero horizontal overflow.
- The sidebar remains visible at 720px until explicitly hidden. Hide removes
  Sidebar, Back, and Forward controls; Show restores navigation and the compact
  Pull requests → New chat round trip.
- The fixed route stack is New chat, Pull requests, Scheduled, Plugins, and
  Explore. `Sites` remains absent from the sampled fixed-route stack.
- Project-group rows remain 30px tall and preserve pointer, Enter, Space,
  focus, resize, and Hide/Show continuity. Counts and scroll height are
  content-dependent.
- Help remains one 320×272.06 DOM menu with eight items, menu focus, Escape
  dismissal, and trigger-focus return.
- Projects remains 736px wide at 1180×820 with 512/64/128 columns and 70px
  rows. Its explicitly collapsed 600×600 state remains 559px wide with
  415/128 columns. Sort, empty search, expansion, focus continuity, and one
  owned scroller were re-observed.

## Account and Settings follow-up

The loopback-only isolated process on port `9899` re-opened the Account menu
and the General and Appearance Settings routes. The account capture changed
only the isolated UI preference through Light and Dark, restored System, and
returned to New chat. The user-owned Codex process was not navigated or
resized. Screenshots and account content remain local-only.

- Light/Dark at both 1180×820 and 720×680 retain the 321.875px sidebar and a
  305.875×188.375px six-item Account menu at x=9. Wide top is 587px and
  compact top is 447px; each state has weight-400 items, zero horizontal
  overflow, Escape dismissal, and trigger-focus return.
- Settings uses the same 321.875px rail. Twenty-two visible navigation rows
  are 305.875×29px, the content column is 768px at x=366.9375, and the sampled
  1180×820 routes have zero document-level horizontal overflow.
- General re-observes the current permission, Composer, editor, panel,
  menu-bar, sleep, keyboard, popout, and notification controls. This capture
  records reachability and geometry without changing their values.
- Appearance retains three 248×199.609375px System/Light/Dark previews plus
  the current code-theme, accent, background/ink, UI/code-font, translucency,
  contrast, cursor, Dock-icon, font-size, diff-marker, and smoothing controls.
  System was verified selected after the capture.
- The public General, Appearance, Account-menu, and Settings-shell fixtures
  already have Browser/CDP, Electron, and reviewed visual coverage. This
  same-build probe promotes those sampled groups from previous-build-only to
  current-build verification; other Settings pages remain separate evidence.

## Completed basic conversation follow-up

The same isolated process submitted one safe, no-tool task with the exact
prompt `Reply with exactly CURRENT BASIC MESSAGE.` and naturally received the
exact no-period response `CURRENT BASIC MESSAGE`. The capture revalidated the
installed archive before extracting only fixed labels, geometry, computed
styles, and reusable SVGs. Private sidebar labels and raw screenshots remain
local-only.

- At 1180×820 the sidebar is 321.875px and the main surface is 858.125px. The
  736px user/assistant lane begins at x=383.4375; the user bubble is
  334.65625×42.75px with a 70% maximum, 22px radius, `10px 16px` padding,
  and 14/22.75px weight-400 type.
- The flat assistant response begins at y=167.75. Copy, Good response, Bad
  response, and Fork chat from here occupy four 26×26px targets at 28px
  intervals, using the exact current SVG sources.
- The 736×98px Composer at y=706 retains a 712×44px editor and the five
  observed Add, permission, model, Dictate, and Send targets. At 721×680 the
  sidebar remains open and the lane/Composer shrink to 366.125px; at
  720×680 the sidebar collapses and both expand to 687px with zero overflow.
- The 47px header now reproduces the separate 28px project-icon hit area,
  title button, Chat actions, Share, summary, bottom-panel, and side-panel
  controls. Header positions and the 721→720 responsive boundary are gated in
  both Browser/CDP and Electron native windows.
- Three reviewed full-frame baselines cover wide, boundary-open, and compact.
  Local-only product comparisons split ownership into header, thread, and
  Composer rather than allowing the dark background to dilute differences.
  The measured ratios are 4.7240%/0.4016%/0.8072% wide,
  6.1697%/0.4733%/0.6858% at 721px, and
  6.2470%/0.0621%/0.8006% at 720px, under independent 7%/0.6%/1% limits.
- The current semantic asset inventory grows to 118 icons and adds the
  observed Explore, Share, window-sidebar, Composer Send, and completed-turn
  response actions. The scoped current-thread baseline does not overwrite
  historical scenario-specific raster evidence.

## Sidebar thread lifecycle follow-up

A second isolated process on loopback-only port `9898` submitted two safe,
no-tool plain-text tasks. One task was observed while selected and running,
then in the background, and after natural completion; the earlier completed
background task supplied a genuine unread state. The user-owned Codex process
was not resized or navigated.

- At both 1180×820 and 720×680, the visible navigation remains 321.875px wide.
  Thread rows are 305.875×30px at x=8, with a 12.5px radius,
  `5px 5px 5px 8px` padding, and 13/18.5714px weight-400 system type.
- A selected running row uses the 8% foreground background and the existing
  20×20 trailing rail with the exact 16×16 two-path spinner. Completion while
  in the background replaces it with an 8×8 `#3a83f7` info-solid dot centered
  in the same rail. The capture explicitly verified that Stop was absent
  before classifying the latter as complete.
- Hover replaces the status rail with exact `Pin chat` and `Archive chat`
  controls. Each is 19×20px, the SVGs render at 16×16, the gap is 8px, and the
  action rail ends 8px from the row edge.
- The public replay exposes separate selected-running, background-unread,
  hover-action, idle, and compact fixtures. Browser/CDP and Electron gate the
  computed geometry, styles, names, status replacement, and zero overflow;
  three reviewed frames cover wide, hover, and compact presentation.
- Local-only current-product comparisons reduce dynamic text to owned shapes:
  action glyphs differ by 2.9762%, the active spinner by 0.3571%, and the
  unread dot by 0%. Raw screenshots, task identifiers, and titles remain
  outside the repository.

This promotes the sampled ordinary thread-history, item-action, and status
groups to current-build verification. Waiting, error, worktree, long-list,
archive/pin mutations, and accessibility behavior not reached by this probe
remain broader or previous-build evidence.

## Sidebar worktree lifecycle follow-up

A further isolated process on loopback-only port `9898` opened a disposable
Git repository through the public application path. Real worktree creation,
a controlled `.git/worktrees` blocker, Retry, response completion, and
restored idle were observed without navigating or resizing the user-owned
Codex process.

- Selected setting-up uses a 305.875×30px row, 14×14 branch at a 39px right
  inset, and the exact two-path 16×16 spinner in a 20×20 rail.
- Current failure is branch plus the exact three-path 16×16 error glyph at an
  8px rail inset and `rgb(255, 103, 100)`. The selected/read sample has no
  unread marker; a later background failure carries error and unread together,
  so notification/read state is independent from worktree failure.
- After removing the controlled blocker and activating Retry, the row returns
  through setting-up. A naturally completed recovered response produces the
  8×8 `#3a83f7` unread dot; reading it leaves the restored branch at an 11px
  inset.
- Hover replaces branch/status presentation with exact `Pin chat` and
  `Archive chat` actions. Browser/CDP and Electron repeat the lifecycle at
  1180×820 and 720×680 with zero horizontal overflow.
- Two reviewed replay frames cover wide and compact composition. Local-only
  84×30 active, failed, and recovered shape comparisons pass at 3.7698%, 0%,
  and 0%; CDP independently locks the SVG paths, viewBoxes, colors, and
  geometry.

The sidebar implementation remains partial because queued/creating sidebar
variants are not promoted by this slice.
Product screenshots, profiles, repository contents, task identifiers, and
raw logs remain outside the repository.

## Worktree setup failure workspace follow-up

A second isolated worktree process on loopback-only port `9898` repeated the
controlled regular-file `.git/worktrees` blocker in a disposable repository.
It reached the real expanded and collapsed failure card, Edit environment,
Retry, both creating stages, the created handoff, and a naturally completed
no-tool response. Both exact tasks were archived, both exact child worktrees
were removed, and only the isolated PID/profile/repository were cleaned; the
user-owned Codex process remained running.

- The failure root is 736×276.5px at 1180×820. Its 736×247.5px card uses a
  1px 8.2%-foreground border, 15px radius, 12px padding, and 12px vertical
  gap. Collapsing details reduces the card to 736×112px.
- `Preparing workspace` and `Checking out files` are 21px rows separated by
  8px. Completed is `rgb(44, 103, 197)` and failed is
  `rgb(255, 103, 100)`; the replay retains the exact runtime-observed
  branch, completed, failed, and disclosure SVG paths.
- The expanded log is 710×123.5px with a 12.5px radius, 1px
  15.7%-foreground border, `rgb(24, 24, 24)` fill, and 13/19.5px weight-500
  monospace text. Repository identifiers and commit IDs are sanitized in the
  public fixture.
- Retry first shows `Creating a worktree`, in-progress Preparing plus pending
  Checkout, then completed Preparing plus in-progress Checkout. The card is
  removed for `Worktree created` and `Starting a task`. Edit environment
  routes to the current unavailable local-environment Settings state.
- At 720×680 the failure root is 366.125px wide and retains all controls with
  zero horizontal overflow. Browser/CDP verifies five frames and the complete
  disclosure/Retry transition; Electron repeats Retry, Cancel, and Edit
  environment. Five reviewed baselines cover expanded, collapsed, creating,
  created, and compact states.
- A local-only 736×277 foreground comparison masks the content-dependent log
  text and differs by 5.2082%, below the independent 6.5% limit. The product
  screenshot, profile, repository, task identifiers, and raw log remain
  outside the repository.

This promotes `workspace.worktrees` to current-build Browser/Electron evidence
for the sampled failure/recovery workspace. Worktree Settings retention,
permanent worktrees, missing-directory variants, and other paths remain
partial.

## Worktrees Settings follow-up

A later isolated process on loopback-only port `9898` opened Worktrees
Settings with a disposable repository and one temporary managed worktree. It
did not navigate or resize the user-owned Codex process. The tracked evidence
retains only fixed labels, sanitized fixture paths, geometry, styles, and
state transitions; raw screenshots and the isolated profile remain local-only.

- The current preferences are Worktree root, Always fetch upstream before
  creating worktrees, Automatically delete old worktrees, and Auto-delete
  limit. The sampled values were blank root, fetch disabled, automatic delete
  enabled, and limit 15; the user path is represented by a sanitized
  placeholder in the public replay.
- Managed worktrees are grouped under their project path. Each sampled card
  exposes Worktree, path, `Starts a fresh chat using the same files and
  branch`, New chat in this worktree, Delete, and Conversations.
- Moving the exact disposable worktree directory away and activating Refresh
  removed the card. Restoring the directory and refreshing restored it. The
  Settings route therefore reconciles missing directories by removal rather
  than showing the separate thread-level missing-directory repair flow.
- Product Delete removed the exact worktree directory and its Git registration
  immediately, with no confirmation dialog. Cleanup verified both were absent
  before moving only the isolated profile and repository recoverably to Trash;
  the main user process remained alive.
- At 1180×820 the Settings rail is 321.875px, the centered content is 768px,
  the preference card is 276.31px high, each project header is 46px, and the
  managed card is 124.58px. At 720×680 the controls remain fixed-size and the
  route keeps zero document-level horizontal overflow.
- Browser/CDP covers wide, light, 720px, empty, linked-conversation, and
  missing-refresh frames plus controlled preference/New chat/Refresh/Delete
  actions. Electron repeats the wide interactions and route persistence. Six
  reviewed baselines pass; a local-only 768×548 owned-route crop differs by
  4.5984%, below the independent 5% limit.

The public component deliberately exposes host-owned callbacks and does not
perform filesystem work itself. `Create permanent worktree` is still present
in the current native project menu, but native UI automation was unavailable
and the action was not clicked. Permanent creation, automatic-pruning timing,
and broader retention lifecycles therefore remain open.

## Project Section submenu drift

The project action boundary still begins with a frozen
`electronBridge.showContextMenu` provider, but the provider and the final
native menu are distinct layers on this build:

- the provider contains `Unpin`, `Edit`, `Section`, `Reveal in Finder`,
  `Create permanent worktree`, `Archive chats`, and `Remove project`, plus
  three separators;
- `Section` owns a submenu and has no direct `onSelect`; the sampled profile
  has no custom sections, so its submenu contains only `New section…`;
- the rendered AppKit menu filters the provider-only `Reveal in Finder` row in
  this context. Its visible six actions are `Unpin`, `Edit`, `Section`,
  `Create permanent worktree`, `Archive chats`, and `Remove project`;
- the layer-101 native main menu is exactly 252×187 and the one-row submenu is
  exactly 118×34;
- the `Section` SVG is captured from the provider as one exact 16×16 source,
  recorded with SHA-256
  `a3eb21ed39b3b2483c2803b81743cd34ff4d1b53ccd134d664177490ab983a17`.

The public replica therefore retains provider evidence separately from the
visible composition. `MenuSubmenu` now exposes an accessible submenu label and
scoped class/style/width hooks. Browser/CDP and Electron gate ArrowRight open,
first-item focus, ArrowLeft close, Escape dismissal, and focus return. Two
reviewed Electron frames cover the main and nested menu. Local-only unmasked
comparisons pass at 3.8155% for the 252×187 main menu and 6.9542% for the
text-dominant 118×34 submenu, with geometry, sampled colors, and exact icon
source gated independently.

## Rich Markdown streaming follow-up

The same installed fingerprint was rechecked before and after one isolated
no-tool task. Its fixed prompt produced a public Codex link, two task rows, a
TypeScript fence, a seven-column table, exactly 36 H2 sections, and a terminal
marker. CDP captured link-only, open-empty-fence, table, long-running,
tail-running, and completed milestones without reading project files.

- the response root remains 736px wide at 14/22.75px; H1 is 21/28px and H2 is
  17.5/24.5px with 14px top and 3.5px bottom margins;
- task controls are disabled 14×14 round `role=checkbox` buttons;
- the completed table is 736×122.75px with a 29px header, a 41.25px ordinary
  row, and a 52.5px final row;
- an open fence mounts an empty TypeScript card instead of visible placeholder
  text; the public component now preserves that empty state;
- running frames retain the 28×28 Stop control and omit response actions;
  completion removes Stop and exposes Copy, Good response, Bad response, and
  Fork chat from here;
- latest uses reverse-origin `scrollTop=0`; negative scroll-away remains
  stable while new chunks arrive and return-to-latest restores zero.

The deterministic public replay passes six Browser/CDP checkpoints, Electron
copy/table/scroll/native-window checks, and five reviewed baselines. Four
local-only product regions pass at 2.4398%, 4.7698%, 2.7219%, and 1.7196%
under an 8% ceiling. Screenshots and private sidebar labels remain outside the
repository. Citations and live media now have independent current slices;
table/Markdown errors and plugin-provided variants remain open.

Activity and other unsampled groups remain 26.825.31414 or earlier evidence.
This follow-up promotes only the sampled Markdown mutation lifecycle; Web
Search and the Browser failure/recovery path are refreshed independently below.

## Web Search current-build follow-up

One isolated read-only task used only Web Search and completed two public
queries without reading or modifying local files. Its screenshots and profile
remain local-only; the committed fixture keeps the fixed prompt, public OpenAI
URL, current labels, and sanitized task identity.

- the outer timeline settles as `Worked for 23s` at 14/21px, weight 400;
- expanding it exposes `Searched the web`; expanding that activity exposes
  `Searched the web for Codex app desktop` and
  `Searched the web for "desktop"`, replacing the previous build's suffix and
  single-quote copy;
- the exact assistant response is `CURRENT 26.825 SEARCH OBSERVED`, followed by
  the four normal response actions; Send is restored and horizontal overflow
  remains zero;
- Browser/CDP gates collapsed, timeline-open, and fully expanded frames.
  Electron repeats disclosure collapse/reopen, and three reviewed baselines
  cover those states;
- the local-only 420×79 foreground region differs by 4.0506% from the current
  product, below a tightened 5% hard limit.

This promotes `thread.search-tool-events` for the sampled current path. The
26.825.31414 Browser activity/workspace capture remains historical evidence;
the independent current failure and success probes below now separate the
activity lifecycle from the workspace lifecycle.

## Browser failure and Composer recovery follow-up

One isolated `26.825.51511` process on a unique profile ran two Browser-only
prompts without project-file access, shell commands, or Web Search. The first
used the public Codex URL inline; the retry put the same exact URL on its own
line to remove prompt punctuation as a confounder. Both turns reached natural
terminal states:

- after `Worked for 3m 46s`, the first assistant answer is exactly
  `错误：Browser 启动失败，缺少 Playwright Chromium 可执行文件。`;
- after `Worked for 27s`, the retry answer is exactly
  `Browser error: Unsupported browser service request.`;
- the settled UI hides the failed protocol call rows, removes Stop, keeps the
  four 26×26 Copy/Good/Bad/Fork actions, and restores a writable 736×98 wide or
  688×98 compact Composer;
- Browser/CDP locks three wide/720 frames, exact 14/22.75px error typography,
  14/21px durations, response actions, zero workspace/MCP disclosure, geometry,
  and terminal state. Electron repeats all three native-window variants and
  proves the Composer accepts new input after each failure;
- three reviewed baselines gate deterministic output. Optional local-only
  product comparisons pass at 1.9864% content and 1.0800% Composer wide, plus
  3.1108% content and 1.4832% Composer at 720×820.

The 12-event replay preserves the two failed `mcpToolCall` records in protocol
state even though the terminal product UI collapses them. This promotes the
sampled current `thread.tool-unavailable-recovery` path and adds current failure
evidence to `thread.browser-tool-events`; it does not promote the successful
Browser activity group or `workspace.browser`. External page pixels, successful
navigation, multi-tab, authentication, download, permission, history, and page
error states remain incomplete. The aggregate deterministic matrix is now 57
fixtures, 641 events, and 370 Browser/CDP and reviewed-pixel scenes.

## Browser success current-build follow-up

One isolated `26.825.51511` process on loopback CDP port `9917` and a unique
profile completed a Browser-only public-docs task naturally. It matched build
`7377` and `app.asar` SHA-256
`f56ac8d5254a10fc4a04e7417fa787d135c3bbca49bad7d668d4ae65833d40c7`.
The main user-owned Codex PID `13503` remained outside the capture boundary.

- the settled timeline is exactly `Worked for 50s`;
- the expanded activity is `Used the browser, ran a command` followed by
  `Load Browser instructions`, `Connect to Browser`, and
  `Verify Codex documentation`;
- the terminal answer is `CURRENT BROWSER SUCCESS 26.825`, and the public
  source is `https://developers.openai.com/codex/`;
- no Browser workspace is mounted at 1180×820 or 720×820, so the former
  419.59375px workspace is retained only as 26.825.31414 regression evidence;
- CDP locks collapsed, timeline-open, fully expanded, and compact-expanded
  frames, exact 14/21px activity typography, 14/22.75px prompt/answer
  typography, public URL, zero overflow, and responsive sidebar state;
- Electron repeats wide/compact disclosure collapse/reopen, while four
  reviewed baselines cover the deterministic states;
- local-only 500×205 foreground comparisons pass at 6.0517% wide and 5.7941%
  compact under a 6.5% hard limit. Product screenshots and task data are not
  committed.

The current globe and Browser-connection glyphs are captured as exact
playground-only runtime assets with component-scoped provenance. This promotes
`thread.browser-tool-events` to current Browser/Electron verification for the
sampled success path while leaving multi-tab, authentication, download,
permission, history, page-owned error states, and `workspace.browser`
incomplete.

Cleanup archived only the disposable task, terminated validated PID `21412`
and its exact-profile children, verified port `9917` closed, and moved the
profile plus raw evidence recoverably to
`/Users/JaminZhou/.Trash/codex-ui-kit-browser-success-26-825-20260901-9917-evidence`.
PID `13503` remained running.

## Thread summary current-build follow-up

One isolated disposable task opened the thread-owned summary while the task's
remote response was reconnecting. That transport state is outside the summary
surface and is excluded from its evidence; the overlay itself remained fully
interactive and was sampled through CDP at 1180×820.

- the 28×28 `Toggle summary` control remains at x=1076/y=9, while the overlay
  now begins at x=804/y=53 rather than the earlier y=45 and remains 300×199px
  with a 25px radius and `rgb(45, 45, 45)` background;
- the Environment header and visible labels use 14/21px weight-400 system
  typography inside 16/24px row containers; the five rows remain 272×29px;
- the sampled dirty branch exposes dynamic Changes counts, Local execution,
  the current branch, enabled Commit/Push, and enabled pull-request creation;
- collapsing Environment reduces the dialog to 300×46.8125px. Escape and an
  outside pointer both dismiss it. The sampled product leaves focus unset,
  while the public replay deliberately keeps the safer trigger-focus return;
- Browser/CDP and Electron gate the updated geometry, current computed styles,
  collapse/reopen/dismissal, and compact containment. One reviewed baseline is
  refreshed. The local-only product crop masks only the dynamic Changes delta
  and differs by 2.6918%, below the 3% hard limit.

The same task exposed the current 726×28.5625px `/compact` command row with
`Compact this chat's context`; that attempt omitted the content-percentage
suffix and then failed with `Error running remote compact task: Connection
failed: error sending request`. A fresh isolated probe after network recovery
supersedes that negative outcome. Its command was `Compact this chat's context
(10% full)`, confirming that the suffix is content-dependent. The task moved
through `Working`, `Compacting context`, and `Context compacted`; a second
exact no-tool message then succeeded in the same task. A second `/compact` in
that recovered task also settled successfully, closing the previously open
repeated-compaction gap.

The clean current capture locks the menu at 13/18.5714px weight 400, running
and completed labels at 14/21px weight 400, 28×28 Stop/Send controls, and zero
horizontal overflow at both 1180×820 and 720×680. Browser/CDP and Electron
drive the first and repeated command-to-recovery lifecycles. Eight reviewed
baselines cover the command menu, running, completed, recovered, repeated, and
compact recovered states. The local-only ownership-masked running comparison
differs by 1.3189% under a 1.5% limit. Raw screenshots, task navigation data,
and the isolated profile remain outside the repository.

## Markdown media follow-up

Two further isolated no-tool turns reached real media output on this exact
build. The first rendered the public `https://openai.com/favicon.ico`, and the
second rendered an intentionally unreachable `example.invalid` URL. The wide
task was resized to 720×680 before the compact samples, and the loaded image
was opened with a real pointer action.

- the loaded response root is 736×110.75px at 1180×820 and 687×110.75px at
  720×680; its media paragraph is 780.5625×74px and 687×74px respectively;
- the ready trigger is 50×74px around a 48×48 image with a one-pixel subtle
  border, 10px radius, 12px vertical margin, 200px maximum size, and the
  sampled five-layer shadow;
- the next paragraph starts 14px below the media paragraph;
- the unavailable root is 736×156.75px wide and 687×156.75px compact. Its
  disabled 96×96 button uses a 28×28 image placeholder, 10px radius,
  `rgba(255,255,255,.08)` background, and has no retry or external-link
  navigation;
- immersive preview is an 1180×820 modal named `Image preview`. Close receives
  initial focus at 1126×12 / 42×40px, the 48×48 media slot is centered at
  x=566/y=338.71875, the caption is 124.046875×34.5625px, and the zoom toolbar
  is 152×44px at x=514/y=744. This source-owned preview resolves to an empty
  image payload in the sampled product, so the public replay explicitly gates
  an empty visual slot while retaining its geometry and alt semantics.

The sanitized ten-event replay, five Browser/CDP frames, five real Electron
flows, and five reviewed baselines now gate loaded, unavailable, compact, and
immersive-preview behavior. Five local-only comparisons pass at 1.2369%
loaded wide, 0.0197% loaded compact, 0.8141% unavailable wide, 0.6471%
unavailable compact, and at most 3.4912% across preview caption, Close, empty
image slot, and zoom regions. The exact public favicon bytes are embedded only
in the deterministic playground; product screenshots, task data, and the
disposable profile remain outside the repository.

## Completed command anchor follow-up

A fresh isolated task used the public terminal path to run only
`/usr/bin/uuidgen`. The generated UUID proved that the response depended on a
real command execution rather than a text-only imitation. The tracked replay
replaces that dynamic value with a same-length sanitized UUID; the real value,
task identifier, screenshots, and profile remain local-only.

- the settled product exposes `Worked for 8s`, then the expanded
  `Ran /usr/bin/uuidgen` row with no Shell card, raw output panel, exit-code
  footer, or row disclosure;
- command and duration text remain 14/21px, weight 400, at x=244.5/223.5 in
  the 1180×820 frame and x=38/17 in the 720×680 frame;
- the generated result is a flat 14/22.75px assistant message, followed by
  Copy, Good response, Bad response, and Fork chat from here;
- completion removes Stop, restores Send, and retains zero horizontal
  overflow at both widths;
- Browser/CDP gates the exact semantic, geometry, style, and hidden-protocol
  contract. Electron repeats the expanded lifecycle and native resize. Two
  reviewed baselines cover wide and compact composition;
- local-only same-build comparisons split ownership into activity, Composer,
  and header regions. Wide ratios are 2.2648%, 0.9375%, and 4.9044%; compact
  ratios are 1.9196%, 1.1022%, and 6.5485%, under independent
  2.3%/1.2%/7% ceilings.

This promotes `thread.command-execution` for the sampled current success
path. The following current-build follow-ups close exit-code failure/recovery
and interruption; background processes, direct Terminal tabs, and broader
recovery variants retain their separately versioned P0 gates.

## Command failure and recovery follow-up

A second isolated task ran exactly one shell command that emitted fixed stdout
and stderr markers and exited with code 7, then accepted one exact no-tool
follow-up in the same thread. The real task title, screenshots, and disposable
profile remain local-only; the public replay contains only the fixed prompt,
fixed markers, and sanitized workspace path.

- The settled product exposes `Worked for 15s` and an expanded neutral
  `Ran printf …; exit 7` row. It does not expose stdout, stderr, a Shell card,
  an output disclosure, or an exit-code footer.
- The protocol replay still retains the exact two-line aggregated output and
  exit code 7, so compact presentation does not erase behavioral evidence.
- The first assistant response is the exact failure acknowledgement. A second
  no-tool prompt receives the exact recovery acknowledgement in the same
  thread; Send is restored and both 1180×820 and 720×680 frames have zero
  horizontal overflow.
- Browser/CDP gates completed, recovered, and compact frames. Electron repeats
  the lifecycle in native windows, and three reviewed baselines cover the same
  compositions.
- The unmasked 736×125 activity/failure region differs from the current
  product by `0.012760869565217391` (1.2761%), below its independent 2% hard
  limit. The recovery turn remains independently gated by CDP, Electron, and
  the reviewed full-frame baseline.

This promotes `thread.command-failure-recovery` for the sampled current path.
Interruption is addressed below; background-process management, direct
Terminal tabs, other command kinds, and truncation variants retain separate
P0 gates.

## Command interruption and recovery follow-up

A third isolated task ran one exact read-only 120-second loop, stopped the real
running turn, waited for its child process to settle, and then accepted one
exact no-tool recovery prompt in the same thread. The real task screenshots
and disposable profile remain local-only; the public fixture retains only the
fixed command and sanitized task identity.

- While running, the product exposes `Working for 19s`, the neutral
  `Running for i in …` row, and the 28×28 Stop control. It shows no Shell card,
  raw command output, footer, or row disclosure.
- Immediately after Stop, the summary reads `You stopped after 0s`. After the
  child process settles, it updates to `You stopped after 20s`; the square
  `Background terminal stopped with …` row persists through settlement and the
  later recovery turn.
- The exact no-tool follow-up receives
  `CURRENT 26.825 INTERRUPTION RECOVERY ACCEPTED` in the same thread. Send is
  restored, Stop is absent, and wide/compact frames retain zero horizontal
  overflow.
- Browser/CDP gates running, immediate-stop, recovered-wide, and
  recovered-compact frames. Electron repeats the four native states, and four
  reviewed baselines cover the same compositions.
- Local-only unmasked comparisons measure 4.9372% for the 736×74 stopped
  region and 2.0661% for the 688×242 compact recovery region, below independent
  5.5% and 3% hard limits.

This promotes `thread.interruption-stop` for the sampled current path. Full
background-process management, direct Terminal tabs, command-output
truncation, and broader command kinds retain separate P0 gates.

## Review card and workspace anchor follow-up

A fresh isolated `apply_patch` task created one file, replaced one path as a
raw delete plus add, and deleted one file. The visible card aggregates those
four protocol entries into three rows with `+5 −5`, while the Review workspace
preserves all four raw entries and both `alpha.txt` identities.

- the closed card is 736×173.5px at 1180×820 and 688×173.5px at 720×680;
- opening Review changes the subtitle to `Review changes ↗` and initially
  expands only `added.txt`;
- the Review panel is 591.828125×820 wide and 344.671875×680 compact, with the
  existing 46px header, 40px toolbar, 203px wide filter, and four diff/tree
  entries;
- the card-files and Undo glyphs are exact same-build SVG captures;
- completion exposes `Worked for 20s`, Copy, Good response, Bad response, Fork,
  and Send, removes Stop, and retains zero horizontal overflow;
- Browser/CDP and Electron gate four wide/compact frames. Four reviewed
  internal baselines pass. Local-only product ratios are 4.2901%, 4.9780%,
  and 6.4586% for the card regions, and 1.9445%/2.5733% for the wide/compact
  panel regions under separate 6.6% and 3% ceilings.

The real task id, screenshots, profile, and disposable file contents remain
local-only. The public fixture contains only sanitized paths and text. This
promotes the sampled file-card, side-panel, editor-diff, and multi-file Review
groups on the current build without claiming binary/conflict, larger-set, or
broader Undo/failure coverage.

## MCP two-turn lifecycle follow-up

A final capture-only probe used one isolated, user-authorized task and the
OpenAI Developer Docs integration. The capture script never submitted a
prompt: it selected the exact task by a title hash and recorded only fixed
labels, geometry, computed styles, reusable SVGs, and sanitized lifecycle
facts. The real task identifier, profile, raw screenshots, and dynamic
workspace counts remain local-only.

- the first turn completes Search → Fetch in 20 seconds and returns the
  fixed `Model Context Protocol` title and public documentation URL;
- the second turn stays in the same thread, records invalid-URL Fetch →
  Search → valid Fetch, and completes in 10 seconds. The failed row remains
  a flat noninteractive call row inside the integration group, with no legacy
  error card or disclosure;
- at 1180×820 the success group starts at x=222/y=303 and the recovery group
  at x=222/y=501. At 720×680 the recovery group starts at x=16/y=338, call
  labels start at x=38, and document-level horizontal overflow remains zero;
- the current pinned summary is 300×313px at x=864/y=59. It combines
  Environment and Sources, uses 272×29px rows, closes when the pinned toggle
  is cleared, stays closed after an outside click, and repins from the same
  header control;
- Browser/CDP gates four exact lifecycle frames and Electron repeats expanded
  success/recovery, summary close/repin, and native compact resize. Four
  reviewed internal baselines pass;
- local-only product comparisons differ by 2.6742% for success, 4.8070% for
  recovery, 3.8995% for compact recovery, and 2.9276% for the pinned summary,
  under independent 2.8%, 5%, 4.1%, and 3.1% limits.

Cleanup archived the exact disposable task, terminated only validated isolated
main PID `56767` and its two exact-profile Crashpad handlers, verified loopback
port `9902` and every exact-profile process were gone, and moved the profile
and raw capture files recoverably to
`/Users/JaminZhou/.Trash/codex-ui-kit-current-mcp.4ct2le-20260831`. The
user-owned Codex PID `21152` remained running.

This supersedes the sampled primary MCP and Sources anchors from 26.820 while
retaining `partial` implementation status for authentication, elicitation,
MCP approvals, cancellation, terminal same-transport failure, and other
integration variants. The later transport section independently supersedes
the sampled successful disconnect/reconnect boundary.

## Personalization Settings follow-up

A read-only isolated-profile probe opened Settings → Personalization and
recorded fixed copy, roles, geometry, computed styles, and responsive scroll
ownership. It did not toggle memory, delete data, change Personality, edit the
stored instructions, or press Save. The settings textarea was masked in every
local-only product screenshot.

- wide content is 768px, the editor is 768×147.94px, the Memory card contains
  three rows, and both observed memory switches are 32×20px;
- the Friendly/Pragmatic menu is 268×105.13px with two 46.56px radio items;
- at 720×680 content is 358.125px wide, the main owner scrolls vertically with
  zero document-level horizontal overflow, and the lower warning/Personality
  composition remains fully reachable;
- the controlled public component verifies dirty/restore, host-owned Save and
  Delete callbacks, switch state, menu selection, keyboard traversal, Escape
  dismissal, and trigger focus return;
- Browser/CDP and Electron cover four wide/compact/menu/bottom states. Four
  reviewed internal baselines pass, while local-only ownership-masked product
  comparisons differ by 5.4469%, 4.5811%, and 7.4199% for wide, menu, and
  compact top under 5.5%/5.5%/8% limits.

The product screenshots, current account values, task identity, and isolated
profile remain untracked. Cleanup archived the exact disposable task, stopped
only validated isolated PID `5806` and its exact-profile process tree, verified
loopback port `9902` closed, moved the profile recoverably to
`/Users/JaminZhou/.Trash/codex-ui-kit-connections.qg25Jy-20260831`, and left
the user-owned Codex PID `21152` running. This promotes only
`settings.personalization`; other Settings lifecycles remain separate.

## Keyboard shortcuts and Voice Settings follow-up

A second read-only isolated-profile probe opened Keyboard shortcuts and Voice
without changing persisted product state. Keyboard exposes 129 fixed rows. Its
`dictation` query returns exactly Start dictation, Hold-to-dictate hotkey, and
Toggle dictation hotkey; edit mode exposes a 144×28 capture field and 28px
Cancel action. The search field sticks at y=66 when the 8391px wide list is
bottom-scrolled. At 720×680 the main content remains 358.125px wide with zero
document overflow; the product itself gives the copy column zero width and the
binding column 324.125px, producing the observed narrow wrapping.

Voice records the 768px wide and 358.125px compact columns, 32×20 switches,
microphone menu, Sol selection, Voice chat and Dictation hotkeys, dependent
dictation-bar switch, dictionary, Recent recordings, and bottom reachability.
The 520×387.75 picker exposes nine fixed choices and a 144×144 host-artwork
button. The public component accepts supplied artwork; the local-only picker
comparison masks only that region.

Browser/CDP gates ten frames and Electron repeats controlled search/capture,
microphone/voice selection, dependent hotkey switching, and wide→compact
geometry. Ten reviewed replay baselines pass. Local-only full-window product
comparisons differ by 4.4204%/3.2076%/4.7363% for Keyboard wide/search/compact
and 3.2773%/3.6313%/3.3589%/6.4479% for Voice
wide/microphone/picker/compact. No product screenshots, account data, audio, or
profile files are tracked. This independently promotes
`settings.keyboard-shortcuts` and `settings.voice-dictation`; other Settings
routes remain open.

Cleanup stopped only validated isolated PID `49357` and its exact-profile
Crashpad handlers, verified loopback port `9910` closed, moved the disposable
profile recoverably to
`/Users/JaminZhou/.Trash/codex-ui-kit-keyboard-voice.yICOOQ-20260831`, and
confirmed the user-owned Codex PID `21152` remained running.

## Usage & billing and embedded plan follow-up

A third isolated-profile probe opened Settings → Usage & billing without
changing persisted account state. The wide route keeps the 321.875px Settings
rail and a 768px content column; the 720×680 route keeps the same rail and a
358.125px content column. The sampled page exposes Your plan, Credits balance,
General usage limits, GPT-5.3-Codex-Spark usage limits, Usage limit resets, and
Cancel plan. Its three meters are 96×8px and report 28%, 100%, and 100% in the
sanitized replay. Bottom scroll positions prove every section remains
reachable without document-level horizontal overflow.

`View plans` opens a separate embedded webview below a 46px host topbar. The
sample covers Personal and Business, 5x/20x and annual/monthly selectors, the
two-column 1180px composition, and the Pro-first 720px stack. The compact
product card is 384px wide and preserves the same max-width rather than filling
the viewport. `Buy credits` was opened only far enough to prove host/webview
ownership and then backed out; no checkout or plan action was completed. The
public surface therefore exposes controlled callbacks and supplied plan data,
not account mutation or provider logic.

Browser/CDP and Electron gate seven Usage/plan frames, meter state,
wide→compact geometry, scroll reachability, audience/selector transitions,
and app return. Seven reviewed replay baselines pass. Local-only product
comparisons differ by 4.3294%/5.2131%/4.8748%/4.8752% for Usage
wide/bottom/compact/compact-bottom and 3.0856%/6.2140%/6.4669% for plan
Personal/Business/compact. Usage masks only the translucent native Settings
rail; plan comparisons are unmasked. No product screenshots, checkout URLs,
account values, or profile files are tracked. This promotes only
`settings.usage-subscription`; real purchase, gifting, billing-provider, and
other Settings lifecycles remain open.

Cleanup stopped only validated isolated PID `86804`, its exact descendant
tree, and the two exact-profile Crashpad handlers. Loopback port `9911` closed,
the disposable 25MB profile moved recoverably to
`/Users/JaminZhou/.Trash/codex-ui-kit-current-usage.Biu3jQ-20260831-0612`,
and the user-owned Codex PID `21152` remained running.

## Inline citations and Sources workspace follow-up

One isolated current-build task used Web Search only against official OpenAI
domains and completed with three short bullets, three inline `OpenAI`
citations, and the fixed `CITATION_SOURCES_READY` marker. The three anchors
resolve to two public OpenAI pages and use the observed 16×16 favicon inside a
14/22.75px inline mention. The task reports `Worked for 24s`; its raw task id,
profile, screenshots, and private sidebar labels remain local-only.

- the current Environment + Sources summary is 300×313px at y=53 and sits
  76px from the window edge in both 1180×820 and 720×680 standalone layouts;
- `View all` opens a nonmodal Sources workspace. Wide preserves the
  321.875px sidebar and uses a 418.515625px side panel; compact assigns
  374.328125px to the conversation and 345.671875px to Sources;
- the Web search activity exposes `Searched 5 times`, a leading globe, and the
  five exact sanitized query rows. Disclosure collapse/expand, tab close, and
  compact `View all` reopen are controlled and keyboard reachable;
- six deterministic wide/compact/completed/summary/Sources scenes pass
  Browser/CDP, reviewed visual, and responsive geometry gates. Electron
  repeats the native 720×680 disclosure, close, and reopen lifecycle;
- five optional local-only product comparisons pass. Summary is 1.4686% in
  wide and compact; Sources is 2.6095% wide and 3.6505% compact; complete and
  Sources-thread regions are 4.0355%–4.6384%; Composer is
  1.1478%–2.3649%; the wide header is 4.5529%, all under independent regional
  ceilings.

This promotes only the observed inline OpenAI citation and Web Search Sources
workspace contract. Other providers, mixed citation clusters, live media,
malformed citations, and Sources failure/empty/loading states remain partial.

## Project and run-location context follow-up

An isolated same-build probe re-observed the complete new-chat context strip
at 1180×820 and the project picker at 720×680. The accepted public replay keeps
the 736×28 context row, 736×98 Composer, 56px home mark, and the installed
project → run location → branch order. The project trigger is 114×28px, Local
is 72.109375×28px, and the branch trigger remains a bounded 200×28px control.

The 260×249.5px project dialog now uses the observed search icon/input,
20px radius, 8px backdrop blur, weight-400 system typography, 15px option
radius, exact selected check asset, and the shared 252×208.9375px listbox.
No-match search preserves `New project` and `Don't work in a project`; Escape
returns focus and the no-project → project round trip remains reversible. Its
compact origin is x=35/y=282 with no document overflow.

The current `Work in` menu is 216×189.3125px and contains Local, New local
worktree, Connect Codex web, disabled Cloud, and Usage remaining. Selecting
New local worktree exposes project, run-location, environment, and
starting-state controls. The 264×91.125px Environment menu contains only Work
without environment and Set up project. Returning to Local exposes the
296×280.125px branch menu with the current project-specific search placeholder,
eight `menuitem` rows, no `aria-checked` radio state, no legacy Select local
environment action, and one Create and checkout action.

Browser/CDP gates all seven frames, exact menu/listbox semantics, focus,
geometry, computed style, and responsive containment. Electron repeats the
real click lifecycle and launches an independent native 720×680 compact
window. Seven reviewed replay baselines pass. Seven optional local-only
product regions pass at 0.7909%, 2.9154%, 2.9108%, 11.1381%, 1.7372%,
1.3736%, and 4.1699%; the 11.5% run-location ceiling accounts for the
observed icon-stroke and antialiasing delta while its semantics and computed
geometry remain independently exact. Product screenshots and the disposable
profile are not committed.

This promotes the sampled current paths for `conversation.context-controls`,
`conversation.project-picker`, `composer.project-worktree-selection`, and
`app.new-thread-workspace-selection` to current Browser/Electron verification.
Populated environment creation/editing,
Remote connection, branch mutation failures, and other project-provider states
remain independent gaps.

Cleanup terminated only the validated isolated PID `76952`, its exact child
tree, and exact-profile Crashpad handlers; loopback port `9897` closed. The
profile moved recoverably to
`/Users/JaminZhou/.Trash/codex-ui-kit-context-26-825.Nh3CEK-20260831-1040`,
and the user-owned Codex PID `43340` remained running.

## Composer permissions and active modes follow-up

A separate isolated same-build process on loopback-only port `9898` observed
the new-chat Composer without navigating or resizing the user-owned Codex
process. The permission menu measures 438.6875×161.6875px with 4px padding,
20px radius, 8px backdrop blur, and three 42.5625px ordinary `menuitem` rows.
It uses Ask for approval, Approve for me, and Full access with no
`aria-checked` radio state; the selected row uses the current check primitive.

Goal keeps the wide Composer at 736×134px and the compact Composer at
688×134px. Plan keeps the corresponding 736×98px and 688×98px surfaces. The
public replay now uses the observed Goal and Plan SVG geometry, active labels,
placeholders, clear action, and editor-focus restoration. Browser/CDP covers
all six wide/compact frames plus permission selection and Escape. Electron
repeats the native permission lifecycle and the compact Goal/Plan clear/focus
paths. Six reviewed baselines pass.

Optional local-only current-product comparisons pass at 6.5300% wide and
6.5272% compact for the text-dense permission overlay under a 7% ceiling;
Goal passes at 3.2000% and 3.6825%, and Plan at 2.9919% and 3.5062%, under 5%
ceilings. The reference screenshots and isolated profile are not committed.
Custom permission policy, permission persistence, unavailable modes, and
other host-owned variants remain outside this sampled contract.

Cleanup terminated only the validated isolated PID `25609` and its child
tree; loopback port `9898` closed. The exact profile moved recoverably to
`/Users/JaminZhou/.Trash/codex-ui-kit-composer-26-825.Ke04O7-20260831-1156`,
and the user-owned Codex PID `43340` remained running.

## Composer resources, multiline input, and queue follow-up

A later isolated same-build process on loopback-only port `9898` refreshed the
remaining new-chat Composer controls. Add opens a 736×320 wide or 687×320
compact resource picker with 4px padding, 20px radius, 8px backdrop blur, a
310px scroller, and 28.5625px options. The observed fixed top contains Files
and folders, Attach Google Chrome, Work in a project, Goal, Plan mode, and
Record a skill. Plugin rows remain sanitized host-supplied examples rather
than copied account data.

Four lines grow the Composer to 736/687×134px and its editor to 712/663×80px.
Twenty lines clamp at 736×259px wide with a 712×205 editor and 687×224px
compact with a 663×170 editor. Both variants preserve 14/20px weight-400
typography, local scrolling, and the bottom controls.

The current queue uses a 710×37px pending tray above the 736×98 Composer. Stop
now exposes a two-stage paused state: a 710×72px tray containing `Queue paused
because you interrupted`, a header Resume action, and the queued row; the
empty Composer primary also becomes Resume. Header Resume enters a ready state,
Composer Resume restarts the interrupted turn, and the queued prompt then runs
automatically before the queue settles. This supersedes the 26.727 observation
that exposed only automatic continuation while retaining that older replay as
regression coverage.

Browser/CDP gates ten wide/compact frames, exact resource and queue geometry,
long-input scroll position, Escape/focus restoration, and the complete
pause → ready → resumed → continued → settled lifecycle. Electron repeats the
compact resource and multiline contracts plus the wide paused queue lifecycle.
Ten reviewed baselines pass. Optional local-only product comparisons pass at
3.2757%/3.7314% for resources, 1.3181%/1.6791% for four lines,
0.6557%/0.9681% for long input, 1.9966%/1.0640% for pending queue, and
2.1011%/2.2862% for paused queue. The compact pending comparison masks only
the captured transient Add tooltip and its hover-reflowed bottom controls;
queue geometry and the Stop primary remain compared. No product screenshots,
private labels, or profile files are tracked.

## Long-thread virtualization and message navigation follow-up

A final isolated same-build process on loopback-only port `9888` created one
disposable 30-turn task whose prompts and replies were deterministic and used
no tools or files. The capture helper is navigation-only: task creation stayed
in a temporary untracked script, and the resulting product screenshots and
profile remain local-only.

At 1180×820 with the sidebar and summary closed, the reverse-origin viewport
is 1180×773px at y=47. Selecting message 15 materializes 12 turns at
`scrollTop = -2394` and `scrollHeight = 4687`. The 30-button navigation rail
is 36×300px at x=16/y=283.5, and its current marker follows the viewport at
message 11 rather than preserving the previous 26.820 stale message-30 state.
The Composer remains 736×98px at x=222/y=706 and the 32px floating control is
centered at x=574/y=650.

At 720×680 the viewport is 720×633px at y=47, the rail is absent, nine turns
remain mounted at `scrollTop = -968`, and the Composer/floating control are
688×98px at x=16/y=566 and 32px at x=344/y=510. Returning from history is a
two-stage transition in both widths: the first click lands at `-402` while
the thread is still not following, and the second reaches zero with eight
turns mounted and the floating control at opacity zero.

Browser/CDP gates both static frames, the viewport-derived current marker,
exact geometry, mounted counts, reverse scroll values, and both return clicks.
Electron repeats message 29 → message 15 navigation and the two-stage return
in native wide and compact windows. Two reviewed baselines pass. Optional
local-only structural product comparisons pass at 1.5731% wide and 1.3954%
compact under independent 2% ceilings; masks exclude virtualized transcript
text while retaining the rail, header, selected-bottom rhythm, floating
control, and Composer.

Cleanup archived only the disposable task, terminated the validated isolated
PID `13562` and its child tree, closed loopback port `9888`, and moved the
exact profile recoverably to
`/Users/JaminZhou/.Trash/codex-ui-kit-timeline-26-825.nkVAea-20260831-1340`.
The user-owned Codex PID `43340` remained running.

## Pull requests route lifecycle follow-up

An isolated same-build process on loopback-only port `9891` re-observed the
Pull requests route from its first loading paint through the settled empty
state at 1180×820. The route keeps the current 321.875px sidebar and divides
the remaining content into a 437.53125px list plus a 419.59375px detail
placeholder separated by a one-pixel border. The loading state exposes only
the `Pull requests` heading, a polite `Loading pull requests` status, five
56px rows, 20px circular avatars, two-line copy, and 48×12px time blocks. It
does not prematurely expose tabs or search.

The settled empty state exposes All, Reviewing, and Authored tabs; a 32px
search control; the exact current search and filter SVG paths; `No pull
requests found`; and the persistent `Select pull request to view` detail
placeholder. The public `PullRequestQueryState` therefore gains a semantic
`split-list` variant rather than changing the legacy list/detail variants.
Browser/CDP and Electron gate both states, exact split geometry, accessibility
status, and state-specific controls. Two reviewed baselines pass. Optional
local-only product comparisons pass at 0.3508% loading and 0.6348% empty for
the owned list region, and 0.1339% for the detail region.

No product screenshot, private sidebar content, profile, or temporary probe is
tracked. Cleanup terminated only isolated PID `28851`, closed port `9891`, and
moved the exact profile recoverably to
`/Users/JaminZhou/.Trash/codex-ui-kit-route-26-825.9bCJ9H-20260831`. The
user-owned Codex PID `13503` remained running.

## Plan progress current-build revalidation

A later isolated same-build process on loopback-only port `9888` tested Plan
progress without modifying workspace files. Simple ordinary-mode prompts and a
simple Plan-mode prompt settled directly without exposing progress. A complex
ordinary-mode read-only audit over seven public workspace files did trigger a
real eight-step plan and completed every step while keeping at most one item in
progress.

At Step 5/8 the Composer-dock chip measures 106.671875×38px at
x=698.09375/y=660 in the 1180×820 frame. Hover opens a 95.578125×200px card at
x=704.4375/y=456 with eight 16px rows, 24px cadence, 8px padding, and 8px gap.
After the final step, both chip and card disappear and the thread settles to
the exact expected answer with zero horizontal overflow. The existing replay
matches the current-product structure at 3.5294%, below its 4.5% ceiling.

The sampled product now renders the chip and expanded list with nonsemantic
`div`/`span` containers and no tooltip role. The public replay deliberately
keeps its accessible button trigger and `role=tooltip`; geometry and pixels are
matched without adopting that accessibility regression.

Cleanup archived only the four disposable probe tasks, terminated the validated
isolated PID `56011`, closed loopback port `9888`, and moved the exact profile
recoverably to
`/Users/JaminZhou/.Trash/codex-ui-kit-plan-26-825.3BENJv-20260831-1410`.
The user-owned Codex PID `43340` remained running.

## Pure-reasoning current-build revalidation

A later isolated same-build process on loopback-only port `9892` ran no-tool
free-form reasoning prompts at 1180×820. The sampled lifecycle exposes the
14/21px weight-400 `Thinking` placeholder, replaces it with the generated
`Defining evidence categories and priorities` cadence label, and settles to a
`Worked for 14s` row plus final answer without any command or MCP activity.

The public replay keeps the running summary in the existing polite status
surface, preserves the 75%-foreground shimmer highlight, and restores the
settled Composer after the answer. CDP and native Electron gate the running
summary and completion independently, including exact 1180×820 geometry, zero
tool rows, Stop presence/removal, and the 14-second duration. Two reviewed
internal baselines cover the same frames. The 300×25px current-product
foreground crop is pixel-identical at 0% difference; the wider
prompt/activity/summary region passes at 3.3389% under a 4.5% ceiling. This
promotes the sampled current Browser/Electron contract while unsampled
reasoning variants keep the combined implementation partial.

Cleanup terminated only the validated isolated PID `13191`, closed port
`9892`, and moved the exact profile recoverably to
`/Users/JaminZhou/.Trash/codex-ui-kit-generic-reasoning-26-825-20260901-0130`.
The user-owned Codex PID `13503` remained running.

## Thread overflow actions follow-up

An isolated same-build process reached the `Chat actions` trigger on one
existing completed task at 1180×820 and 720×680. The trigger is a transparent
28×28 Renderer button at y=9 with 10px radius and 4px padding. Clicking it
mounts no `[role="menu"]` in the Renderer; the visible surface is a native
macOS menu with ten root actions, three separators, Copy/Fork/Open-in submenu
indicators, and shortcuts for Pin, Rename, Archive, and New side chat.

The public `ThreadOverflowMenu` keeps that grouping and supplies controlled
callbacks plus host-owned icons and submenu contents. Browser/CDP and native
Electron cover the accessible root, wide/compact geometry, ArrowDown and
ArrowRight traversal, submenu-only Escape, root focus restoration, and a
callback-only Pin fixture. Two reviewed raster baselines pass. A 244×279
local-only native crop compared without masks at threshold 0.1 passes at
9.0281% wide and 8.9650% compact under a 9.5% ceiling. The difference budget
accounts for native AppKit text/icon rasterization and material blur rather
than omitted elements.

The guarded capture helper selects the task only by title SHA-256, verifies the
promoted fingerprint and exact loopback/profile owner, opens and dismisses the
native menu, and never selects an action. Sanitized evidence is retained in
`research/current-thread-overflow-26-825.json`; raw desktop frames, task
content, native screenshots, and profile data remain untracked.

Only isolated PID `24607` and its validated profile-owned processes were
stopped. Loopback port `9931` closed, and the exact profile plus raw OS frames
were moved recoverably to
`/Users/JaminZhou/.Trash/codex-ui-kit-thread-overflow-26-825-20260902-9931`
and its `-evidence` sibling. User-owned Codex PID `13503` remained running.

## Skill detail and unsent Try now entry follow-up

A separate isolated same-build Renderer captured an installed Skill detail at
1180×820 and 720×680. Its modal is exactly 600px wide, 720px high in the wide
viewport, and full-height 680px in the compact viewport. The fixed contract
includes the 40px artwork slot, current title/suffix typography, enabled
switch, More and Close controls, 558px instruction frame, bottom scroll,
73.921875px Uninstall and 88.75px Try now actions, and the 121.6875×93.6875px
Open/Reveal in Finder/Copy Markdown menu. Escape dismisses the menu first and
the dialog second.

Try now was allowed only through the unsent draft boundary. It produced a
contenteditable 664×44px Composer textbox at 720×680 with the current inline
skill mention and enabled Send action; Send was never clicked. The public
`SkillDetailDialog` and `SkillPromptMention` keep artwork, instructions, file
actions, persistence, uninstall, submission, and execution host-owned. Five
Browser/CDP, five Electron, and five reviewed pixel scenes pass. Ten regional
product comparisons range from 0.0040% to 0.8616% under the 8% ceiling. The
sanitized accepted record is `research/current-skill-detail-26-825.json`; raw
screenshots and profile/account context remain untracked.

Cleanup terminated only validated isolated main PID `75875` and its owned
children, closed loopback port `9925`, and moved the exact temporary profile
recoverably to
`/Users/JaminZhou/.Trash/codex-ui-kit-skill-detail-26-825-20260901-9925`.
The user-owned Codex PID `13503` remained running.

## MCP Settings manager follow-up

A separate same-build Renderer opened Settings → Plugins → MCPs at 1180×820
and 720×680. The capture was read-only with respect to MCP configuration: it
navigated list, empty-search, Add menu, STDIO/HTTP create forms, and one update
detail but did not save, toggle, or uninstall a server. Dynamic server names,
profile data, and raw screenshots remain local-only.

- The five tabs expose Plugins 13, Apps 6, MCPs 6, Skills 2, and Marketplace
  2 with exact 28px controls. Wide search is 224×32px; compact search is
  unmounted. The sampled list contains four 52px standalone rows and two
  42.5625px plugin-provided rows.
- The wide cards are 768×210px and 768×87.125px at y=271.796875 and
  y=547.796875. The 720px cards are 358.125px wide at y=311.796875 and
  y=587.796875, with zero document overflow.
- Create exposes Name/Type plus either the five STDIO protocol groups or the
  four HTTP groups. The current Back control is 69.765625×28px. Compact HTTP
  moves Back, heading, and the two cards to y=265.796875, 313.796875,
  387.390625, and 528.390625 without changing their 135/414px heights.
- Update exposes Uninstall, the current 13/18.5625px explanatory copy, URL,
  bearer-token variable, and both header groups. Its protocol card starts at
  y=346.359375 and remains 414px tall.
- Browser/CDP verifies six deterministic lifecycle frames and interactions;
  native Electron repeats four representative states. Six reviewed baselines
  pass. The six local-only owned-main comparisons range from 1.3674% to
  3.8928% under a 4% limit after masking only content-dependent names and the
  sampled real URL.

The sanitized record is `research/current-mcp-settings-26-825.json`. Real
save/toggle/uninstall persistence, OAuth or permission prompts, and validation
or transport failures remain explicit follow-up boundaries.

## Scheduled tasks and manual editor follow-up

An isolated same-build process on loopback-only port `9922` reached the current
Scheduled route without touching user-owned Codex PID `13503`. The sanitized
capture records only fixed labels, counts, geometry, styles, and the three
built-in suggestion templates; existing task, chat, sidebar, prompt, and
project content is excluded.

- Wide 1180×820 uses a 321.875px sidebar and a centered 768px frame; compact
  720×820 hides the sidebar. Heading, description, 728/679px search field,
  four filters, two content-redacted task rows, three suggestions, and zero
  horizontal overflow were observed.
- Active, Paused, Completed, and a synthetic no-match search were exercised
  read-only. Empty states use `No scheduled tasks found`.
- The Pause control and all three built-in suggestion glyphs reuse the exact
  visible current-renderer SVG paths captured from this route; their path-set
  hashes are retained in the sanitized record.
- The Create split menu exposes `Create with Codex` and `Set up manually`.
  Create-with-Codex was observed only through its unsent fixed prefill.
- The manual editor uses 437.125/420px wide columns and 374/346px compact
  columns. Runs on, Runs in, Repeat, At, and Notifications menus were sampled;
  the content-bearing Chat menu was deliberately not opened. No form was
  submitted and no existing task was mutated.
- `ScheduledTasksPage`, filter tabs, navigator, Create menu, and controlled
  editor now expose protocol-neutral host callbacks. Browser/CDP, Electron,
  unavailable/retry, and four reviewed pixel baselines gate the slice. Four
  local-only product main-region comparisons pass from 1.2120% to 2.1933%.

The sanitized evidence is `research/current-automations-26-825.json`. Raw
screenshots and the isolated profile remain local-only and are not tracked.

## Plugins and Skills index follow-up

A final isolated process on loopback port `9920` reached Plugins and Skills at
1180×820 and 720×820 without touching the user-owned Codex process. Both
routes share a top Plugins/Skills tablist, 28/33.6px page title, 728px bounded
search field, 28px body gap, responsive two-column groups, and no horizontal
overflow. Plugins exposes Installed, Public, and Personal groups; Skills
exposes Installed, Personal, System, Project, and Recommended groups.

The public `IntegrationCatalogPage` reproduces the controlled index behavior
while keeping catalog discovery, installation, permissions, persistence, and
exact third-party artwork host-owned. Browser/CDP covers search, scope,
actions, tab routing, and unavailable-to-retry recovery; native Electron
repeats the route and interaction contract. Four reviewed baselines cover both
widths. Optional local-only full-main comparisons pass at 4.4911% and 5.1784%
for Plugins and 3.3067% and 3.8461% for Skills, including the intentional
third-party-icon difference. The sanitized accepted record is
`research/current-integrations-26-825.json`.

A separate successful Browser activity could not expose a current mounted
Browser workspace route in this process. The older workspace sample remains
legacy evidence and is not promoted by the activity-only result.

Cleanup terminated only the validated isolated PID `86298` and its two
profile-owned Crashpad handlers, closed port `9920`, and moved the exact
profile plus local-only product evidence recoverably to
`/Users/JaminZhou/.Trash/codex-ui-kit-integrations-26-825-20260901-9920`.
The user-owned Codex PID `13503` remained running.

## Plugin detail follow-up

A later isolated process reached one installed and one discovery Plugin detail
route at 1180×820 and 720×680. The fixed detail contract uses a 129.59375px
header, 58px artwork, 20/26.6px medium title, 28px actions, three prompt
suggestions, responsive Apps rows, seven Information rows, a disclosure, and
zero horizontal overflow. Installed content measures 1178px wide and 1242px
compact; discovery measures 1096px wide and 1137px compact. Exact bottom
scroll positions are 404px and 322px.

The installed action menu is 180×36.5625 with Uninstall. Its connected-app
menu is 160×65.125 with Reconnect and Disconnect. Capture only opened and
dismissed these menus; it did not trigger clipboard or Try-now navigation,
install/uninstall, connection persistence, OAuth/permissions, external links,
or failure behavior.

The public `PluginDetailPage` and `PluginDetailBreadcrumb` reproduce the
controlled structure while leaving artwork and all effects host-owned. Eight
Browser/CDP scenes, four Electron representatives, and eight reviewed replay
baselines pass. Local-only product crops cover headers, each suggestion row,
Apps, menus, and bottom content below an 8% ceiling. The sanitized accepted
record is `research/current-plugin-detail-26-825.json`; product screenshots
and profile data remain untracked.

Cleanup terminated only isolated PID `88986` and its validated profile-owned
processes, closed port `9924`, and moved the exact profile plus local-only
evidence recoverably to
`/Users/JaminZhou/.Trash/codex-ui-kit-plugin-detail-26-825-20260901-9924`.
The user-owned Codex PID `13503` remained running.

## Terminal lifecycle current-build refresh

An isolated same-build process on loopback-only port `9893` re-observed the
bottom Terminal at 1180×820 and 720×820. The current outer track is 280px; its
one-pixel separator precedes a 279px panel with a 40px header and 239px content
region. The 1180px layout uses the current 321.875px sidebar and starts the
panel at x=322.875/width=857.125. At 720px the default hidden-sidebar panel is
x=1/width=719, while explicit Show produces the current
320.265625/399.734375 split.

Three 156×28px project-labelled tabs advance by 163px and use the current
Terminal/close assets. The 280×122.25px picker exposes Review, Terminal,
Browser, and Files. The bounded probe verified close-nearest selection, label
reindexing, close-all collapse, fresh Toggle creation, a real command surviving
hide/reopen and settling to stdout, plus the exact cross-worktree warning. Open
new terminal preserves the older `codex-ui-kit 1` tab and selects `/ 2`;
Dismiss removes only the warning.

CDP gates accessibility, interaction, computed geometry, and paint; Electron
repeats the native lifecycle. Nine reviewed scenes cover wide, compact,
picker, process, mismatch, and closed states. Local-only product comparisons
pass at 0.9064%/0.3706% wide, 1.1498%/0.5602% compact-hidden, and
2.1375%/1.1768% compact-pinned for panel/content under 2.5%/1.5% ceilings. No
product screenshot or private sidebar content is committed.

Cleanup terminated only isolated PID `11830` and its descendants, closed port
`9893`, and moved the exact profile/evidence recoverably to
`/Users/JaminZhou/.Trash/codex-ui-kit-terminal-26-825-20260901-0315`. The
user-owned Codex PID `13503` remained running.

## External-file approval lifecycle refresh

A later isolated same-build process on loopback-only port `9894` created three
disposable tasks under Ask for approval: one held pending, one denied, and one
allowed once. It did not navigate or resize the user-owned Codex process. The
capture helper is read-only after task setup and fails closed unless the exact
installed fingerprint, isolated profile, loopback listener, title hashes, and
new output directory all match.

- The current question is `Allow ChatGPT to edit the following file?`, under
  `Edit files`; the previous `Would you like…` copy is absent.
- The card is 736×177px at x=222/y=627 wide and 688×177px at x=16/y=487
  compact. It keeps a 25px radius, 16px inset, 13/20px identity, 14/20px
  question, and 14/21px path typography.
- The file preview separates the muted directory from the weight-500 file name
  and displays +1/-0 at 16/16px in `rgb(64, 201, 119)` and
  `rgb(250, 66, 62)`.
- The compact split menu is 168×67.125px with `Allow once` and `Allow all
  edits`; the previous build's `Allow this conversation` label is absent.
- Deny leaves the exact target absent and settles as `Rejected 1 file`,
  `Worked for 10s`, and the exact sanitized denial. Allow once creates the
  requested bytes and settles as `Edited 1 file`, `Worked for 13s`, Review,
  and the exact sanitized answer.
- Browser/CDP gates six wide/compact pending, options, denied, allow-pending,
  and allowed frames. Electron repeats both decisions in native windows, and
  six reviewed baselines cover the same lifecycle.
- Unmasked current-product comparisons pass at 3.1787% wide pending, 3.3225%
  compact pending, and 1.3415% compact options under independent 6.5%, 6.5%,
  and 5.5% ceilings. Geometry, semantics, focus, and transition state are
  independently hard-gated.

Cleanup verified that pending and denied targets never existed and that the
allowed target matched the expected bytes. All three disposable tasks were
archived; the created file and exact isolated profile moved recoverably to
`/Users/JaminZhou/.Trash/codex-ui-kit-approval-26-825-68531-20260901`.
Exact isolated PID `68531` and its profile-owned processes stopped, port `9894`
closed, and the user-owned Codex PID `13503` remained running. Product
screenshots, task identifiers, and profile contents remain untracked.

## Transport recovery refresh

One further isolated same-build process used a unique Chromium profile and a
per-process loopback-only HTTP CONNECT proxy. The proxy accepted only port 443,
required the explicit `CODEX_CURRENT_TRANSPORT_PROXY_ALLOW_DROP=1` opt-in, and
could stop/restore only its own tunnels with `SIGUSR1`/`SIGUSR2`. The primary
user-owned Codex PID `13503` was never routed through the proxy.

An active no-tool response first exposed the persisted standard
`Reconnecting 1/5` row. Holding only the isolated proxy offline then emitted
`Reconnecting... waiting for network` at approximately 3.1, 10.9, 24.1, 47.1,
90.2, and 153.3 seconds. This proves at least six open-ended network waits; the
current path does not terminal-fail after the standard five-attempt label. The
waiting row uses 14/21px typography, weight 400, 60%-foreground color, a 16×16
four-path reconnect glyph, a 6px icon/label gap, and 37px repeated-row top
spacing. Wide x=222 and compact x=16 content anchors remain overflow-free.

Restoring the proxy allowed the original turn to complete. Every transient
network-wait row cleared, the standard retry history remained, Stop changed to
Send, and an exact no-tool follow-up in the same task returned
`CURRENT 26.825 TRANSPORT RECOVERY ACCEPTED`. The public trace models all six
waits, selective successful-turn cleanup, recovered completion, and follow-up.
Browser/CDP and native Electron gate six wide/compact lifecycle frames; six
reviewed internal baselines cover the same states. The optional local-only
1180×820 product comparison crops only the owned waiting row and passes at
3.3371% under a 4% ceiling.

The exact disposable task was archived. The isolated app, App Server, proxy,
and profile-owned Crashpad handlers stopped; loopback CDP/proxy ports closed;
the exact profile and product screenshots moved recoverably to Trash. No raw
product screenshot, task identifier, profile data, or dynamic navigation text
is committed. This promotes the sampled recovery path while terminal transport
failure and other retry causes remain partial.

## Subagent delegation and collaboration refresh

One later isolated same-build process on loopback-only port `9905` created
three deterministic no-tool tasks: one Verifier, concurrent Alpha/Beta, and a
nested Parent that delegated Child. The current UI differs materially from the
previous 26.730 presentation:

- active root work is one inline avatar-chip group; settled root chips are
  removed, leaving only the turn duration and final answer;
- the nested root timeline shows Parent only. Opening Parent reveals Child's
  chip and thinking state, and Child can be opened in the same transcript
  panel;
- the global `Subagents` panel is flat and preview-free, with `Active · n`,
  `No active subagents`, and `Done · n` sections plus 14/24px names and
  12/16px relative times;
- the side panel is 419.59375px at 1180×820, remains 319px in the existing
  820×680 continuity state, and becomes a 345.671875px overlay beginning at
  x=374.328125 after the sidebar hides at 720×680;
- Verifier, Alpha, Beta, Parent, and Child use five distinct 16×16 rendered
  SVG avatars. The normalized copies carry explicit OpenAI provenance and are
  excluded from the repository's MIT relicensing; no `app.asar` source was
  used.

The public traces retain the exact fixed prompts and sanitized outputs.
Twenty-one focused Browser/CDP frames gate single, concurrent, nested,
recovery, panel, transcript, and compact state; Electron repeats list →
transcript → back navigation and native wide/820/720 resizing. Reviewed visual
baselines cover the same deterministic frames. The broader twelve-agent
waiting/failure/interruption matrix remains protocol-backed regression
coverage rather than a claim that every terminal transition was freshly
runtime-reached.

All three exact disposable tasks were archived. Only isolated PID `31437` and
its profile-owned processes were stopped, port `9905` closed, and the profile
plus screenshots moved recoverably to
`/Users/JaminZhou/.Trash/codex-ui-kit-subagent-26-825-20260901`. The user-owned
Codex PID `13503` remained running. Product screenshots, task identifiers,
private labels, and profile data remain untracked.

## Global success notification refresh

A later same-build process on loopback-only port `9914` created four safe,
no-tool disposable tasks. The probe first reached one genuine `Chat unpinned`
success notification through Pin chat → `⌘Z`, then pinned all four tasks and
issued four rapid `⌘Z` operations so the complete simultaneous stack could be
measured before its duration elapsed. Every task returned to its initial
unpinned state.

- The Sonner region remains top-center at y=48 with the polite
  `Notifications alt+T` section, `aria-relevant="additions text"`,
  `aria-atomic="false"`, an 8px gap, and Light Sonner mechanics carrying the
  Dark success alert tokens.
- The front alert is 170.4375×42px with 15px radius, 8px padding, 14/21px
  weight-400 system type, `#011c0b` background, `#40c977` foreground, and the
  same exact two-path success and one-path Close glyphs.
- Four notifications stay mounted. The collapsed stack exposes the first
  three and uses widths 170.4375/161.9156/153.3938/144.8718px, tops
  48/57.05/66.10/75.15px, scales 1/.95/.90/.85, and translations
  0/8/16/24px. The fourth has opacity 0 and no pointer events. Hover expands
  the visible three to approximately 50px intervals while the fourth remains
  noninteractive.
- `capture:current-notifications` now verifies the installed fingerprint,
  loopback listener ownership, isolated profile, four unique disposable-title
  hashes, reversible Pin/Undo state, DOM/live-region semantics, computed
  geometry/styles, exact icon hashes, and two notification-only screenshots.
  It never submits prompts or archives tasks. The accepted local record SHA is
  `e6c5fe0d412b14c8e5e930ce94c70daf747ea93d894e9bd39b55dbf5a4b23215`.
- Existing `shell-notification-queue` and
  `shell-notification-success-stack` Browser/CDP, native Electron, and pixel
  gates already reproduce this exact sampled contract, so the global
  notification row is current-build verified for success and bounded stacking.
  Info, warning, danger, and other product triggers remain source-backed replay
  rather than current runtime claims.

The capture requires four already-completed disposable tasks and explicit
authorization:

```sh
CODEX_CURRENT_NOTIFICATIONS_CDP_PORT=<isolated-port> \
CODEX_CURRENT_NOTIFICATIONS_PROFILE=<isolated-profile> \
CODEX_CURRENT_NOTIFICATIONS_OUTPUT_DIR=<new-profile-child> \
CODEX_CURRENT_NOTIFICATIONS_TASK_TITLE_SHA256S=<four-comma-separated-hashes> \
CODEX_CURRENT_NOTIFICATIONS_ALLOW_CAPTURE=1 \
pnpm capture:current-notifications
```

All four exact disposable tasks were archived. Isolated PID `20272` stopped,
port `9914` closed, and its profile plus local-only regional evidence moved
recoverably to
`/Users/JaminZhou/.Trash/codex-ui-kit-notifications-26-825-20260901-0857`.
The user-owned Codex PID `13503` remained running. Product screenshots, task
identifiers, titles, and profile contents remain untracked.

## App Server fatal recovery follow-up

A final isolated same-build process on loopback-only port `9916` exercised the
fatal App Server boundary without touching user-owned Codex PID `13503`. The
capture helper required the exact direct-child PID plus an explicit mutation
opt-in before sending `SIGTERM`; it retained no task, project, profile, or
navigation labels.

The current recovery Renderer is now a full-window `#141414` composition rather
than the former small error card. At 1180×820 its 896px content frame contains
a 160×160 illustration at x=502/y=189.40625, a 345.625×105.1875 copy block at
x=417.1875/y=357.40625, 28/39.2px weight-600 heading, and two 14/21px
weight-400 paragraphs. `Restart ChatGPT` is 150.546875×36px and `Send
feedback` is 138.796875×36px with a 12px gap. The same composition moves to
y=119.40625 / 287.40625 at 720×680 without horizontal overflow.

The captured transition is three-phase: App Server PID `58305` was terminated,
PID `59487` replaced it while the fatal page remained visible, and clicking
Restart replaced that process with PID `59518` before the full shell returned.
The fatal page appeared after 82ms and the shell returned 364ms after Restart
in this sample. The installed product illustration remains local-only: its
1358×1159 PNG identity is retained only as byte count and SHA-256
`c9aab30680332edfff4fa8bcd90bec879a6809665dc06a83e110623f8a277768`.

`AppServerCrashRecovery` now reproduces the current copy, layout, typography,
buttons, responsive geometry, and restart contract while exposing a
host-supplied illustration slot. Its public fallback artwork is independently
drawn. Browser/CDP and native Electron cover wide, compact, and restoration;
reviewed replay baselines cover both viewport states. The optional local-only
product comparison masks only the 160×160 proprietary illustration and passes
at 0.0978% full-frame and 1.3754% for the text/button core. The accepted record
SHA is `46c398bc3f8dbca5212deab48bc4aecb79c63a2a847d8a1fc498f42dae4fefcd`.

Isolated PID `56171` stopped, port `9916` closed, and the profile plus local
screenshots moved recoverably to
`/Users/JaminZhou/.Trash/codex-ui-kit-app-server-crash-26-825-20260901-formal`.
No product raster, screenshot, profile data, or private transport detail is
tracked.

## Mixed image/file attachment lifecycle follow-up

An isolated same-build process on loopback port `9918` used trusted CDP drag
events with the synthetic `probe.png` and `probe.txt` fixtures. The image is
byte-identical to the already reviewed public `attachment-current-ready.png`
baseline (SHA-256 `181a65d87617df8be67a814d42d8bf07b2aa388d9155ad94588be6c29a601666`);
the text fixture SHA-256 is
`e9c02334af96f73869964dcf40292a3dc176b35df87e56ec4ad8fdd47c1624bd`.

- Wide/compact post-picker Composers are 736×154px and 688×154px. Their 70px
  trays retain a 54×54 image, 156.484375×52.5 text card, exact document and
  Remove glyphs, 14/21px title, 13/19.5px metadata, and zero overflow.
- The immersive preview now always exposes Edit, Download, and Close in this
  sampled feature state. Initial focus is Edit. Compact fits the image at 56%
  and one 1.2× step displays 67%; wide fits at 82%. Escape returns focus to the
  image card.
- Removing the text card focuses the remaining image; final removal empties the
  tray and restores Composer focus. The fixed no-tool prompt completes with
  `CURRENT ATTACHMENT SUCCESS 26.825.`; two sent attachments remain outside
  the user bubble and the empty Composer restores to 98px.
- Browser/CDP and native Electron repeat the state machine. Six reviewed
  wide/compact baselines and six local-only product-region comparisons gate
  layout and pixels, including the completed transcript and wide-only
  Outputs/Sources summary. The sanitized record is
  `research/current-attachment-26-825.json`.

The disposable task was archived. Isolated PID `2328` stopped, port `9918`
closed, and the profile plus local screenshots moved recoverably to
`/Users/JaminZhou/.Trash/codex-ui-kit-attachment-26-825-20260901-9918`.
The user-owned Codex PID `13503` remained running.

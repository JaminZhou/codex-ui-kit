# Codex Desktop 26.825.51511 scoped runtime baseline

This note records the application-shell surfaces re-observed against the
installed Codex Desktop build on 2026-08-30. Evidence sampled only on
26.825.31414 or an earlier build remains previous-build regression evidence
until that surface is probed again.

## Fingerprint

- App version: `26.825.51511`
- Build number: `7377`
- Chromium: `151.0.7922.174`
- `app.asar` size: `284032150` bytes
- `app.asar` SHA-256:
  `f56ac8d5254a10fc4a04e7417fa787d135c3bbca49bad7d668d4ae65833d40c7`
- Installed resource:
  `/Applications/ChatGPT.app/Contents/Resources/app.asar`

The capture hashed the installed archive before and after sampling, retained
its device and inode identity, and proved that the isolated Renderer started
after the sampled bundle metadata settled.

## Capture boundary

One user-authorized second process used loopback-only CDP port `9897` and a
unique profile under `/private/tmp`. Existing Codex windows were not navigated
or resized. Main Renderer selection used the application URL, viewport area,
shell landmarks, and visible-control density. The capture contains only the
package identity, sanitized fixed labels, geometry, styles, focus state, and
content-dependent counts. Screenshots and private account, project,
repository, and task text remain local-only.

The reproducible capture is:

```sh
CODEX_CURRENT_BASELINE_CDP_PORT=9897 \
CODEX_CURRENT_BASELINE_PROFILE=/absolute/path/to/the/isolated/profile \
CODEX_CURRENT_BASELINE_ALLOW_NAVIGATION=1 \
CODEX_CURRENT_BASELINE_OUTPUT=/absolute/path/to/the/isolated/profile/current-baseline.json \
pnpm capture:current-baseline
```

## Re-observed shell contract

- New chat remains reachable at 1180×820, 820×680, 721×680, and 720×680.
  The persisted sidebar is 321.875px wide, the Composer editor is 44px tall,
  and every sampled state has zero horizontal overflow.
- The sidebar remains visible at 720px until explicitly hidden. Hide removes
  Sidebar, Back, and Forward controls; Show restores navigation and the compact
  Pull requests → New chat round trip.
- The fixed route stack is New chat, Pull requests, Scheduled, Plugins, and
  Explore. `Sites` remains absent from the sampled fixed-route stack.
- Project-group rows remain 30px tall and preserve pointer, Enter, Space,
  focus, resize, and Hide/Show continuity. Counts and scroll height are
  content-dependent.
- Help remains one 320×272.06 DOM menu with eight items, menu focus, Escape
  dismissal, and trigger-focus return.
- Projects remains 736px wide at 1180×820 with 512/64/128 columns and 70px
  rows. Its explicitly collapsed 600×600 state remains 559px wide with
  415/128 columns. Sort, empty search, expansion, focus continuity, and one
  owned scroller were re-observed.

## Account and Settings follow-up

The loopback-only isolated process on port `9899` re-opened the Account menu
and the General and Appearance Settings routes. The account capture changed
only the isolated UI preference through Light and Dark, restored System, and
returned to New chat. The user-owned Codex process was not navigated or
resized. Screenshots and account content remain local-only.

- Light/Dark at both 1180×820 and 720×680 retain the 321.875px sidebar and a
  305.875×188.375px six-item Account menu at x=9. Wide top is 587px and
  compact top is 447px; each state has weight-400 items, zero horizontal
  overflow, Escape dismissal, and trigger-focus return.
- Settings uses the same 321.875px rail. Twenty-two visible navigation rows
  are 305.875×29px, the content column is 768px at x=366.9375, and the sampled
  1180×820 routes have zero document-level horizontal overflow.
- General re-observes the current permission, Composer, editor, panel,
  menu-bar, sleep, keyboard, popout, and notification controls. This capture
  records reachability and geometry without changing their values.
- Appearance retains three 248×199.609375px System/Light/Dark previews plus
  the current code-theme, accent, background/ink, UI/code-font, translucency,
  contrast, cursor, Dock-icon, font-size, diff-marker, and smoothing controls.
  System was verified selected after the capture.
- The public General, Appearance, Account-menu, and Settings-shell fixtures
  already have Browser/CDP, Electron, and reviewed visual coverage. This
  same-build probe promotes those sampled groups from previous-build-only to
  current-build verification; other Settings pages remain separate evidence.

## Completed basic conversation follow-up

The same isolated process submitted one safe, no-tool task with the exact
prompt `Reply with exactly CURRENT BASIC MESSAGE.` and naturally received the
exact no-period response `CURRENT BASIC MESSAGE`. The capture revalidated the
installed archive before extracting only fixed labels, geometry, computed
styles, and reusable SVGs. Private sidebar labels and raw screenshots remain
local-only.

- At 1180×820 the sidebar is 321.875px and the main surface is 858.125px. The
  736px user/assistant lane begins at x=383.4375; the user bubble is
  334.65625×42.75px with a 70% maximum, 22px radius, `10px 16px` padding,
  and 14/22.75px weight-400 type.
- The flat assistant response begins at y=167.75. Copy, Good response, Bad
  response, and Fork chat from here occupy four 26×26px targets at 28px
  intervals, using the exact current SVG sources.
- The 736×98px Composer at y=706 retains a 712×44px editor and the five
  observed Add, permission, model, Dictate, and Send targets. At 721×680 the
  sidebar remains open and the lane/Composer shrink to 366.125px; at
  720×680 the sidebar collapses and both expand to 687px with zero overflow.
- The 47px header now reproduces the separate 28px project-icon hit area,
  title button, Chat actions, Share, summary, bottom-panel, and side-panel
  controls. Header positions and the 721→720 responsive boundary are gated in
  both Browser/CDP and Electron native windows.
- Three reviewed full-frame baselines cover wide, boundary-open, and compact.
  Local-only product comparisons split ownership into header, thread, and
  Composer rather than allowing the dark background to dilute differences.
  The measured ratios are 4.7240%/0.4016%/0.8072% wide,
  6.1697%/0.4733%/0.6858% at 721px, and
  6.2470%/0.0621%/0.8006% at 720px, under independent 7%/0.6%/1% limits.
- The current semantic asset inventory grows to 118 icons and adds the
  observed Explore, Share, window-sidebar, Composer Send, and completed-turn
  response actions. The scoped current-thread baseline does not overwrite
  historical scenario-specific raster evidence.

## Sidebar thread lifecycle follow-up

A second isolated process on loopback-only port `9898` submitted two safe,
no-tool plain-text tasks. One task was observed while selected and running,
then in the background, and after natural completion; the earlier completed
background task supplied a genuine unread state. The user-owned Codex process
was not resized or navigated.

- At both 1180×820 and 720×680, the visible navigation remains 321.875px wide.
  Thread rows are 305.875×30px at x=8, with a 12.5px radius,
  `5px 5px 5px 8px` padding, and 13/18.5714px weight-400 system type.
- A selected running row uses the 8% foreground background and the existing
  20×20 trailing rail with the exact 16×16 two-path spinner. Completion while
  in the background replaces it with an 8×8 `#3a83f7` info-solid dot centered
  in the same rail. The capture explicitly verified that Stop was absent
  before classifying the latter as complete.
- Hover replaces the status rail with exact `Pin chat` and `Archive chat`
  controls. Each is 19×20px, the SVGs render at 16×16, the gap is 8px, and the
  action rail ends 8px from the row edge.
- The public replay exposes separate selected-running, background-unread,
  hover-action, idle, and compact fixtures. Browser/CDP and Electron gate the
  computed geometry, styles, names, status replacement, and zero overflow;
  three reviewed frames cover wide, hover, and compact presentation.
- Local-only current-product comparisons reduce dynamic text to owned shapes:
  action glyphs differ by 2.9762%, the active spinner by 0.3571%, and the
  unread dot by 0%. Raw screenshots, task identifiers, and titles remain
  outside the repository.

This promotes the sampled ordinary thread-history, item-action, and status
groups to current-build verification. Waiting, error, worktree, long-list,
archive/pin mutations, and accessibility behavior not reached by this probe
remain broader or previous-build evidence.

## Sidebar worktree lifecycle follow-up

A further isolated process on loopback-only port `9898` opened a disposable
Git repository through the public application path. Real worktree creation,
a controlled `.git/worktrees` blocker, Retry, response completion, and
restored idle were observed without navigating or resizing the user-owned
Codex process.

- Selected setting-up uses a 305.875×30px row, 14×14 branch at a 39px right
  inset, and the exact two-path 16×16 spinner in a 20×20 rail.
- Current failure is branch plus the exact three-path 16×16 error glyph at an
  8px rail inset and `rgb(255, 103, 100)`. The selected/read sample has no
  unread marker; a later background failure carries error and unread together,
  so notification/read state is independent from worktree failure.
- After removing the controlled blocker and activating Retry, the row returns
  through setting-up. A naturally completed recovered response produces the
  8×8 `#3a83f7` unread dot; reading it leaves the restored branch at an 11px
  inset.
- Hover replaces branch/status presentation with exact `Pin chat` and
  `Archive chat` actions. Browser/CDP and Electron repeat the lifecycle at
  1180×820 and 720×680 with zero horizontal overflow.
- Two reviewed replay frames cover wide and compact composition. Local-only
  84×30 active, failed, and recovered shape comparisons pass at 3.7698%, 0%,
  and 0%; CDP independently locks the SVG paths, viewBoxes, colors, and
  geometry.

The sidebar implementation remains partial because queued/creating sidebar
variants are not promoted by this slice.
Product screenshots, profiles, repository contents, task identifiers, and
raw logs remain outside the repository.

## Worktree setup failure workspace follow-up

A second isolated worktree process on loopback-only port `9898` repeated the
controlled regular-file `.git/worktrees` blocker in a disposable repository.
It reached the real expanded and collapsed failure card, Edit environment,
Retry, both creating stages, the created handoff, and a naturally completed
no-tool response. Both exact tasks were archived, both exact child worktrees
were removed, and only the isolated PID/profile/repository were cleaned; the
user-owned Codex process remained running.

- The failure root is 736×276.5px at 1180×820. Its 736×247.5px card uses a
  1px 8.2%-foreground border, 15px radius, 12px padding, and 12px vertical
  gap. Collapsing details reduces the card to 736×112px.
- `Preparing workspace` and `Checking out files` are 21px rows separated by
  8px. Completed is `rgb(44, 103, 197)` and failed is
  `rgb(255, 103, 100)`; the replay retains the exact runtime-observed
  branch, completed, failed, and disclosure SVG paths.
- The expanded log is 710×123.5px with a 12.5px radius, 1px
  15.7%-foreground border, `rgb(24, 24, 24)` fill, and 13/19.5px weight-500
  monospace text. Repository identifiers and commit IDs are sanitized in the
  public fixture.
- Retry first shows `Creating a worktree`, in-progress Preparing plus pending
  Checkout, then completed Preparing plus in-progress Checkout. The card is
  removed for `Worktree created` and `Starting a task`. Edit environment
  routes to the current unavailable local-environment Settings state.
- At 720×680 the failure root is 366.125px wide and retains all controls with
  zero horizontal overflow. Browser/CDP verifies five frames and the complete
  disclosure/Retry transition; Electron repeats Retry, Cancel, and Edit
  environment. Five reviewed baselines cover expanded, collapsed, creating,
  created, and compact states.
- A local-only 736×277 foreground comparison masks the content-dependent log
  text and differs by 5.2082%, below the independent 6.5% limit. The product
  screenshot, profile, repository, task identifiers, and raw log remain
  outside the repository.

This promotes `workspace.worktrees` to current-build Browser/Electron evidence
for the sampled failure/recovery workspace. Worktree Settings retention,
permanent worktrees, missing-directory variants, and other paths remain
partial.

## Worktrees Settings follow-up

A later isolated process on loopback-only port `9898` opened Worktrees
Settings with a disposable repository and one temporary managed worktree. It
did not navigate or resize the user-owned Codex process. The tracked evidence
retains only fixed labels, sanitized fixture paths, geometry, styles, and
state transitions; raw screenshots and the isolated profile remain local-only.

- The current preferences are Worktree root, Always fetch upstream before
  creating worktrees, Automatically delete old worktrees, and Auto-delete
  limit. The sampled values were blank root, fetch disabled, automatic delete
  enabled, and limit 15; the user path is represented by a sanitized
  placeholder in the public replay.
- Managed worktrees are grouped under their project path. Each sampled card
  exposes Worktree, path, `Starts a fresh chat using the same files and
  branch`, New chat in this worktree, Delete, and Conversations.
- Moving the exact disposable worktree directory away and activating Refresh
  removed the card. Restoring the directory and refreshing restored it. The
  Settings route therefore reconciles missing directories by removal rather
  than showing the separate thread-level missing-directory repair flow.
- Product Delete removed the exact worktree directory and its Git registration
  immediately, with no confirmation dialog. Cleanup verified both were absent
  before moving only the isolated profile and repository recoverably to Trash;
  the main user process remained alive.
- At 1180×820 the Settings rail is 321.875px, the centered content is 768px,
  the preference card is 276.31px high, each project header is 46px, and the
  managed card is 124.58px. At 720×680 the controls remain fixed-size and the
  route keeps zero document-level horizontal overflow.
- Browser/CDP covers wide, light, 720px, empty, linked-conversation, and
  missing-refresh frames plus controlled preference/New chat/Refresh/Delete
  actions. Electron repeats the wide interactions and route persistence. Six
  reviewed baselines pass; a local-only 768×548 owned-route crop differs by
  4.5984%, below the independent 5% limit.

The public component deliberately exposes host-owned callbacks and does not
perform filesystem work itself. `Create permanent worktree` is still present
in the current native project menu, but native UI automation was unavailable
and the action was not clicked. Permanent creation, automatic-pruning timing,
and broader retention lifecycles therefore remain open.

## Project Section submenu drift

The project action boundary still begins with a frozen
`electronBridge.showContextMenu` provider, but the provider and the final
native menu are distinct layers on this build:

- the provider contains `Unpin`, `Edit`, `Section`, `Reveal in Finder`,
  `Create permanent worktree`, `Archive chats`, and `Remove project`, plus
  three separators;
- `Section` owns a submenu and has no direct `onSelect`; the sampled profile
  has no custom sections, so its submenu contains only `New section…`;
- the rendered AppKit menu filters the provider-only `Reveal in Finder` row in
  this context. Its visible six actions are `Unpin`, `Edit`, `Section`,
  `Create permanent worktree`, `Archive chats`, and `Remove project`;
- the layer-101 native main menu is exactly 252×187 and the one-row submenu is
  exactly 118×34;
- the `Section` SVG is captured from the provider as one exact 16×16 source,
  recorded with SHA-256
  `a3eb21ed39b3b2483c2803b81743cd34ff4d1b53ccd134d664177490ab983a17`.

The public replica therefore retains provider evidence separately from the
visible composition. `MenuSubmenu` now exposes an accessible submenu label and
scoped class/style/width hooks. Browser/CDP and Electron gate ArrowRight open,
first-item focus, ArrowLeft close, Escape dismissal, and focus return. Two
reviewed Electron frames cover the main and nested menu. Local-only unmasked
comparisons pass at 3.8155% for the 252×187 main menu and 6.9542% for the
text-dominant 118×34 submenu, with geometry, sampled colors, and exact icon
source gated independently.

## Rich Markdown streaming follow-up

The same installed fingerprint was rechecked before and after one isolated
no-tool task. Its fixed prompt produced a public Codex link, two task rows, a
TypeScript fence, a seven-column table, exactly 36 H2 sections, and a terminal
marker. CDP captured link-only, open-empty-fence, table, long-running,
tail-running, and completed milestones without reading project files.

- the response root remains 736px wide at 14/22.75px; H1 is 21/28px and H2 is
  17.5/24.5px with 14px top and 3.5px bottom margins;
- task controls are disabled 14×14 round `role=checkbox` buttons;
- the completed table is 736×122.75px with a 29px header, a 41.25px ordinary
  row, and a 52.5px final row;
- an open fence mounts an empty TypeScript card instead of visible placeholder
  text; the public component now preserves that empty state;
- running frames retain the 28×28 Stop control and omit response actions;
  completion removes Stop and exposes Copy, Good response, Bad response, and
  Fork chat from here;
- latest uses reverse-origin `scrollTop=0`; negative scroll-away remains
  stable while new chunks arrive and return-to-latest restores zero.

The deterministic public replay passes six Browser/CDP checkpoints, Electron
copy/table/scroll/native-window checks, and five reviewed baselines. Four
local-only product regions pass at 2.4398%, 4.7698%, 2.7219%, and 1.7196%
under an 8% ceiling. Screenshots and private sidebar labels remain outside the
repository. Citations, live media, table/Markdown errors, and plugin-provided
variants remain open.

Activity, Search/Browser, long-thread continuity, and other unsampled groups
remain 26.825.31414 or earlier evidence. This follow-up promotes only the
sampled Markdown mutation lifecycle.

## Completed command anchor follow-up

A fresh isolated task used the public terminal path to run only
`/usr/bin/uuidgen`. The generated UUID proved that the response depended on a
real command execution rather than a text-only imitation. The tracked replay
replaces that dynamic value with a same-length sanitized UUID; the real value,
task identifier, screenshots, and profile remain local-only.

- the settled product exposes `Worked for 8s`, then the expanded
  `Ran /usr/bin/uuidgen` row with no Shell card, raw output panel, exit-code
  footer, or row disclosure;
- command and duration text remain 14/21px, weight 400, at x=244.5/223.5 in
  the 1180×820 frame and x=38/17 in the 720×680 frame;
- the generated result is a flat 14/22.75px assistant message, followed by
  Copy, Good response, Bad response, and Fork chat from here;
- completion removes Stop, restores Send, and retains zero horizontal
  overflow at both widths;
- Browser/CDP gates the exact semantic, geometry, style, and hidden-protocol
  contract. Electron repeats the expanded lifecycle and native resize. Two
  reviewed baselines cover wide and compact composition;
- local-only same-build comparisons split ownership into activity, Composer,
  and header regions. Wide ratios are 2.2648%, 0.9375%, and 4.9044%; compact
  ratios are 1.9196%, 1.1022%, and 6.5485%, under independent
  2.3%/1.2%/7% ceilings.

This promotes `thread.command-execution` for the sampled current success
path. Exit-code failure, interruption, background processes, direct Terminal
tabs, and broader recovery variants retain their separately versioned P0
gates.

## Review card and workspace anchor follow-up

A fresh isolated `apply_patch` task created one file, replaced one path as a
raw delete plus add, and deleted one file. The visible card aggregates those
four protocol entries into three rows with `+5 −5`, while the Review workspace
preserves all four raw entries and both `alpha.txt` identities.

- the closed card is 736×173.5px at 1180×820 and 688×173.5px at 720×680;
- opening Review changes the subtitle to `Review changes ↗` and initially
  expands only `added.txt`;
- the Review panel is 591.828125×820 wide and 344.671875×680 compact, with the
  existing 46px header, 40px toolbar, 203px wide filter, and four diff/tree
  entries;
- the card-files and Undo glyphs are exact same-build SVG captures;
- completion exposes `Worked for 20s`, Copy, Good response, Bad response, Fork,
  and Send, removes Stop, and retains zero horizontal overflow;
- Browser/CDP and Electron gate four wide/compact frames. Four reviewed
  internal baselines pass. Local-only product ratios are 4.2901%, 4.9780%,
  and 6.4586% for the card regions, and 1.9445%/2.5733% for the wide/compact
  panel regions under separate 6.6% and 3% ceilings.

The real task id, screenshots, profile, and disposable file contents remain
local-only. The public fixture contains only sanitized paths and text. This
promotes the sampled file-card, side-panel, editor-diff, and multi-file Review
groups on the current build without claiming binary/conflict, larger-set, or
broader Undo/failure coverage.

## MCP two-turn lifecycle follow-up

A final capture-only probe used one isolated, user-authorized task and the
OpenAI Developer Docs integration. The capture script never submitted a
prompt: it selected the exact task by a title hash and recorded only fixed
labels, geometry, computed styles, reusable SVGs, and sanitized lifecycle
facts. The real task identifier, profile, raw screenshots, and dynamic
workspace counts remain local-only.

- the first turn completes Search → Fetch in 20 seconds and returns the
  fixed `Model Context Protocol` title and public documentation URL;
- the second turn stays in the same thread, records invalid-URL Fetch →
  Search → valid Fetch, and completes in 10 seconds. The failed row remains
  a flat noninteractive call row inside the integration group, with no legacy
  error card or disclosure;
- at 1180×820 the success group starts at x=222/y=303 and the recovery group
  at x=222/y=501. At 720×680 the recovery group starts at x=16/y=338, call
  labels start at x=38, and document-level horizontal overflow remains zero;
- the current pinned summary is 300×313px at x=864/y=59. It combines
  Environment and Sources, uses 272×29px rows, closes when the pinned toggle
  is cleared, stays closed after an outside click, and repins from the same
  header control;
- Browser/CDP gates four exact lifecycle frames and Electron repeats expanded
  success/recovery, summary close/repin, and native compact resize. Four
  reviewed internal baselines pass;
- local-only product comparisons differ by 2.6742% for success, 4.8070% for
  recovery, 3.8995% for compact recovery, and 2.9276% for the pinned summary,
  under independent 2.8%, 5%, 4.1%, and 3.1% limits.

Cleanup archived the exact disposable task, terminated only validated isolated
main PID `56767` and its two exact-profile Crashpad handlers, verified loopback
port `9902` and every exact-profile process were gone, and moved the profile
and raw capture files recoverably to
`/Users/JaminZhou/.Trash/codex-ui-kit-current-mcp.4ct2le-20260831`. The
user-owned Codex PID `21152` remained running.

This supersedes the sampled primary MCP and Sources anchors from 26.820 while
retaining `partial` implementation status for authentication, elicitation,
MCP approvals, cancellation, same-transport disconnect/reconnect, and other
integration variants.

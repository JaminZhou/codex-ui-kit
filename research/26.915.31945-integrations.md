# Current Plugins and Skills boundary — 26.915.31945

The isolated current-build capture used the installed `26.915.31945` / build
`9922` package (`Chromium 153.0.8010.48`, app.asar SHA-256
`1f7939c1c781887c167043c4d1d307af3400d324685cfc315dfe2f80e634f483`). The
main Codex process was preserved. CDP sampled the Settings → Plugins and
Settings → Skills index routes at the native wide viewport and at an emulated
720×680 compact viewport. The sanitized record is
`research/current-integrations-26.915.json`; the isolated profile and raw
frames remain local-only.

This build no longer shares the 26.911 route copy: Plugins is titled
`Plugins` with `Work with Codex across your favorite tools`, while Skills is
titled `Skills` with `Extend Codex with task-specific skills`. Both routes
keep their catalog search mounted at 720px, unlike the 26.911 candidate.

Plugins records a two-column catalog at the native wide viewport (480px tracks
inside a 984px content root) and at 720px (327.5px tracks inside a 679px
content root). Skills records a 728px native-wide root with 360px tracks and a
679px compact root with 335.5px tracks. Both routes report zero horizontal
overflow. The visible labels are limited to the public catalog surface and
remain host-supplied.

The capture is read-only and does not install, uninstall, authorize, enable,
execute, or mutate any plugin or skill. It refreshes current package/runtime
evidence without promoting host-owned installation, permission, OAuth,
registry, network, third-party artwork, or product-pixel behavior to a
renderer guarantee. The `integrations.plugins` and `integrations.skills` rows
therefore remain `partial`.
